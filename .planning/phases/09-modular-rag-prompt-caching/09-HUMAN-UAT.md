---
status: failed
phase: 09-modular-rag-prompt-caching
source: [09-VERIFICATION.md]
started: 2026-04-18T21:47:35+07:00
updated: 2026-04-18T22:55:00+07:00
---

## Current Test

[blocked on StructuredOutputValidationAdvisor schema mismatch — see Gaps]

## Tests

### 1. VietnameseRegressionIT — 20-query live regression
expected: ≥95% accuracy on 20-query Vietnamese benchmark; refusal parity preserved; two-turn memory holds across follow-up query. Run with `OPENROUTER_API_KEY` set.
result: FAILED — 0/20 pass (relevancy=false on every query). Root cause: StructuredOutputValidationAdvisor rejects every LLM response due to schema mismatch (see Gap G1, G2). Every request falls back to "grounding refusal" → 100% refusal rate.

### 2. twoTurnConversationMemoryWorks — memory across turns
expected: Turn-2 answer references turn-1 topic (đèn đỏ / vượt / xe máy).
result: FAILED — Turn-2 returns out-of-scope refusal ("Câu hỏi này nằm ngoài phạm vi luật giao thông Việt Nam..."). Same root cause as test 1 (validation advisor rejects LLM response → refusal fallback).

### 3. refusalRateWithinTenPercentOfPhase7Baseline
expected: Refusal rate within ±10% of Phase-7 baseline.
result: FAILED — 100% refusal rate vs baseline NaN% (baseline not populated). Same root cause.

### 4. CitationFormatRegressionIT — byte-for-byte JSON contract parity (ARCH-05)
expected: `ChatAnswerResponse` JSON output byte-identical to Phase-8 baseline.
result: pending — cannot run meaningfully until G1/G2 are fixed; every response is currently the refusal fallback.

### 5. EmptyContextRefusalIT — verbatim refusal wording (T-9-02)
expected: Empty-context refusal text matches Phase-8 verbatim.
result: pending — blocked by G1/G2.

### 6. Post-deploy 50-sample trust-tier audit (D-03)
expected: Manual review of 50 production samples confirms trust-tier filtering and provenance.
result: pending — post-deploy item; not runnable pre-deploy.

## Summary

total: 6
passed: 0
issues: 3
pending: 3
skipped: 0
blocked: 0

## Gaps

### G1 — StructuredOutputValidationAdvisor rejects extra LLM fields (blocker)

LLM (Claude Sonnet via OpenRouter) returns extra top-level fields `confidence`, `intent`, `note` that are not declared in `LegalAnswerDraft`. Schema generated by `BeanOutputConverter` uses `additionalProperties: false`, so validation fails on every response:

```
property 'confidence' is not defined in the schema and the schema does not allow additional properties
property 'intent' is not defined ...
property 'note' is not defined ...
```

With `maxRepeatAttempts=1` (D-10, Pitfall 6), the retry also fails and the chain falls through to the grounding refusal.

Evidence: `build/test-results/liveTest/TEST-com.vn.traffic.chatbot.chat.regression.VietnameseRegressionIT.xml` (live run 2026-04-18T21:58).

Likely origin: a DB-stored `systemPrompt` in `ai_parameter_set` that instructs the LLM to emit `confidence`/`intent`/`note`. `LegalQueryAugmenter.java:50` loads it via `paramProvider.getString("systemPrompt", ...)`.

Candidate fixes (pick one):
- A — prompt-side: update `LegalQueryAugmenter.buildPromptTemplate` (~line 69) to explicitly forbid extra fields ("Khong them confidence/intent/note hoac bat ky truong nao khac").
- B — schema-side: add `confidence`/`intent`/`note` as optional fields to `LegalAnswerDraft.java`. Risk: may break ARCH-05 byte-for-byte parity with Phase 8.
- C — source-side: fix the DB systemPrompt so it no longer asks for those fields.

### G2 — Array-typed schema fields returned as strings

Schema declares `List<String>` for `legalBasis`, `penalties`, `requiredDocuments`, `procedureSteps`, `nextSteps`. LLM returns plain strings for them, producing:

```
/legalBasis: string found, array expected
/penalties, /requiredDocuments, /procedureSteps, /nextSteps: same
```

Fix: strengthen `buildPromptTemplate` (`LegalQueryAugmenter.java:69`) to state that these fields MUST be JSON arrays of strings (e.g. `"legalBasis": ["...", "..."]`). Consider a one-shot example in the template.

### G3 — maxRepeatAttempts=1 too low under current conditions

With G1+G2 making every first attempt invalid, one retry is not enough. Once G1/G2 are fixed, current value of 1 is likely fine (T-9-04 monitor retry rate before tightening to 0).

### G4 — SPRING_AI_CHAT_MEMORY.conversation_id VARCHAR(36) overflow (NEW — surfaced 2026-04-18 by Plan 09-03 live re-run)

`ChatService.java:80-81` builds `"ephemeral-" + UUID.randomUUID()` = 46 chars when caller passes `conversationId == null`. Column `SPRING_AI_CHAT_MEMORY.conversation_id` is `VARCHAR(36)`, so every memory INSERT throws PSQL `value too long for type character varying(36)`, ChatService catches it and returns `GroundingStatus.REFUSED`. This explains the 0/20 pass rate in `twentyQueryRegressionSuiteAtLeast95Percent` (test calls `chatService.answer(..., null)`). `twoTurnConversationMemoryWorks` passes a 36-char UUID directly so it fits and the test stays GREEN.

Fix candidates:
  - (A) Drop the `"ephemeral-"` prefix and use the bare UUID (36 chars) — 1-line change, no migration.
  - (B) Liquibase changeset to widen the column to `VARCHAR(64)` — preserves the prefix as a debug signal.

G1/G2 root cause is fully resolved at the prompt level (LLM JSON in the live log conforms to the 8-key LegalAnswerDraft schema with arrays). G4 is the new blocker for the 95% gate.

### G5 — IntentClassifier receives LegalAnswerDraft JSON instead of IntentDecision (NEW — surfaced 2026-04-18)

Every test request logs `BeanOutputConverter could not parse … into class IntentDecision` because the JSON it receives is a LegalAnswerDraft (8 keys, no `confidence`). IntentClassifier falls back to `LEGAL` per D-02, so it does NOT break tests, but it spams an error per request and wastes one LLM call. Likely cause: misrouted ChatClient or shared converter between the intent and answer chains. Lower priority than G4 — investigate after G4 fix.

### G6 — Phase7Baseline.REFUSAL_RATE_PERCENT = NaN (PRE-EXISTING from Plan 08-01)

`refusalRateWithinTenPercentOfPhase7Baseline` fails with `NaN` because `Phase7Baseline.REFUSAL_RATE_PERCENT` was never backfilled. Plan 08-01 documented this as a TODO and Phase7Baseline.java still has the placeholder. Out of scope for Phase 9 gap closure but blocks the test from going GREEN.

### Recommended next step

`/gsd-plan-phase 09 --gaps` — create a gap-closure plan that (a) fixes G4 (Bug A: ephemeral conversationId overflow), (b) optionally addresses G5 (IntentClassifier misroute), (c) re-runs `liveTest *VietnameseRegressionIT *CitationFormatRegressionIT *EmptyContextRefusalIT` to verify ≥95% pass rate. G6 stays deferred until Phase 7 baseline backfill.
