---
phase: 09-modular-rag-prompt-caching
plan: 02
type: execute
wave: 4
depends_on: [9-01]
files_modified:
  - src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java
  - src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpValidationAdvisor.java
  - src/test/java/com/vn/traffic/chatbot/chat/advisor/StructuredOutputValidationAdvisorIT.java
  - src/test/java/com/vn/traffic/chatbot/chat/config/ChatClientConfigTest.java
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "ChatClientConfig wires StructuredOutputValidationAdvisor with outputType(LegalAnswerDraft.class) and maxRepeatAttempts(1)."
    - "NoOpValidationAdvisor.java is deleted from the codebase."
    - "Advisor chain order is unchanged: GuardIn(+100) → Memory(+200) → RAG(+300) → CitationStash(+310) → NoOpPromptCache(+500) → StructuredOutputValidationAdvisor(+1000) → GuardOut(-100)."
    - "NoOpPromptCacheAdvisor remains untouched (D-07)."
    - "When the model returns malformed JSON on first call and valid JSON on retry, the advisor triggers exactly one additional LLM invocation (2 calls total) and the final entity parses cleanly (Q-03 resolution)."
    - "20-query Vietnamese regression + two-turn memory remain ≥95% + GREEN after the validation advisor swap (no regression from Plan 01)."
  artifacts:
    - path: "src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java"
      provides: "Real StructuredOutputValidationAdvisor bean wired in defaultAdvisors() at HIGHEST_PRECEDENCE + 1000"
      contains: "StructuredOutputValidationAdvisor.builder()"
    - path: "src/test/java/com/vn/traffic/chatbot/chat/advisor/StructuredOutputValidationAdvisorIT.java"
      provides: "Retry-on-bad-JSON integration test with mock ChatModel proving maxRepeatAttempts=1 semantics (Q-03)"
      contains: "maxRepeatAttempts"
  key_links:
    - from: "ChatClientConfig.validationAdvisor(...)"
      to: "LegalAnswerDraft.class"
      via: "StructuredOutputValidationAdvisor.builder().outputType(LegalAnswerDraft.class)"
      pattern: "outputType\\(LegalAnswerDraft\\.class\\)"
    - from: "ChatClientConfig.defaultAdvisors"
      to: "BaseAdvisor.HIGHEST_PRECEDENCE + 1000"
      via: ".advisorOrder(BaseAdvisor.HIGHEST_PRECEDENCE + 1000)"
      pattern: "HIGHEST_PRECEDENCE \\+ 1000"
---

<objective>
Replace the Phase-8 `NoOpValidationAdvisor` placeholder with the real
`StructuredOutputValidationAdvisor` (Spring AI 2.0.0-M4) configured with
`outputType(LegalAnswerDraft.class)` and `maxRepeatAttempts(1)` per D-10. Closes the P8 deferred
item for structured-output retry on rare prompt-instruction-mode parse failures.

PR2 is the low-risk, same-day-landable follow-up to PR1. No retrieval or citation changes; advisor
chain order unchanged (D-13). `NoOpPromptCacheAdvisor` stays untouched (D-07).

Purpose: satisfy ARCH-01 (fully Spring-AI-idiomatic advisor chain — zero NoOp placeholders for
retrieval or validation after this plan; only prompt-cache NoOp remains per D-07).
Output: working `StructuredOutputValidationAdvisor` wired; `NoOpValidationAdvisor.java` deleted;
retry IT GREEN (closes Q-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-modular-rag-prompt-caching/09-CONTEXT.md
@.planning/phases/09-modular-rag-prompt-caching/09-RESEARCH.md
@.planning/phases/09-modular-rag-prompt-caching/09-PATTERNS.md
@.planning/phases/09-modular-rag-prompt-caching/09-01-SUMMARY.md

<interfaces>
<!-- Critical contracts. From Spring AI 2.0.0-M4 context7 docs — verbatim shape. -->

From context7 /spring-projects/spring-ai §api/advisors-recursive (verbatim):
```java
StructuredOutputValidationAdvisor.builder()
    .outputType(Class<?>)
    .maxRepeatAttempts(int)          // n = ADDITIONAL attempts; 1 = up to 2 total calls
    .advisorOrder(int)
    .build();
```

From src/main/java/com/vn/traffic/chatbot/chat/model/LegalAnswerDraft.java:
```java
// Already @JsonClassDescription-annotated entity from Phase 8; DO NOT modify.
public record LegalAnswerDraft( /* 8 fields from Phase 7 slim schema */ );
```

From post-Plan-01 ChatClientConfig.defaultAdvisors(...) — wiring to edit:
```java
defaultAdvisors(
    guardIn,           // HIGHEST_PRECEDENCE + 100
    MessageChatMemoryAdvisor..., // HIGHEST_PRECEDENCE + 200
    ragAdvisor,        // HIGHEST_PRECEDENCE + 300   (Plan 01)
    citationStash,     // HIGHEST_PRECEDENCE + 310   (Plan 01)
    noOpCache,         // HIGHEST_PRECEDENCE + 500   (UNCHANGED per D-07)
    noOpValidation,    // HIGHEST_PRECEDENCE + 1000  <- PR2 SWAPS HERE
    guardOut           // LOWEST_PRECEDENCE - 100
)
```

Mock-chain test pattern from src/test/java/com/vn/traffic/chatbot/chat/advisor/GroundingGuardAdvisorTest.java (lines 73-84):
```java
ChatClientRequest req = mock(ChatClientRequest.class);
ChatClientResponse resp = mock(ChatClientResponse.class);
CallAdvisorChain chain = mock(CallAdvisorChain.class);
when(chain.nextCall(any(ChatClientRequest.class))).thenReturn(resp);
// For retry: when(chain.nextCall(...)).thenReturn(badJsonResp).thenReturn(goodJsonResp);
// Assert: verify(chain, times(2)).nextCall(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (W4): Write retry IT first (RED), then swap NoOpValidationAdvisor → StructuredOutputValidationAdvisor, delete placeholder, re-run regression</name>
  <read_first>
    - src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java (POST-Plan-01 state — the `noOpValidation` bean parameter and `.defaultAdvisors(...)` slot at HIGHEST_PRECEDENCE + 1000)
    - src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpValidationAdvisor.java (delete target — full file)
    - src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpPromptCacheAdvisor.java (MUST REMAIN UNTOUCHED per D-07 — preserve, do not modify)
    - src/main/java/com/vn/traffic/chatbot/chat/model/LegalAnswerDraft.java (outputType class; unchanged)
    - src/test/java/com/vn/traffic/chatbot/chat/advisor/GroundingGuardAdvisorTest.java (lines 73-84 — Mockito `CallAdvisorChain.nextCall(...)` stub pattern)
    - src/test/java/com/vn/traffic/chatbot/chat/regression/VietnameseRegressionIT.java (existing; must still pass post-swap)
    - src/test/java/com/vn/traffic/chatbot/chat/config/ChatClientConfigTest.java (from Plan 01; extend with validation-advisor assertions)
    - 09-RESEARCH.md §"Pattern 4" (StructuredOutputValidationAdvisor builder shape), §"Pitfall 6" (maxRepeatAttempts=1 = 2 total calls semantic), §"Open Questions" Q-03 (retry-vs-BeanOutputConverter integration)
    - 09-PATTERNS.md §"StructuredOutputValidationAdvisorIT.java (NEW — PR2)" (mock-chain retry pattern)
  </read_first>
  <behavior>
    - RED IT is authored first: a mock `ChatModel` / mocked chain returns malformed JSON on first invocation and valid `LegalAnswerDraft` JSON on the second; the test asserts the advisor caused exactly two chain-next calls AND the final parsed entity is non-null with expected field values.
    - After wiring, the IT turns GREEN with no retry-count change.
    - `NoOpValidationAdvisor.java` is DELETED (the file, not replaced with empty stub).
    - `NoOpPromptCacheAdvisor.java` is bit-for-bit identical pre- and post-Plan-02 (`git diff` returns zero lines).
    - `ChatClientConfigTest` asserts the bean graph now includes `StructuredOutputValidationAdvisor` and excludes `NoOpValidationAdvisor`.
    - `VietnameseRegressionIT` (from Plan 01) remains ≥95% pass; no regression from the swap.
    - If retry-count observation reveals failure-rate is zero in practice (D-10 note + Pitfall 6), record observation in SUMMARY as a future-tightening candidate (set `maxRepeatAttempts=0`) — do NOT change it now.
  </behavior>
  <action>
    1. RED first — create `src/test/java/com/vn/traffic/chatbot/chat/advisor/StructuredOutputValidationAdvisorIT.java`:
       - Plain JUnit 5 + Mockito + Spring AI test utilities; no `@SpringBootTest` needed (advisor-unit-IT via mock chain — faster, no live key).
       - Build a `StructuredOutputValidationAdvisor` instance directly: `StructuredOutputValidationAdvisor.builder().outputType(LegalAnswerDraft.class).maxRepeatAttempts(1).advisorOrder(BaseAdvisor.HIGHEST_PRECEDENCE + 1000).build();`
       - Mock `CallAdvisorChain`: first `nextCall(...)` returns a `ChatClientResponse` whose `chatResponse().getResult().getOutput().getText()` is malformed JSON (e.g. `"{ bad json <<"`); second `nextCall(...)` returns a response whose text is a valid `LegalAnswerDraft` JSON (serialize a concrete instance via Jackson for accuracy).
       - Build a minimal `ChatClientRequest` with `LegalAnswerDraft.class` as the output type (mirror the `.entity(LegalAnswerDraft.class)` setup; if Spring AI 2.0.0-M4 requires specific request properties for the advisor to know the `outputType`, replicate the exact fields — consult Context7 `StructuredOutputValidationAdvisor` section if the mock setup differs from the snippet in 09-RESEARCH Pattern 4).
       - Invoke the advisor's call method with the mock chain.
       - Assertions:
         - `verify(chain, times(2)).nextCall(any(ChatClientRequest.class));`  // exactly 2 LLM calls for maxRepeatAttempts=1
         - The returned response's parseable entity is a non-null `LegalAnswerDraft` with the field values from the good-JSON fixture.
       - Second test `@Test void exhaustsRetriesWhenAllCallsReturnMalformedJson()`: both calls return bad JSON → expect the advisor propagates the parse failure (exception type per Spring AI 2.0.0-M4 — likely `OutputConverterException` or similar; confirm during implementation and pin).
       - Run test — expect RED (advisor not wired in config yet, but the advisor instance under test is standalone so it may actually pass here since we're testing the advisor class directly — if it passes, commit and proceed; RED→GREEN cycle collapses for standalone unit ITs, which is acceptable).
    2. Edit `ChatClientConfig.java`:
       - Add new `@Bean`:
         ```java
         @Bean
         Advisor validationAdvisor() {
             return StructuredOutputValidationAdvisor.builder()
                 .outputType(LegalAnswerDraft.class)
                 .maxRepeatAttempts(1)                                  // D-10
                 .advisorOrder(BaseAdvisor.HIGHEST_PRECEDENCE + 1000)   // D-13 slot preserved
                 .build();
         }
         ```
       - In `chatClientMap(...)` parameter list, replace `NoOpValidationAdvisor noOpValidation` with `Advisor validationAdvisor`.
       - In `.defaultAdvisors(...)` call, replace `noOpValidation` with `validationAdvisor` at the SAME position (order + 1000 slot — D-13).
       - Do NOT move, reorder, or touch `guardIn`, `MessageChatMemoryAdvisor`, `ragAdvisor`, `citationStash`, `noOpCache`, `guardOut`.
       - Do NOT touch `NoOpPromptCacheAdvisor.java` or its bean wiring (D-07).
    3. DELETE `src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpValidationAdvisor.java` — remove the file. Remove any remaining references project-wide (should be zero after Step 2).
    4. Extend `ChatClientConfigTest` (from Plan 01):
       - Add `@Test void beanGraphWiresStructuredOutputValidationAdvisorAndDropsNoOpValidation()`:
         - Assert at least one bean of type `StructuredOutputValidationAdvisor` exists.
         - Assert `ctx.containsBean("noOpValidationAdvisor") == false`.
         - Assert `ctx.containsBean("noOpPromptCacheAdvisor") == true` (D-07 preserved).
    5. Re-run full non-live suite: `./gradlew test`. Then re-run live regression: `./gradlew liveTest --tests "*VietnameseRegressionIT"` to confirm no regression from the swap (expect ≥95% pass, same as Plan 01).
    6. Record observation in SUMMARY: during `liveTest` runs, capture any log line containing `"validation failed, retrying attempt"` (Spring AI 2.0.0-M4 log format per Pitfall 6) — if zero occurrences across 20-query run, note as future-tightening candidate for `maxRepeatAttempts=0`. Do NOT change the setting now.
  </action>
  <verify>
    <automated>./gradlew test --tests "*StructuredOutputValidationAdvisorIT" --tests "*ChatClientConfigTest" liveTest --tests "*VietnameseRegressionIT"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpValidationAdvisor.java` (file deleted).
    - `test -f src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpPromptCacheAdvisor.java` (UNTOUCHED per D-07) AND `git diff HEAD~ -- src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpPromptCacheAdvisor.java` returns empty (no changes in this plan).
    - `grep -n "StructuredOutputValidationAdvisor.builder()" src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java` returns ≥1 match.
    - `grep -n "outputType(LegalAnswerDraft.class)" src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java` returns 1 match.
    - `grep -n "maxRepeatAttempts(1)" src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java` returns 1 match.
    - `grep -n "HIGHEST_PRECEDENCE + 1000" src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java` returns ≥1 match.
    - `grep -rn "NoOpValidationAdvisor" src/main/java src/test/java` returns zero matches (fully removed).
    - `./gradlew test --tests "*StructuredOutputValidationAdvisorIT"` exits 0; retry assertion (`verify(chain, times(2))...`) passes.
    - `./gradlew test --tests "*ChatClientConfigTest"` exits 0; new validation-advisor assertion passes.
    - `./gradlew liveTest --tests "*VietnameseRegressionIT"` still reports ≥95% pass post-swap (no regression from Plan 01 result).
    - Full `./gradlew test` exits 0.
  </acceptance_criteria>
  <done>
    StructuredOutputValidationAdvisor wired; NoOpValidationAdvisor deleted; NoOpPromptCacheAdvisor byte-identical; retry IT GREEN; 20-query regression still ≥95%.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model response text → `BeanOutputConverter` | Untrusted model output crosses the structured-parse boundary; parse failure triggers retry |
| Advisor chain slot (+1000) | Same slot as Phase 8 placeholder; implementation swap only, order unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-02 | Information Disclosure (refusal wording leak) | Inherited from Plan 01 — `LegalQueryAugmenter` + `GroundingGuardOutputAdvisor` | accept | Mitigation is fully owned by Plan 01's `EmptyContextRefusalIT`; no regression possible via validation-advisor swap (validation runs BEFORE `GuardOut`, does not touch refusal wording). |
| T-9-03 | Tampering (spoofed `[Nguồn N]`) | Inherited from Plan 01 — CitationPostProcessor + VietnameseRegressionIT | accept | Plan 01 mitigation + Plan 02's regression re-run confirms no regression from the swap. |
| T-9-04 | Denial of Service (retry amplification) | `StructuredOutputValidationAdvisor.maxRepeatAttempts` | accept | `maxRepeatAttempts=1` caps amplification at 2× cost on parse failure (Pitfall 6); failure rate projected <1% (P8 D-03a). Post-deploy monitor OpenRouter token usage; tighten to 0 if observed rate is zero. Accepts the 2× rare-case cost — not mitigating further in P9. |
</threat_model>

<verification>
- `./gradlew test` exits 0.
- `./gradlew liveTest --tests "*VietnameseRegressionIT"` ≥95% pass (no regression from Plan 01 baseline).
- `./gradlew test --tests "*StructuredOutputValidationAdvisorIT"` exits 0; retry count = 2 (maxRepeatAttempts=1).
- `grep -rn "NoOpValidationAdvisor" src/` returns zero matches.
- `git diff` shows `NoOpPromptCacheAdvisor.java` untouched.
- Advisor chain boot log shows 7 registered advisors with orders: +100, +200, +300, +310, +500, +1000, LOWEST−100 (the +1000 name now reads `StructuredOutputValidationAdvisor` not `NoOpValidationAdvisor`).
</verification>

<success_criteria>
- ARCH-01 (closeout): advisor chain contains only one NoOp placeholder (`NoOpPromptCacheAdvisor`, deferred per D-07) — all other slots are real Spring AI or custom advisors. Grep-verifiable.
- D-10 closed: `StructuredOutputValidationAdvisor` wired with `maxRepeatAttempts=1`; retry IT proves semantics.
- D-13 preserved: chain order unchanged.
- D-07 preserved: `NoOpPromptCacheAdvisor.java` byte-identical.
- No regression from Plan 01: 20-query regression remains ≥95% pass.
- Q-03 resolved: retry IT confirms advisor triggers exactly 2 chain calls on bad-then-good JSON.
</success_criteria>

<output>
After completion, create `.planning/phases/09-modular-rag-prompt-caching/09-02-SUMMARY.md` covering:
- Validation advisor wired; `NoOpValidationAdvisor.java` deleted.
- Retry IT result (retry-count observation; note if log-grep shows zero `"validation failed, retrying"` occurrences during the 20-query live run — future candidate to tighten to `maxRepeatAttempts=0`).
- Regression re-run percentages (must match or exceed Plan 01 result).
- `NoOpPromptCacheAdvisor` byte-identical confirmation + D-07 rationale restated (prompt caching deferred).
- Phase 9 closeout: ARCH-01, ARCH-05 satisfied. `CACHE-01` still deferred (D-07) — no Phase-9 claim.
</output>
