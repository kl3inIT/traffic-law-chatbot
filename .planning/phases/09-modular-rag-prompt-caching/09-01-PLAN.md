---
phase: 09-modular-rag-prompt-caching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/vn/traffic/chatbot/chat/advisor/LegalQueryAugmenter.java
  - src/main/java/com/vn/traffic/chatbot/chat/advisor/CitationPostProcessor.java
  - src/main/java/com/vn/traffic/chatbot/chat/advisor/CitationStashAdvisor.java
  - src/main/java/com/vn/traffic/chatbot/chat/advisor/context/ChatAdvisorContextKeys.java
  - src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java
  - src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java
  - src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java
  - src/main/java/com/vn/traffic/chatbot/chat/citation/CitationMapper.java
  - src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpRetrievalAdvisor.java
  - src/test/java/com/vn/traffic/chatbot/chat/advisor/LegalQueryAugmenterTest.java
  - src/test/java/com/vn/traffic/chatbot/chat/advisor/CitationPostProcessorTest.java
  - src/test/java/com/vn/traffic/chatbot/chat/regression/CitationFormatRegressionIT.java
  - src/test/java/com/vn/traffic/chatbot/chat/regression/VietnameseRegressionIT.java
  - src/test/java/com/vn/traffic/chatbot/chat/regression/EmptyContextRefusalIT.java
  - src/test/java/com/vn/traffic/chatbot/chat/archunit/ChatServiceShrinkArchTest.java
  - src/test/java/com/vn/traffic/chatbot/chat/config/ChatClientConfigTest.java
autonomous: true
requirements: [ARCH-01, ARCH-05]
must_haves:
  truths:
    - "User chat flow runs through RetrievalAugmentationAdvisor; ChatService contains zero vectorStore.similaritySearch calls."
    - "ChatService.doAnswer LOC is ≥60% smaller than its P8 baseline."
    - "[Nguồn n] citation labels and ChatAnswerResponse/sources[]/citations[] JSON are byte-for-byte identical to v1.0 fixtures."
    - "FILTER_EXPRESSION enforces RetrievalPolicy.RETRIEVAL_FILTER literal (approvalState == 'APPROVED' && trusted == 'true' && active == 'true') on every retrieval."
    - "Empty-context retrieval produces an augmented prompt (allowEmptyContext=true), and GroundingGuardOutputAdvisor owns refusal wording verbatim from AnswerComposer.composeRefusal()."
    - "20-query Vietnamese regression passes ≥95% with real retrieval; refusal rate is within 10% of P7 baseline; two-turn memory test passes."
    - "NoOpPromptCacheAdvisor is preserved unchanged in the advisor chain."
  artifacts:
    - path: "src/main/java/com/vn/traffic/chatbot/chat/advisor/LegalQueryAugmenter.java"
      provides: "Custom QueryAugmenter (Vietnamese [Nguồn n] template, allowEmptyContext=true)"
      contains: "class LegalQueryAugmenter"
    - path: "src/main/java/com/vn/traffic/chatbot/chat/advisor/CitationPostProcessor.java"
      provides: "DocumentPostProcessor assigning labelNumber metadata 1..n"
      contains: "implements DocumentPostProcessor"
    - path: "src/main/java/com/vn/traffic/chatbot/chat/advisor/CitationStashAdvisor.java"
      provides: "Thin CallAdvisor reading RetrievalAugmentationAdvisor.DOCUMENT_CONTEXT and publishing List<CitationResponse> to response context (Q-01 resolution)"
      contains: "implements CallAdvisor"
    - path: "src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java"
      provides: "Real RetrievalAugmentationAdvisor wired in place of NoOpRetrievalAdvisor"
      contains: "RetrievalAugmentationAdvisor.builder()"
  key_links:
    - from: "ChatClientConfig.retrievalAdvisor(...)"
      to: "RetrievalPolicy.RETRIEVAL_FILTER"
      via: "VectorStoreDocumentRetriever.builder().filterExpression(Supplier<FilterExpression>)"
      pattern: "FilterExpressionTextParser.*RetrievalPolicy\\.RETRIEVAL_FILTER"
    - from: "CitationPostProcessor"
      to: "Document.metadata[\"labelNumber\"]"
      via: "build new Document with HashMap<>(metadata).put(\"labelNumber\", i+1)"
      pattern: "labelNumber"
    - from: "CitationStashAdvisor"
      to: "ChatClientResponse.context()[CITATIONS_KEY, SOURCES_KEY]"
      via: "reads RetrievalAugmentationAdvisor.DOCUMENT_CONTEXT, calls CitationMapper, publishes lists"
      pattern: "RetrievalAugmentationAdvisor\\.DOCUMENT_CONTEXT"
    - from: "ChatService.doAnswer"
      to: "ChatClientResponse.context()[CITATIONS_KEY]"
      via: "response.getMetadata().get(ChatAdvisorContextKeys.CITATIONS_KEY)"
      pattern: "ChatAdvisorContextKeys\\.CITATIONS_KEY"
---

<objective>
Replace the Phase-8 `NoOpRetrievalAdvisor` placeholder with a real Spring AI modular RAG pipeline:
`RetrievalAugmentationAdvisor` wired with `VectorStoreDocumentRetriever` (Supplier-based per-request
`topK` / `similarityThreshold` / `FILTER_EXPRESSION` per D-01, D-02), a custom
`LegalQueryAugmenter extends ContextualQueryAugmenter` that owns Vietnamese `[Nguồn n]` numbering
verbatim from `ChatPromptFactory` (D-04), a `CitationPostProcessor implements DocumentPostProcessor`
assigning `labelNumber` metadata pre-augmentation (D-04), and a `CitationStashAdvisor` wrapping the
RAG advisor that publishes `List<CitationResponse>` + `List<SourceReferenceResponse>` to
`ChatClientResponse.context()` (Q-01 resolution). `ChatService.doAnswer` shrinks ≥60% in LOC — all
retrieval, citation mapping, and prompt construction move into the advisor chain; `doAnswer` reads
citations/sources from advisor context post-call (D-05). `ChatPromptFactory` is absorbed into
`LegalQueryAugmenter` and deleted.

PR1 is the risk carrier — it closes the two Phase-8 deferred regression items
(`twentyQueryRegressionSuiteAtLeast95Percent`, `refusalRateWithinTenPercentOfPhase7Baseline`) plus
the two-turn memory test (D-11). Must pass all three before merge.

Purpose: satisfy ARCH-01 (advisor-driven orchestration) and ARCH-05 (byte-for-byte citation /
response JSON contract preservation).
Output: working Spring AI modular RAG chain; zero `vectorStore.similaritySearch` in `ChatService`;
v1.0 regression fixtures green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-modular-rag-prompt-caching/09-CONTEXT.md
@.planning/phases/09-modular-rag-prompt-caching/09-RESEARCH.md
@.planning/phases/09-modular-rag-prompt-caching/09-PATTERNS.md
@.planning/phases/08-structured-output-groundingguardadvisor/08-CONTEXT.md

<interfaces>
<!-- Critical contracts the executor must honor. Extracted from repo. No exploration needed. -->

From src/main/java/com/vn/traffic/chatbot/retrieval/RetrievalPolicy.java:
```java
public static final String RETRIEVAL_FILTER =
    "approvalState == 'APPROVED' && trusted == 'true' && active == 'true';
public SearchRequest buildRequest(String query, int topKOverride);
public int getTopK();
public double getSimilarityThreshold();
```

From src/main/java/com/vn/traffic/chatbot/chat/citation/CitationMapper.java:
```java
// Existing format (DO NOT CHANGE WORDING):
// inlineLabel = "Nguồn " + labelNumber
public List<CitationResponse> toCitations(List<Document> documents);
public List<SourceReferenceResponse> toSources(List<CitationResponse> citations);
```

From src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java (to be ABSORBED):
Line 56 Vietnamese rule — MUST survive byte-for-byte in LegalQueryAugmenter template:
"Mọi nhận định có căn cứ phải gắn nhãn trích dẫn nội dòng đúng định dạng [Nguồn n]; tuyệt đối không tự tạo nhãn ngoài danh sách được cung cấp."
Line 87 formatCitation format — MUST be emitted byte-for-byte by LegalQueryAugmenter per-doc:
```
"- [" + citation.inlineLabel() + "] "
    + nullSafe(citation.sourceTitle())
    + " | origin=" + nullSafe(citation.origin())
    + " | page=" + (citation.pageNumber() == null ? "null" : citation.pageNumber())
    + " | section=" + nullSafe(citation.sectionRef())
    + " | excerpt=" + nullSafe(citation.excerpt());
```

From Spring AI 2.0.0-M4 (context7 /spring-projects/spring-ai §api/retrieval-augmented-generation):
```java
RetrievalAugmentationAdvisor.builder()
    .documentRetriever(VectorStoreDocumentRetriever.builder()
        .vectorStore(vectorStore)
        .similarityThreshold(Supplier<Double> | double)
        .topK(Supplier<Integer> | int)
        .filterExpression(Supplier<FilterExpression> | FilterExpression)
        .build())
    .documentPostProcessors(DocumentPostProcessor...)
    .queryAugmenter(QueryAugmenter)
    .build();
// Response metadata key: RetrievalAugmentationAdvisor.DOCUMENT_CONTEXT -> List<Document>
```

From Phase 8 (ChatClientConfig.java lines 98-107 — current wiring to edit):
```java
defaultAdvisors(
    guardIn,                                              // HIGHEST_PRECEDENCE + 100
    MessageChatMemoryAdvisor.builder(chatMemory).build(), // HIGHEST_PRECEDENCE + 200
    noOpRag,                                              // HIGHEST_PRECEDENCE + 300  <- SWAP
    noOpCache,                                            // HIGHEST_PRECEDENCE + 500  (UNCHANGED per D-07)
    noOpValidation,                                       // HIGHEST_PRECEDENCE + 1000 (PR2 swaps)
    guardOut                                              // LOWEST_PRECEDENCE  - 100
)
```
PR1 inserts CitationStashAdvisor at HIGHEST_PRECEDENCE + 310 (immediately after RAG, before noOpCache).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (W1): Scaffold Wave-0 test skeletons + context-key constants + label-prefix extraction</name>
  <read_first>
    - src/main/java/com/vn/traffic/chatbot/chat/citation/CitationMapper.java (line 61 — `"Nguồn " + labelNumber`)
    - src/main/java/com/vn/traffic/chatbot/chat/advisor/GroundingGuardInputAdvisor.java (lines 28-59 — `public static final String FORCE_REFUSAL = "chat.guard.forceRefusal"` pattern)
    - src/main/java/com/vn/traffic/chatbot/chat/advisor/GroundingGuardOutputAdvisor.java (advisor component shape + `@PostConstruct void logOrder()`)
    - src/test/java/com/vn/traffic/chatbot/chat/advisor/GroundingGuardAdvisorTest.java (JUnit+AssertJ+Mockito shape — lines 20-33, 73-84)
    - src/test/java/com/vn/traffic/chatbot/chat/archunit/ChatServiceDeletionArchTest.java (file-parse archunit shape — lines 19-43)
    - src/test/java/com/vn/traffic/chatbot/chat/regression/StructuredOutputMatrixIT.java (lines 34-43 — `@SpringBootTest @Tag("live") @DisabledIfEnvironmentVariable` block)
    - 09-PATTERNS.md §"Shared Patterns" (label-format single source of truth; Pitfall 4 defense)
  </read_first>
  <behavior>
    - Extract label prefix constant so LegalQueryAugmenter and CitationMapper read from the SAME source (Pitfall 4).
    - Publish advisor-context key constants for citations/sources passback.
    - Create RED test scaffolds for every Wave-0 gap listed in RESEARCH §"Validation Architecture".
    - Test names must express behavior (e.g., `emitsInlineLabelPrefixMatchingCitationMapper`, `chatServiceNoLongerReferencesVectorStoreSimilaritySearch`).
  </behavior>
  <action>
    1. Add `public static final String INLINE_LABEL_PREFIX = "Nguồn ";` to `CitationMapper.java` (just above line 61). Update the existing `toCitation` line to use the constant: `INLINE_LABEL_PREFIX + labelNumber`. No other change.
    2. Create `src/main/java/com/vn/traffic/chatbot/chat/advisor/context/ChatAdvisorContextKeys.java` — final class with `private ChatAdvisorContextKeys() {}` and three constants:
       - `public static final String CITATIONS_KEY = "chat.rag.citations";`
       - `public static final String SOURCES_KEY = "chat.rag.sources";`
       - `public static final String LABEL_NUMBER_METADATA = "labelNumber";`
    3. Create RED test scaffolds (each compiles but fails until implementation lands):
       - `src/test/java/com/vn/traffic/chatbot/chat/advisor/LegalQueryAugmenterTest.java` — one `@Test void emitsCitationBlockByteForByteMatchingChatPromptFactoryFormatCitation()` with `fail("RED — implement LegalQueryAugmenter first")`.
       - `src/test/java/com/vn/traffic/chatbot/chat/advisor/CitationPostProcessorTest.java` — `@Test void assignsLabelNumberOneThroughNInRetrievalOrder()` with `fail("RED")`.
       - `src/test/java/com/vn/traffic/chatbot/chat/regression/CitationFormatRegressionIT.java` — live IT shell with `@SpringBootTest @Tag("live") @DisabledIfEnvironmentVariable(named="OPENROUTER_API_KEY", matches="^$")` + one `@Test void v1FixtureReplayPreservesCitationsAndSourcesByteForByte()` with `fail("RED")`.
       - `src/test/java/com/vn/traffic/chatbot/chat/regression/EmptyContextRefusalIT.java` — live IT shell + `@Test void emptyRetrievalRoutesThroughGroundingGuardRefusalVerbatim()` with `fail("RED")`.
       - `src/test/java/com/vn/traffic/chatbot/chat/archunit/ChatServiceShrinkArchTest.java` — copy shape from `ChatServiceDeletionArchTest.java`; list of deleted identifiers = `["vectorStore", "similaritySearch", "citationMapper.toCitations", "chatPromptFactory", "buildPrompt", "ChatPromptFactory"]`; single `@Test` reads `ChatService.java` and asserts each identifier absent. Initially fails (those identifiers still exist) — RED.
       - `src/test/java/com/vn/traffic/chatbot/chat/config/ChatClientConfigTest.java` — `@SpringBootTest` + `@Autowired ChatClient chatClient` assertions: bean graph must NOT contain `NoOpRetrievalAdvisor`; must contain `RetrievalAugmentationAdvisor`. Initially fails — RED.
    4. Do NOT modify `LegalQueryAugmenter` / `CitationPostProcessor` / `ChatClientConfig` / `ChatService` yet — those land in Tasks 2/3/4.
  </action>
  <verify>
    <automated>./gradlew compileJava compileTestJava</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "INLINE_LABEL_PREFIX" src/main/java/com/vn/traffic/chatbot/chat/citation/CitationMapper.java` shows a declaration line AND a consuming line (both present).
    - `grep -n "CITATIONS_KEY\|SOURCES_KEY\|LABEL_NUMBER_METADATA" src/main/java/com/vn/traffic/chatbot/chat/advisor/context/ChatAdvisorContextKeys.java` returns exactly 3 matches.
    - Six new test files exist under `src/test/java/com/vn/traffic/chatbot/chat/{advisor,regression,archunit,config}/`.
    - `./gradlew compileJava compileTestJava` exits 0.
    - `./gradlew test --tests "*LegalQueryAugmenterTest"` fails with message containing `"RED"` (proves scaffold wired).
    - `./gradlew test --tests "*CitationMapperTest"` still passes (constant extraction did not change output).
  </acceptance_criteria>
  <done>
    Constants landed; 6 RED test scaffolds compile; existing CitationMapperTest still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (W1): Implement LegalQueryAugmenter + CitationPostProcessor + CitationStashAdvisor (unit-tested, not yet wired)</name>
  <read_first>
    - src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java (full file — the logic to PORT; lines 44, 53-69, 87 are load-bearing)
    - src/main/java/com/vn/traffic/chatbot/chat/advisor/GroundingGuardOutputAdvisor.java (advisor-component shape, `@PostConstruct void logOrder()`)
    - src/main/java/com/vn/traffic/chatbot/retrieval/RetrievalPolicy.java (for `ActiveParameterSetProvider` injection pattern, lines 8-27)
    - src/main/java/com/vn/traffic/chatbot/chat/citation/CitationMapper.java (callee — `toCitations`, `toSources`; unchanged)
    - src/test/java/com/vn/traffic/chatbot/chat/citation/CitationMapperTest.java (lines 17-42 — `Document.builder().text(...).metadata(Map.of(...)).build()` fixture pattern)
    - 09-RESEARCH.md §"Pattern 2" (LegalQueryAugmenter), §"Pattern 3" (CitationPostProcessor), Pitfall 3 (CitationStashAdvisor need), Pitfall 4 (byte drift)
    - 09-PATTERNS.md §"LegalQueryAugmenter.java (NEW)" and §"CitationPostProcessor.java (NEW)"
  </read_first>
  <behavior>
    - LegalQueryAugmenter.augment(query, emptyList) returns an augmented Query (not null, not a refusal) — allowEmptyContext=true.
    - LegalQueryAugmenter.augment(query, 3-doc fixture) produces a prompt whose citation block substring is byte-for-byte identical to `ChatPromptFactory.formatCitation` output on the same 3 citations.
    - Vietnamese rule text on line 56 of ChatPromptFactory is present verbatim in the LegalQueryAugmenter template.
    - CitationPostProcessor.process(query, docs) returns a new `List<Document>` of identical size where each doc has `metadata.get("labelNumber")` equal to its 1-based retrieval-order index; original docs not mutated.
    - CitationStashAdvisor reads `RetrievalAugmentationAdvisor.DOCUMENT_CONTEXT` from `ChatClientResponse` after the RAG advisor runs, invokes `CitationMapper.toCitations` + `toSources`, and writes both lists to `ChatClientResponse.context()` under `ChatAdvisorContextKeys.CITATIONS_KEY` / `SOURCES_KEY`.
    - CitationStashAdvisor order = `Ordered.HIGHEST_PRECEDENCE + 310` (between RAG at +300 and NoOpPromptCache at +500).
  </behavior>
  <action>
    1. Create `src/main/java/com/vn/traffic/chatbot/chat/advisor/LegalQueryAugmenter.java`:
       - `package com.vn.traffic.chatbot.chat.advisor;`
       - `@Slf4j @Component @RequiredArgsConstructor public final class LegalQueryAugmenter implements QueryAugmenter { ... }`
       - Inject `ActiveParameterSetProvider paramProvider` (same style as `RetrievalPolicy`) so `systemPrompt = paramProvider.getString("systemPrompt", SYSTEM_CONTEXT_FALLBACK)` is preserved.
       - Build an internal `ContextualQueryAugmenter delegate` via `ContextualQueryAugmenter.builder().allowEmptyContext(true).promptTemplate(LEGAL_PROMPT_TEMPLATE).emptyContextPromptTemplate(EMPTY_CONTEXT_TEMPLATE).build();`
       - `LEGAL_PROMPT_TEMPLATE` (static `PromptTemplate`) MUST include the verbatim Vietnamese line 56 rule from `ChatPromptFactory.java` ("Mọi nhận định có căn cứ phải gắn nhãn trích dẫn nội dòng đúng định dạng [Nguồn n]; tuyệt đối không tự tạo nhãn ngoài danh sách được cung cấp.") plus the full instruction block from ChatPromptFactory lines 53-69 (conclusion-first, section order, JSON-only, required keys, section headers `Lịch sử hội thoại` / `Câu hỏi người dùng` / `Danh sách trích dẫn được phép dùng`). Placeholders: `{query}`, `{question_answer_context}`.
       - Override `public Query augment(Query query, List<Document> documents)`:
         - For each doc, build a NEW `Document` (via builder) whose text is the byte-for-byte replica of `ChatPromptFactory.formatCitation` line 87 output, computed from `doc.getMetadata()` keys `labelNumber`, `sourceTitle`, `origin`, `pageNumber`, `sectionRef`, `excerpt` (nullSafe → `"null"` for missing).
         - Label prefix: use `CitationMapper.INLINE_LABEL_PREFIX + labelNumber` (from Task 1).
         - Delegate to `delegate.augment(query, formattedDocs)`.
    2. Create `src/main/java/com/vn/traffic/chatbot/chat/advisor/CitationPostProcessor.java`:
       - `@Slf4j @Component @RequiredArgsConstructor public final class CitationPostProcessor implements DocumentPostProcessor`
       - `public List<Document> process(Query query, List<Document> documents)`:
         ```java
         List<Document> labeled = new ArrayList<>(documents.size());
         for (int i = 0; i < documents.size(); i++) {
             Document d = documents.get(i);
             Map<String, Object> md = new HashMap<>(d.getMetadata());
             md.put(ChatAdvisorContextKeys.LABEL_NUMBER_METADATA, i + 1);
             labeled.add(Document.builder().id(d.getId()).text(d.getText()).metadata(md).score(d.getScore()).build());
         }
         return labeled;
         ```
       - Do NOT mutate input metadata in place (Pitfall: immutable maps from pgvector).
       - Do NOT call `CitationMapper` here — that belongs to `CitationStashAdvisor` (see Pitfall 3 / Q-01 resolution).
    3. Create `src/main/java/com/vn/traffic/chatbot/chat/advisor/CitationStashAdvisor.java`:
       - `@Slf4j @Component @RequiredArgsConstructor public final class CitationStashAdvisor implements CallAdvisor`
       - `private final CitationMapper citationMapper;`
       - `private static final int ORDER = Ordered.HIGHEST_PRECEDENCE + 310;`
       - `getName() = "CitationStashAdvisor"; getOrder() = ORDER;` + `@PostConstruct void logOrder() { log.info("Advisor registered: {} order={}", getName(), ORDER); }`
       - `public ChatClientResponse adviseCall(ChatClientRequest req, CallAdvisorChain chain) {`
         - `ChatClientResponse resp = chain.nextCall(req);`
         - `@SuppressWarnings("unchecked") List<Document> docs = (List<Document>) resp.chatResponse().getMetadata().get(RetrievalAugmentationAdvisor.DOCUMENT_CONTEXT);`
         - `if (docs == null) docs = List.of();`
         - `List<CitationResponse> citations = citationMapper.toCitations(docs);`
         - `List<SourceReferenceResponse> sources = citationMapper.toSources(citations);`
         - `resp.context().put(ChatAdvisorContextKeys.CITATIONS_KEY, citations);`
         - `resp.context().put(ChatAdvisorContextKeys.SOURCES_KEY, sources);`
         - `return resp;`
       - `}` (Resolves Research Q-01: thin CallAdvisor is the explicit, idiomatic pattern over ThreadLocal.)
    4. Implement `LegalQueryAugmenterTest.emitsCitationBlockByteForByteMatchingChatPromptFactoryFormatCitation`:
       - Build 3 `CitationResponse` fixtures (varied `sourceTitle`, `origin`, `pageNumber`, `sectionRef`, `excerpt`, including one with a null `pageNumber`).
       - Convert each to a `Document` with metadata mirroring `CitationMapper.toCitation` output (labelNumber 1/2/3, sourceTitle, origin, pageNumber, sectionRef, excerpt).
       - Instantiate `ChatPromptFactory` (with a stub `ActiveParameterSetProvider` returning defaults) and compute `formatCitation(List<CitationResponse>)` reference string.
       - Instantiate `LegalQueryAugmenter` with the same stub provider; call `.augment(new Query("câu hỏi"), labeledDocs)`; extract the citation-block substring from the augmented Query text.
       - Assert `augmenterBlock` contains each line from the `ChatPromptFactory` reference block verbatim.
    5. Implement `CitationPostProcessorTest.assignsLabelNumberOneThroughNInRetrievalOrder` + second `@Test void preservesOriginalDocumentIdTextAndScore` + third `@Test void doesNotMutateInputMetadata`.
    6. Keep `NoOpRetrievalAdvisor.java` in place for this task — it is STILL wired in `ChatClientConfig` until Task 3.
    7. Do NOT touch `ChatClientConfig.java` / `ChatService.java` yet.
  </action>
  <verify>
    <automated>./gradlew test --tests "*LegalQueryAugmenterTest" --tests "*CitationPostProcessorTest" --tests "*CitationMapperTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Mọi nhận định có căn cứ phải gắn nhãn trích dẫn nội dòng đúng định dạng \[Nguồn n\]" src/main/java/com/vn/traffic/chatbot/chat/advisor/LegalQueryAugmenter.java` returns ≥1 match.
    - `grep -n "allowEmptyContext(true)" src/main/java/com/vn/traffic/chatbot/chat/advisor/LegalQueryAugmenter.java` returns ≥1 match.
    - `grep -n "implements DocumentPostProcessor" src/main/java/com/vn/traffic/chatbot/chat/advisor/CitationPostProcessor.java` returns 1 match.
    - `grep -n "HIGHEST_PRECEDENCE + 310" src/main/java/com/vn/traffic/chatbot/chat/advisor/CitationStashAdvisor.java` returns 1 match.
    - `grep -n "RetrievalAugmentationAdvisor.DOCUMENT_CONTEXT" src/main/java/com/vn/traffic/chatbot/chat/advisor/CitationStashAdvisor.java` returns 1 match.
    - `./gradlew test --tests "*LegalQueryAugmenterTest" --tests "*CitationPostProcessorTest"` exits 0 (both GREEN).
    - `./gradlew test --tests "*CitationMapperTest"` still exits 0 (no regression from prefix extraction).
    - Existing `./gradlew test` (non-live) suite still compiles and passes at baseline (no advisor chain wired yet → NoOpRetrievalAdvisor still active, P8 tests still green).
  </acceptance_criteria>
  <done>
    LegalQueryAugmenter + CitationPostProcessor + CitationStashAdvisor exist and unit-tested GREEN; wiring still uses P8 NoOpRetrievalAdvisor.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (W2): Wire real advisors in ChatClientConfig; delete NoOpRetrievalAdvisor; run empty-context IT</name>
  <read_first>
    - src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java (FULL FILE — lines 51-121 are the edit target; current P8 wiring at lines 98-107)
    - src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpRetrievalAdvisor.java (delete target)
    - src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpPromptCacheAdvisor.java (MUST remain UNTOUCHED per D-07 — preserved, not no-op-claimed)
    - src/main/java/com/vn/traffic/chatbot/retrieval/RetrievalPolicy.java (for `RETRIEVAL_FILTER`, `getTopK`, `getSimilarityThreshold` method references)
    - 09-RESEARCH.md §"Pattern 1" (full advisor builder) + Pitfall 5 (method-reference vs value-call for Supplier)
    - 09-RESEARCH.md §"Open Questions" Q-02 (Supplier support on Builder — if absent, fallback)
    - 09-PATTERNS.md §"ChatClientConfig.java" (current wiring shape + P9 swap shape)
  </read_first>
  <behavior>
    - Two new `@Bean` methods exist in `ChatClientConfig`: `Advisor retrievalAdvisor(...)` and the existing `chatClientMap(...)` swaps `NoOpRetrievalAdvisor` out for the new `Advisor retrievalAdvisor` parameter and adds `CitationStashAdvisor` to the chain at order 310.
    - `NoOpRetrievalAdvisor.java` deleted; Spring context boots successfully.
    - `NoOpPromptCacheAdvisor.java` untouched (preserved at HIGHEST_PRECEDENCE + 500).
    - Advisor chain order verifiable: GuardIn(+100) → Memory(+200) → RAG(+300) → CitationStash(+310) → NoOpPromptCache(+500) → NoOpValidation(+1000, swapped in PR2) → GuardOut(-100 from LOWEST).
    - `ChatClientConfigTest` (from Task 1) now GREEN: bean graph includes `RetrievalAugmentationAdvisor` and `CitationStashAdvisor`; excludes `NoOpRetrievalAdvisor`.
    - `EmptyContextRefusalIT` (from Task 1) now GREEN: a query with zero retrieval results (pgvector returns no docs for a nonsense query) routes through `GroundingGuardOutputAdvisor` and returns `AnswerComposer.composeRefusal()` wording verbatim — NOT Spring AI's generic refusal. Closes T-9-02.
  </behavior>
  <action>
    1. In `ChatClientConfig.java`, add a new `@Bean`:
       ```java
       @Bean
       Advisor retrievalAdvisor(VectorStore vectorStore,
                                RetrievalPolicy retrievalPolicy,
                                CitationPostProcessor citationPostProcessor,
                                LegalQueryAugmenter legalQueryAugmenter) {
           return RetrievalAugmentationAdvisor.builder()
               .documentRetriever(VectorStoreDocumentRetriever.builder()
                   .vectorStore(vectorStore)
                   .similarityThreshold(retrievalPolicy::getSimilarityThreshold)  // D-02, Pitfall 5
                   .topK(retrievalPolicy::getTopK)                                 // D-02
                   .filterExpression(() -> new FilterExpressionTextParser()
                       .parse(RetrievalPolicy.RETRIEVAL_FILTER))                   // D-01, T-9-01 mitigation
                   .build())
               .documentPostProcessors(citationPostProcessor)
               .queryAugmenter(legalQueryAugmenter)
               .build();
       }
       ```
       Add `.order(Ordered.HIGHEST_PRECEDENCE + 300)` explicitly on the `RetrievalAugmentationAdvisor` registration — do NOT rely on Spring AI's internal default (target slot is authoritative per D-13 chain order, not inferred). Q-02 RESOLVED: prefer method-reference Supplier on the retriever builder (`.similarityThreshold(retrievalPolicy::getSimilarityThreshold)` + `.topK(retrievalPolicy::getTopK)`); fallback (Pitfall 5) only if the overloads are absent — build a fresh `SearchRequest` per call from `RetrievalPolicy.buildRequest(query, topK)` inside a thin `DocumentRetriever` decorator preserving D-02 intent.
    2. In `chatClientMap(...)` (lines 52-59), replace `NoOpRetrievalAdvisor noOpRag` parameter with `Advisor ragAdvisor`. Add `CitationStashAdvisor citationStash` parameter. In the `.defaultAdvisors(...)` call (lines 98-107), replace `noOpRag` with `ragAdvisor` and INSERT `citationStash` immediately after `ragAdvisor`. Keep `noOpCache` and `noOpValidation` unchanged (D-07, PR2 respectively). Keep `guardIn`, `MessageChatMemoryAdvisor`, `guardOut` unchanged (D-13).
    3. Delete `src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpRetrievalAdvisor.java` (the file, not the class reference — references must all be removed by Step 2).
    4. DO NOT TOUCH `NoOpPromptCacheAdvisor.java` (D-07). Do not touch `NoOpValidationAdvisor.java` (PR2 handles it).
    5. Implement `EmptyContextRefusalIT.emptyRetrievalRoutesThroughGroundingGuardRefusalVerbatim`:
       - Input: `chatService.answer("zzzznonsensexxxxx no_such_legal_topic_q8q8q8", defaultModelId, null)`.
       - Expect: returned `ChatAnswerResponse.answer()` equals/starts-with the output of `AnswerComposer.composeRefusal()` (fetch the composer via `@Autowired` and call it for the exact comparison string). Citations list empty. Sources list empty.
    6. Implement `ChatClientConfigTest` GREEN logic:
       - `@SpringBootTest` + `@Autowired ApplicationContext ctx` (or `@Autowired ChatClient chatClient`).
       - Assert `ctx.getBeanNamesForType(Advisor.class)` includes a `RetrievalAugmentationAdvisor`-type bean.
       - Assert `ctx.getBeansOfType(CitationStashAdvisor.class).size() == 1`.
       - Assert `ctx.containsBean("noOpRetrievalAdvisor") == false` (bean gone after class deletion).
       - Assert `ctx.containsBean("noOpPromptCacheAdvisor") == true` (D-07 preserved).
    7. Run `./gradlew bootRun` smoke manually only if IT fails to boot; expected: context loads, chain order logs print for all 6 advisors (+100/+200/+300/+310/+500/+1000/-100).
  </action>
  <verify>
    <automated>./gradlew test --tests "*ChatClientConfigTest" liveTest --tests "*EmptyContextRefusalIT"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpRetrievalAdvisor.java` (file deleted) — verify with `ls` returning not-found.
    - `test -f src/main/java/com/vn/traffic/chatbot/chat/advisor/placeholder/NoOpPromptCacheAdvisor.java` (untouched, D-07).
    - `grep -n "RetrievalAugmentationAdvisor.builder()" src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java` returns ≥1 match.
    - `grep -n "HIGHEST_PRECEDENCE + 300" src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java` returns ≥1 match on the retrieval advisor registration (explicit `.order(...)` — D-13 chain slot authoritative, not inferred). `ChatClientConfigTest` additionally asserts the retrieval advisor bean's `getOrder() == Ordered.HIGHEST_PRECEDENCE + 300`.
    - `grep -n "retrievalPolicy::getSimilarityThreshold\|retrievalPolicy::getTopK" src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java` returns ≥2 matches (method references, not value calls — Pitfall 5).
    - `grep -n "RetrievalPolicy.RETRIEVAL_FILTER" src/main/java/com/vn/traffic/chatbot/chat/config/ChatClientConfig.java` returns ≥1 match (T-9-01 mitigation).
    - `grep -rn "NoOpRetrievalAdvisor" src/main/java src/test/java` returns zero matches (fully removed).
    - `grep -rn "NoOpPromptCacheAdvisor" src/main/java` returns ≥1 match (preserved).
    - `./gradlew test --tests "*ChatClientConfigTest"` exits 0.
    - `./gradlew liveTest --tests "*EmptyContextRefusalIT"` exits 0 when `OPENROUTER_API_KEY` is set (and auto-skips when absent via `@DisabledIfEnvironmentVariable`).
    - Boot log (captured during IT) contains `Advisor registered: CitationStashAdvisor order=` followed by `HIGHEST_PRECEDENCE + 310`-equivalent integer.
  </acceptance_criteria>
  <done>
    RetrievalAugmentationAdvisor + CitationStashAdvisor wired; NoOpRetrievalAdvisor deleted; NoOpPromptCacheAdvisor preserved; empty-context IT GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (W3): Shrink ChatService.doAnswer ≥60%; absorb ChatPromptFactory; run full regression suite</name>
  <read_first>
    - src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java (FULL FILE — target lines to delete: 48, 50, 52, 114-138, 141-149, 154; target lines to keep: 79-112 intent dispatch, 158-171 memConvId + CONVERSATION_ID param, 176-191 .entity call, 206 answerComposer.compose)
    - src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java (full file — DELETE after confirming all logic absorbed by LegalQueryAugmenter in Task 2)
    - src/main/java/com/vn/traffic/chatbot/chat/composition/AnswerComposer.java (D-06 — Claude's discretion on signature; preserve output parity)
    - src/test/java/com/vn/traffic/chatbot/chat/regression/VietnameseRegressionIT.java (existing P8 test; extend/re-run)
    - src/test/java/com/vn/traffic/chatbot/chat/archunit/ChatServiceDeletionArchTest.java (pattern for the new shrink archunit)
    - 09-RESEARCH.md §"Validation Architecture" (full test map + Wave 0 gaps)
    - 09-PATTERNS.md §"ChatService.java" (exact lines-to-delete list + advisor-context read pattern)
  </read_first>
  <behavior>
    - `ChatService.doAnswer` no longer contains `vectorStore.similaritySearch`, `citationMapper.toCitations`, `chatPromptFactory.buildPrompt`, or any `ChatPromptFactory` reference.
    - `ChatService.doAnswer` method-body LOC is ≥60% smaller than its P8 baseline (measured as non-comment non-blank lines between `{` and matching `}`). Baseline = count once before edits.
    - Citation + source lists are read from `response.context().get(ChatAdvisorContextKeys.CITATIONS_KEY)` and passed to `AnswerComposer.compose(...)` with zero transformation.
    - `ChatPromptFactory.java` DELETED (absorbed into LegalQueryAugmenter).
    - `ChatServiceShrinkArchTest` GREEN (identifier list from Task 1 all absent in `ChatService.java`).
    - `CitationFormatRegressionIT` GREEN (v1.0 `ChatAnswerResponse` JSON byte-for-byte parity).
    - `VietnameseRegressionIT` ≥95% pass (closes P8 deferred item 1), refusal rate within 10% of P7 baseline (closes P8 deferred item 2), two-turn memory test GREEN (D-14 + Pitfall 2).
    - `ChatAnswerResponse` JSON byte-for-byte identical to v1.0 fixture for the canonical regression queries.
  </behavior>
  <action>
    1. Measure baseline: `wc -l` of `ChatService.doAnswer` body lines (count lines between method open-brace and its matching close-brace, excluding blank lines and comment-only lines). Record in commit message for audit. Target after shrink: ≤ baseline × 0.40.
    2. Edit `ChatService.java`:
       - Remove fields: `private final VectorStore vectorStore;` (line 48), `private final CitationMapper citationMapper;` (line 50), `private final ChatPromptFactory chatPromptFactory;` (line 52).
       - Remove lines 114-138 (manual retrieval + `citationMapper.toCitations` + `toSources` block).
       - Remove lines 141-149 (`GroundingStatus.REFUSED` early-return based on document count — now owned by GroundingGuardOutputAdvisor per D-08).
       - Replace line 154 (`chatPromptFactory.buildPrompt(...)` call) with passing the bare `question` String to `.user(question)`.
       - Keep the rest of `doAnswer` (lines 79-112 intent dispatch, 158-171 memConvId + `a.param(ChatMemory.CONVERSATION_ID, memConvId)`, 176-191 `.entity(LegalAnswerDraft.class)`, 206 `answerComposer.compose(...)`).
       - Citation read pattern after `.call().chatResponse()`:
         ```java
         ChatResponse chatResponse = spec.call().chatResponse();
         LegalAnswerDraft draft = /* existing .entity-based extraction */;
         @SuppressWarnings("unchecked")
         List<CitationResponse> citations = (List<CitationResponse>)
             chatResponse.getMetadata().get(ChatAdvisorContextKeys.CITATIONS_KEY);
         @SuppressWarnings("unchecked")
         List<SourceReferenceResponse> sources = (List<SourceReferenceResponse>)
             chatResponse.getMetadata().get(ChatAdvisorContextKeys.SOURCES_KEY);
         if (citations == null) citations = List.of();
         if (sources == null) sources = List.of();
         return answerComposer.compose(GroundingStatus.GROUNDED, draft, citations, sources);
         ```
       - `AnswerComposer.compose(...)` signature — Claude's discretion per D-06; keep output `ChatAnswerResponse` byte-for-byte identical.
    3. DELETE `src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java` (fully absorbed by `LegalQueryAugmenter` in Task 2). Remove all references from the codebase. If any non-`ChatService` caller remains (unlikely), migrate it to `LegalQueryAugmenter` or delete the call-site.
    4. Implement `CitationFormatRegressionIT.v1FixtureReplayPreservesCitationsAndSourcesByteForByte`:
       - Load v1.0 `ChatAnswerResponse` JSON snapshot(s) from `src/test/resources/fixtures/v1/citation-format/*.json` (create snapshots from a clean pre-P9 checkout — regenerate manually once, committed to repo). If snapshots don't exist yet, capture them live on first run against a pinned set of 3-5 canonical queries and commit the snapshots as the fixture.
       - For each fixture, replay `chatService.answer(question, modelId, null)` and assert the serialized `ChatAnswerResponse` JSON equals the snapshot byte-for-byte EXCEPT for trivially variable fields (timestamps, IDs — whitelist). Citations list: compare `inlineLabel`, `sourceTitle`, `origin`, `pageNumber`, `sectionRef` verbatim. `[Nguồn n]` substrings in `answer` text: compare verbatim.
    5. Extend `VietnameseRegressionIT` — it should already exist from P8 with the 20-query fixture. Re-run; expect ≥19/20 pass now that real retrieval is wired (closes `twentyQueryRegressionSuiteAtLeast95Percent`). Refusal parity: compare refusal rate vs P7 baseline stored at `.planning/phases/07-chat-latency-foundation/baseline-refusal-rate.txt` or equivalent; assert within 10% absolute.
    6. Two-turn memory test inside `VietnameseRegressionIT` (or extract to `TwoTurnMemoryIT` if missing): turn-1 query sets context, turn-2 follow-up references it; assert turn-2 answer is non-refusal and references turn-1 topic. If missing from P8, add one now.
    7. Make `ChatServiceShrinkArchTest` GREEN: the RED test from Task 1 already asserts the identifier list; after this task's edits, the test passes automatically.
    8. Re-run `./gradlew test liveTest` full suite with `OPENROUTER_API_KEY` set. Three existing P8 live tests must stay green (`StructuredOutputMatrixIT`, `IntentClassifierIT`, `twoTurnConversationMemoryWorks` if free-standing).
  </action>
  <verify>
    <automated>./gradlew test liveTest --tests "*VietnameseRegressionIT" --tests "*CitationFormatRegressionIT" --tests "*ChatServiceShrinkArchTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "vectorStore|similaritySearch|citationMapper|chatPromptFactory|ChatPromptFactory|buildPrompt" src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java` returns 0.
    - `test ! -f src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java` (file deleted).
    - `grep -rn "ChatPromptFactory" src/main/java src/test/java` returns 0 matches (fully absorbed).
    - `grep -n "ChatAdvisorContextKeys.CITATIONS_KEY\|ChatAdvisorContextKeys.SOURCES_KEY" src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java` returns ≥2 matches.
    - LOC shrink measurement: record in commit message; `ChatService.doAnswer` body LOC (non-comment, non-blank, between matched braces) ≤ 0.40 × baseline.
    - `./gradlew test --tests "*ChatServiceShrinkArchTest"` exits 0.
    - `./gradlew liveTest --tests "*CitationFormatRegressionIT"` exits 0 with `OPENROUTER_API_KEY` set.
    - `./gradlew liveTest --tests "*VietnameseRegressionIT"` reports ≥19/20 pass (≥95%), refusal rate delta vs P7 baseline ≤ 10% absolute, two-turn memory assertion PASS.
    - No change to frontend files (`grep -l "ChatAnswerResponse\|citations\|sources" frontend/ 2>/dev/null` — unchanged between pre- and post-P9 by `git status frontend/`).
  </acceptance_criteria>
  <done>
    ChatService.doAnswer shrunk ≥60%; ChatPromptFactory deleted; all regression tests green (20-query ≥95%, refusal parity, two-turn memory, CitationFormat byte-for-byte); P8 deferred items closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User query → advisor chain | Untrusted user input enters `LegalQueryAugmenter` prompt template (parameter substitution, not string concat) |
| pgvector chunk store → retriever | Only `approvalState=APPROVED && trusted=true && active=true` chunks may cross into augmentation |
| Advisor chain → model API (OpenRouter) | Augmented prompt + grounded context egress to upstream LLM |
| Model response → `ChatService` | Text + `ChatClientResponse.context()` citations re-enter app layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Tampering / Information Disclosure | `VectorStoreDocumentRetriever.FILTER_EXPRESSION` in `ChatClientConfig.retrievalAdvisor` | mitigate | Task 3 wires `filterExpression(() -> parser.parse(RetrievalPolicy.RETRIEVAL_FILTER))` literal; `ChatClientConfigTest` asserts filter expression string equals `"approvalState == 'APPROVED' && trusted == 'true' && active == 'true'"`; ROADMAP SC 3 adds post-deploy 50-sample eyeball review (D-03). Accepts: no automated harness (D-03). |
| T-9-02 | Information Disclosure (refusal wording leak) | `LegalQueryAugmenter.allowEmptyContext` + `GroundingGuardOutputAdvisor` | mitigate | Task 2 sets `.allowEmptyContext(true)` verbatim; Task 3 wires advisor; Task 3 `EmptyContextRefusalIT` asserts refusal text equals `AnswerComposer.composeRefusal()` verbatim (Vietnamese, not generic Spring AI English). Pitfall 1 defense. |
| T-9-03 | Tampering (model emits spoofed `[Nguồn N]` outside provided list) | Model output → `CitationStashAdvisor` → `ChatAnswerResponse.answer` | mitigate | Task 2 `LegalQueryAugmenter` template includes verbatim rule "tuyệt đối không tự tạo nhãn ngoài danh sách được cung cấp"; `CitationPostProcessor` assigns deterministic 1..n labels pre-augmentation; Phase-8 `GroundingGuardOutputAdvisor` citation-presence check gates output; Task 4 `VietnameseRegressionIT` 20-query suite is the statistical detector (≥95% means <1 spoof per 20). Escalate to citation-regex validation in future phase if live data shows spoofing. |
</threat_model>

<verification>
- Task 1: constants + context keys landed; 6 RED scaffolds compile; existing tests green.
- Task 2: LegalQueryAugmenter + CitationPostProcessor + CitationStashAdvisor unit-tested GREEN; wiring unchanged.
- Task 3: `RetrievalAugmentationAdvisor` + `CitationStashAdvisor` wired; `NoOpRetrievalAdvisor` deleted; `NoOpPromptCacheAdvisor` preserved; `EmptyContextRefusalIT` GREEN.
- Task 4: `ChatService.doAnswer` shrunk ≥60%; `ChatPromptFactory` deleted; `VietnameseRegressionIT` ≥95%, refusal parity ≤10%, two-turn memory GREEN; `CitationFormatRegressionIT` byte-for-byte GREEN.

Phase-level:
- `./gradlew test` (all non-live tests) exits 0.
- `./gradlew liveTest` (with `OPENROUTER_API_KEY` set) exits 0.
- Zero matches for `vectorStore|similaritySearch|ChatPromptFactory|NoOpRetrievalAdvisor` under `src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java` and zero deleted-file references project-wide.
- `git diff --stat frontend/` shows zero changes (byte-for-byte API contract per ARCH-05 intent).
</verification>

<success_criteria>
- ARCH-01: `RetrievalAugmentationAdvisor` wired; `vectorStore.similaritySearch` absent from `ChatService`; `doAnswer` LOC ≥60% smaller — all three grep-verifiable + archunit-enforced.
- ARCH-05: `[Nguồn n]` byte-for-byte unit test (`LegalQueryAugmenterTest`) GREEN; `ChatAnswerResponse` JSON byte-for-byte IT (`CitationFormatRegressionIT`) GREEN; zero frontend changes.
- P8 deferred regression closed: `VietnameseRegressionIT` 20-query ≥95%, refusal-parity within 10% of P7 baseline.
- Two-turn memory preserved: D-14 regression test GREEN.
- Empty-context refusal verbatim: `EmptyContextRefusalIT` GREEN (Pitfall 1 / T-9-02 mitigation verified live).
- `NoOpPromptCacheAdvisor` preserved unchanged (D-07).
- `ChatPromptFactory` deleted; `LegalQueryAugmenter` owns Vietnamese prompt rules.
- All commits pass pre-commit hooks; no `--no-verify` used.
</success_criteria>

<output>
After completion, create `.planning/phases/09-modular-rag-prompt-caching/09-01-SUMMARY.md` covering:
- What was built (LegalQueryAugmenter, CitationPostProcessor, CitationStashAdvisor, ChatClientConfig swap, ChatService shrink).
- `doAnswer` baseline vs post-shrink LOC numbers (quantitative ≥60% proof).
- Test results: unit + ArchUnit + live regression percentages.
- Files deleted (NoOpRetrievalAdvisor, ChatPromptFactory).
- Files preserved intentionally (NoOpPromptCacheAdvisor per D-07).
- Remaining Phase 9 work pointer: Plan 02 (Validation advisor swap).
</output>
