---
phase: 03-multi-turn-case-analysis
plan: 03
type: execute
wave: 3
depends_on: ["03-01-PLAN", "03-02-PLAN"]
files_modified:
  - src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java
  - src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java
  - src/main/java/com/vn/traffic/chatbot/chat/service/ScenarioAnswerComposer.java
  - src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java
  - src/main/java/com/vn/traffic/chatbot/chat/service/LegalAnswerDraft.java
  - src/main/java/com/vn/traffic/chatbot/chat/api/dto/ChatAnswerResponse.java
  - src/main/java/com/vn/traffic/chatbot/chat/api/dto/ScenarioAnalysisResponse.java
  - src/test/java/com/vn/traffic/chatbot/chat/ChatScenarioAnalysisIntegrationTest.java
  - src/test/java/com/vn/traffic/chatbot/chat/service/ScenarioAnswerComposerTest.java
  - src/test/java/com/vn/traffic/chatbot/chat/api/ChatContractSerializationTest.java
  - src/test/java/com/vn/traffic/chatbot/chat/ChatThreadFlowIntegrationTest.java
autonomous: false
requirements: [CASE-01, CASE-04, CHAT-02]
must_haves:
  truths:
    - "After required facts are present, the user receives a grounded final scenario answer structured as Facts → Rule → Outcome plus actions and sources per D-05 and D-06."
    - "Each substantive turn still re-runs retrieval and returns citations/sources from approved+trusted+active material instead of relying only on memory."
    - "The response contract is stable enough for the later sidebar chat UI to distinguish clarification-needed vs final-analysis states in one payload."
  artifacts:
    - path: "src/main/java/com/vn/traffic/chatbot/chat/service/ScenarioAnswerComposer.java"
      provides: "Scenario-aware composition for Facts → Rule → Outcome plus actions and visible sources"
      exports: ["ScenarioAnswerComposer"]
    - path: "src/test/java/com/vn/traffic/chatbot/chat/ChatScenarioAnalysisIntegrationTest.java"
      provides: "End-to-end regression coverage for grounded final analysis after clarification"
      exports: ["ChatScenarioAnalysisIntegrationTest"]
    - path: "src/test/java/com/vn/traffic/chatbot/chat/api/ChatContractSerializationTest.java"
      provides: "Serialization lock for thread-aware response mode fields and scenario analysis sections"
      exports: ["ChatContractSerializationTest"]
  key_links:
    - from: "ChatService"
      to: "RetrievalPolicy / vector search"
      via: "rebuild retrieval query from current turn + active facts before final analysis"
      pattern: "buildRequest|similaritySearch|RETRIEVAL_FILTER"
    - from: "ScenarioAnswerComposer"
      to: "ChatAnswerResponse.scenarioAnalysis"
      via: "Facts → Rule → Outcome + actions serialization"
      pattern: "scenarioAnalysis|facts|rule|outcome|actions"
    - from: "ChatContractSerializationTest"
      to: "Phase 4 consumers"
      via: "stable JSON fields for responseMode, threadId, pendingFacts, scenarioAnalysis, citations, and sources"
      pattern: "threadId|responseMode|scenarioAnalysis|citations|sources"
---

<objective>
Finish Phase 3 by composing grounded multi-turn scenario answers on top of the thread + clarification foundation and lock the resulting contract with end-to-end regression coverage.

Purpose: Deliver D-05 and D-06 together with CASE-01 and CASE-04, while preserving Phase 2 grounding, disclaimer, citation, and retrieval-gate behavior on every substantive turn.
Output: Scenario answer composer, final-analysis orchestration, serialization/integration regressions, and one combined human verification checkpoint for tone/workflow fit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/ai/traffic-law-chatbot/.planning/PROJECT.md
@D:/ai/traffic-law-chatbot/.planning/REQUIREMENTS.md
@D:/ai/traffic-law-chatbot/.planning/ROADMAP.md
@D:/ai/traffic-law-chatbot/.planning/STATE.md
@D:/ai/traffic-law-chatbot/.planning/phases/03-multi-turn-case-analysis/03-CONTEXT.md
@D:/ai/traffic-law-chatbot/.planning/phases/03-multi-turn-case-analysis/03-RESEARCH.md
@D:/ai/traffic-law-chatbot/.planning/phases/03-multi-turn-case-analysis/03-VALIDATION.md
@D:/ai/traffic-law-chatbot/.planning/phases/02-grounded-legal-q-a-core/02-04-SUMMARY.md
@D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java
@D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java
@D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java
@D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/chat/service/LegalAnswerDraft.java
@D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/retrieval/RetrievalPolicy.java

<interfaces>
Existing response composition to preserve:
```java
public ChatAnswerResponse compose(
        GroundingStatus groundingStatus,
        LegalAnswerDraft draft,
        List<CitationResponse> citations,
        List<SourceReferenceResponse> sources
)
```

Existing prompt contract:
```java
public String buildPrompt(String question, GroundingStatus groundingStatus, List<CitationResponse> citations)
```

Final structure locked for this plan:
- D-05: final scenario answers must follow Facts -> Rule -> Outcome.
- D-06: still include practical actions and sources.
- Preserve Phase 2 disclaimer, citations, source list, and approved+trusted+active retrieval gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose grounded final scenario answers with Facts → Rule → Outcome plus actions and sources</name>
  <files>
    src/main/java/com/vn/traffic/chatbot/chat/service/ScenarioAnswerComposer.java,
    src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java,
    src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java,
    src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java,
    src/main/java/com/vn/traffic/chatbot/chat/service/LegalAnswerDraft.java,
    src/main/java/com/vn/traffic/chatbot/chat/api/dto/ChatAnswerResponse.java,
    src/main/java/com/vn/traffic/chatbot/chat/api/dto/ScenarioAnalysisResponse.java,
    src/test/java/com/vn/traffic/chatbot/chat/service/ScenarioAnswerComposerTest.java,
    src/test/java/com/vn/traffic/chatbot/chat/ChatScenarioAnalysisIntegrationTest.java,
    src/test/java/com/vn/traffic/chatbot/chat/ChatThreadFlowIntegrationTest.java
  </files>
  <behavior>
    - Test 1: when required facts are present, final responses use `responseMode=FINAL_ANALYSIS` and populate `scenarioAnalysis.facts`, `scenarioAnalysis.rule`, `scenarioAnalysis.outcome`, and `scenarioAnalysis.actions` in that exact conceptual order.
    - Test 2: substantive follow-up turns rebuild retrieval from current turn plus active facts and still return citations/sources sourced through `RetrievalPolicy.RETRIEVAL_FILTER`.
    - Test 3: clarification-needed responses from Plan 02 transition into final analysis after the missing fact is answered within the same thread.
    - Test 4: weakly grounded scenario turns still return `REFUSED` or limited-grounding behavior instead of unsupported final legal conclusions.
  </behavior>
  <action>
    Implement `ScenarioAnswerComposer` and update `ChatService` so final case analysis stays grounded on each substantive turn. Use the active thread facts to enrich retrieval queries for short follow-ups such as “vậy mức phạt thế nào?”, but do not let memory replace retrieval for legal authority. The final payload must explicitly represent Facts → Rule → Outcome per D-05 while still exposing practical actions and the Phase 2 source fields per D-06.

    Update `LegalAnswerDraft` and `ChatPromptFactory` only as required to support the structured scenario fields. Keep the current disclaimer and citation behavior from Phase 2, and preserve the refusal path from `02-04-SUMMARY.md`: empty or weak retrieval must not produce a substantive final analysis. Do not add Phase 4 UI logic or branchable/forked threads.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.traffic.chatbot.chat.service.ScenarioAnswerComposerTest" --tests "com.vn.traffic.chatbot.chat.ChatScenarioAnalysisIntegrationTest" --tests "com.vn.traffic.chatbot.chat.ChatThreadFlowIntegrationTest"</automated>
  </verify>
  <done>Resolved threads can return grounded final scenario analysis in the locked structure without weakening refusal, disclaimer, or citation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock thread-aware response serialization and full regression coverage</name>
  <files>
    src/test/java/com/vn/traffic/chatbot/chat/api/ChatContractSerializationTest.java,
    src/test/java/com/vn/traffic/chatbot/chat/ChatScenarioAnalysisIntegrationTest.java,
    src/test/java/com/vn/traffic/chatbot/chat/ChatThreadFlowIntegrationTest.java,
    src/main/java/com/vn/traffic/chatbot/chat/api/dto/ChatAnswerResponse.java,
    src/main/java/com/vn/traffic/chatbot/chat/api/dto/ScenarioAnalysisResponse.java
  </files>
  <behavior>
    - Test 1: JSON serialization keeps `threadId`, `responseMode`, `pendingFacts`, `rememberedFacts`, `scenarioAnalysis`, `citations`, and `sources` stable for Phase 4 consumers.
    - Test 2: an end-to-end scenario regression covers thread creation -> clarification-needed -> user correction/answer -> final analysis with sources.
    - Test 3: final scenario payloads include both structured scenario sections and the existing disclaimer/citation fields.
    - Test 4: no regression removes Phase 2 field names or changes citation/source visibility.
  </behavior>
  <action>
    Extend `ChatContractSerializationTest` to lock the full Phase 3 response envelope, not just Phase 2 fields. Add explicit assertions for the new thread-aware fields and scenario-analysis object while preserving the existing field names used by Phase 2 tests. Expand integration coverage so one realistic Vietnamese traffic-law scenario proves the complete lifecycle: create thread, receive clarification-needed response because of missing material facts, answer the clarification, then receive final structured grounded analysis with citations and sources.

    Run the focused Phase 3 suite plus the relevant existing Phase 2 chat regressions to prove this phase extends rather than breaks the shipped backend. If any Phase 2 behavior regresses, fix it in this plan before finishing.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.traffic.chatbot.chat.api.ChatContractSerializationTest" --tests "com.vn.traffic.chatbot.chat.ChatScenarioAnalysisIntegrationTest" --tests "com.vn.traffic.chatbot.chat.ChatThreadFlowIntegrationTest" --tests "com.vn.traffic.chatbot.chat.api.ChatControllerTest"</automated>
  </verify>
  <done>The Phase 3 contract is serialization-locked and proven through end-to-end regression without breaking Phase 2 grounded-answer consumers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real multi-turn Vietnamese case flow quality</name>
  <action>After Tasks 1 and 2 pass, run one combined human verification pass focused on the final product behavior that automation cannot fully judge: Vietnamese legal tone, perceived caution, and whether the thread/mode contract feels usable for the later sidebar chat workflow. Do not use this checkpoint to replace automated testing; it is a final acceptance gate after the regression suite is green.</action>
  <verify>
    <automated>MISSING — Wave 0 and Tasks 1-2 automated suites in this plan must pass before this checkpoint</automated>
  </verify>
  <done>Human review confirms the end-to-end case flow is acceptable, or returns specific issues for fixes/replanning.</done>
  <what-built>Persistent thread continuity, clarification-needed blocking, correction-wins memory, and final grounded scenario analysis on the backend.</what-built>
  <how-to-verify>
    1. Start the backend locally and create a new thread using the new thread-create endpoint.
    2. Send an ambiguous Vietnamese traffic case missing a material fact, for example a violation description without the vehicle type or another outcome-changing detail.
    3. Confirm the response returns the same `threadId`, uses `responseMode=CLARIFICATION_NEEDED`, lists `pendingFacts`, and does not provide a final `scenarioAnalysis.outcome`.
    4. Reply within the same thread with the missing fact and, if useful, a correction to a previously stated fact.
    5. Confirm the next response uses `responseMode=FINAL_ANALYSIS`, reads naturally in Vietnamese, keeps a cautious informational tone, and shows Facts → Rule → Outcome plus actions, citations, and sources.
    6. Confirm the earlier corrected fact is no longer shown as active remembered state in the response.
  </how-to-verify>
  <resume-signal>Type "approved" if the workflow and tone are acceptable, or describe any issues that require replanning/fixes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| active thread facts -> retrieval prompt | Remembered facts influence retrieval queries and model prompts for later turns. |
| retrieved legal sources -> final scenario analysis | Only approved/trusted/active legal material may support penalties, procedures, and outcomes. |
| backend response contract -> future frontend | Phase 4 depends on stable thread/mode/scenario fields and visible citations/sources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | retrieval query builder | mitigate | Rebuild substantive-turn queries from current user text plus active remembered facts, then pass through unchanged `RetrievalPolicy.RETRIEVAL_FILTER` so memory cannot bypass legal grounding. |
| T-03-03-02 | T | final scenario composer | mitigate | Populate Facts → Rule → Outcome only after clarification policy confirms material facts are present; otherwise keep `CLARIFICATION_NEEDED` or `REFUSED`. |
| T-03-03-03 | I | API serialization | mitigate | Lock JSON field names for `threadId`, `responseMode`, `scenarioAnalysis`, `citations`, and `sources` with serialization tests so Phase 4 consumers do not read ambiguous payloads. |
| T-03-03-04 | D | weak-grounding scenario turn | mitigate | Reuse Phase 2 refusal and limited-grounding handling so empty/weak retrieval cannot escalate into unsupported scenario conclusions. |
| T-03-03-05 | R | multi-turn case workflow | mitigate | Add end-to-end regression from thread creation through clarification and final analysis so the delivered scenario flow is reproducible and auditable. |
</threat_model>

<verification>
- `./gradlew test --tests "com.vn.traffic.chatbot.chat.service.ScenarioAnswerComposerTest" --tests "com.vn.traffic.chatbot.chat.ChatScenarioAnalysisIntegrationTest" --tests "com.vn.traffic.chatbot.chat.ChatThreadFlowIntegrationTest"`
- `./gradlew test --tests "com.vn.traffic.chatbot.chat.api.ChatContractSerializationTest" --tests "com.vn.traffic.chatbot.chat.ChatScenarioAnalysisIntegrationTest" --tests "com.vn.traffic.chatbot.chat.ChatThreadFlowIntegrationTest" --tests "com.vn.traffic.chatbot.chat.api.ChatControllerTest"`
- Manual workflow in Task 3 after automated verification passes.
</verification>

<success_criteria>
- Final scenario responses follow Facts → Rule → Outcome plus actions and sources.
- Follow-up turns stay grounded through fresh retrieval and keep visible citations/source lists.
- The full thread lifecycle is covered by automated regression and one combined human verification pass.
- Phase 2 grounded-answer contract behavior remains intact for existing consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-turn-case-analysis/03-03-SUMMARY.md`
</output>
