# Phase 01.1: Refactor ingestion parser architecture to a hybrid Spring AI approach - Context

**Gathered:** 2026-04-08
**Status:** Ready for planning

<domain>
## Phase Boundary

Refactor the existing Phase 1 ingestion parser architecture toward a hybrid Spring AI approach without weakening the system's trust, provenance, async job, or retrieval-gating guarantees. This phase fixes incorrect `final_url` tracking, keeps the current source/source_version/ingestion_job model intact, and incrementally replaces low-value parsing internals where Spring AI is a better fit. It does not add new ingestion capabilities, crawling behavior, or chat-facing features.

</domain>

<decisions>
## Implementation Decisions

### Refactor scope
- **D-01:** Phase 01.1 uses a hybrid refactor strategy, not a full rewrite and not a full keep-custom approach.
- **D-02:** The custom ingestion domain boundary stays in place: `ParsedDocument`, async orchestration, source/source_version/job separation, approval/trusted/active retrieval gating, and provenance persistence remain first-class.
- **D-03:** The immediate implementation focus is to fix `final_url` tracking and refactor URL HTML ingestion first.
- **D-04:** PDF/file parser replacement can be evaluated in this phase only as an incremental follow-on behind the same custom boundary; chunking changes are lower priority and should come last.

### Provenance and metadata contract
- **D-05:** Metadata preservation is strict. The refactor must preserve retrieval-compatible metadata behavior, including source linkage, page/section or URL location hints, processing version, parser identity, and approval/trust compatibility.
- **D-06:** Chunk-level audit fields such as ordinal and content hash remain required unless a replacement path can preserve them equivalently without breaking current inspection/admin flows.
- **D-07:** URL ingestion must correctly persist both requested URL and final resolved URL as provenance data.

### URL safety and parsing boundary
- **D-08:** The network trust boundary remains custom. SSRF validation, host checks, fetch provenance, and final URL capture must stay outside Spring AI readers.
- **D-09:** Spring AI HTML parsing may be introduced only after a custom safe fetch step has validated and fetched the URL content.
- **D-10:** URL ingestion remains page-by-page import only; no crawler or broader website discovery behavior is added in this phase.

### Migration shape
- **D-11:** Rollout should be bugfix-first and adapter-based. Fix the `final_url` bug first, then introduce hybrid adapters behind the existing parser abstractions.
- **D-12:** Existing API contracts, job visibility, and retrieval policy behavior should remain stable during migration.
- **D-13:** Avoid broad one-phase replacement of URL parser, file parsers, and chunker together unless research shows the risk is low and metadata behavior is preserved.

### Spring AI adoption direction
- **D-14:** Use Spring AI for commodity ETL internals where it reduces maintenance without taking ownership of domain policy.
- **D-15:** Preferred evaluation order is: safe custom URL fetch + Spring AI HTML reader first, then page-aware PDF reader if it preserves page provenance adequately, then token-based splitting only if metadata carry-through remains clean.

### Claude's Discretion
- Exact adapter class names and package layout.
- Whether PDF evaluation lands in the same plan set or a follow-on plan within this phase.
- Exact split between parser adapter responsibilities and orchestrator responsibilities, as long as the locked boundary and metadata guarantees are preserved.

</decisions>

<specifics>
## Specific Ideas

- The team should keep custom code where the system is genuinely domain-specific: SSRF safety, requested/final URL provenance, source/version/job boundaries, approval-gated retrieval, and ingestion auditability.
- Spring AI should replace low-value parsing internals, not the system's trust boundary.
- The practical sequence discussed was: fix `final_url` first, prototype the hybrid URL ingestion slice, evaluate PDF next, and leave chunker replacement for last.

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase definition
- `.planning/ROADMAP.md` — Defines inserted Phase 01.1 and its placement between Phase 1 and Phase 2.
- `.planning/STATE.md` — Records that Phase 01.1 is urgent follow-up work inserted after shipped Phase 1.
- `.planning/PROJECT.md` — Defines REST-first Spring alignment, Jmix-ai-backend preservation goals, and trusted-source constraints.
- `.planning/REQUIREMENTS.md` — Defines Phase 1 knowledge/provenance/retrieval requirements that this refactor must continue to satisfy.

### Prior locked phase decisions
- `.planning/phases/01-backend-foundation-knowledge-base/01-CONTEXT.md` — Carries forward async ingestion, trust-gated retrieval, source/version/job boundaries, and provenance requirements.
- `.planning/phases/01-backend-foundation-knowledge-base/01-03-PLAN.md` — Defines the original ingestion pipeline architecture being refactored.

### Current implementation to refactor
- `src/main/java/com/vn/traffic/chatbot/ingestion/parser/UrlPageParser.java` — Current custom URL validation/fetch/parse path.
- `src/main/java/com/vn/traffic/chatbot/ingestion/parser/TikaDocumentParser.java` — Current custom Tika-backed file parser.
- `src/main/java/com/vn/traffic/chatbot/ingestion/parser/ParsedDocument.java` — Current provenance-aware parser output contract to preserve.
- `src/main/java/com/vn/traffic/chatbot/ingestion/chunking/TextChunker.java` — Current chunk contract and audit metadata source.
- `src/main/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestrator.java` — Current async pipeline and the `final_url` bug location.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `ParsedDocument` already provides a stable domain output shape for parser adapters.
- `IngestionOrchestrator` already owns async pipeline transitions, vector-store writes, and lifecycle updates.
- `TextChunker` already produces chunk ordinal/hash metadata used by downstream inspection and retrieval flows.

### Established Patterns
- The current ingestion design separates domain policy from raw content extraction, which is the right seam for hybridization.
- Retrieval safety depends on metadata fields remaining compatible with `RetrievalPolicy` and lifecycle metadata updates.
- URL ingestion already has custom SSRF validation before persistence and fetch, so the safest hybrid path is to keep that boundary and swap only the parsing internals.

### Integration Points
- URL refactor work centers on `UrlPageParser.java` and `IngestionOrchestrator.java`.
- Any Spring AI parser adoption must map back into `ParsedDocument` before chunking/indexing.
- Provenance persistence must continue to write through `KbSourceVersion` and `KbSourceFetchSnapshot` without changing the higher-level ingestion API.

</code_context>

<deferred>
## Deferred Ideas

- Broad website crawling or seed-based discovery remains out of scope.
- Full parser/chunker stack replacement in one pass is deferred unless research proves metadata and rollout risk are acceptable.
- Chat-facing changes and any retrieval behavior expansion remain outside this phase.

</deferred>

---

*Phase: 01.1-refactor-ingestion-parser-architecture-to-a-hybrid-spring-ai*
*Context gathered: 2026-04-08*
