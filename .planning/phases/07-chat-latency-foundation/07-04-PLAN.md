---
phase: 07
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - .planning/phases/07-chat-latency-foundation/07-SMOKE-REPORT.md
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03, CACHE-02]
requirements_addressed: [PERF-01, PERF-02, PERF-03, CACHE-02]
tags: [manual-smoke, prometheus, observability, verification]
user_setup: []

must_haves:
  truths:
    - "Backend starts without NoUniqueBeanDefinitionException on EmbeddingModel (Assumption A3 confirmed)"
    - "curl POST /api/chat with 'Xin chào' returns a conversational reply and triggers the chitchat pipeline-log marker"
    - "curl POST /api/chat with 'Vượt đèn đỏ phạt bao nhiêu' returns a grounded legal answer (not refusal)"
    - "The same Vietnamese legal query submitted twice shows cache_gets_total{cache=\"embedding\",result=\"hit\"} incrementing"
    - "HTTP response for /api/chat returns observably before the corresponding chat_log row is persisted (async behavior visible in thread names + timing)"
    - "No LegalAnswerDraft JSON validation errors for OpenRouter-served answers after prompt trim"
    - "7-SMOKE-REPORT.md captures pre/post timings and observations per CONTEXT.md D-06"
  artifacts:
    - path: ".planning/phases/07-chat-latency-foundation/07-SMOKE-REPORT.md"
      provides: "Manual-smoke checklist results + eyeball p95 from Prometheus"
  key_links:
    - from: "Backend boot"
      to: "VectorStore consumes @Primary CachingEmbeddingModel"
      via: "Spring @Primary resolution (Assumption A3 smoke)"
      pattern: "no exception during context load"
    - from: "curl /api/chat"
      to: "chat_log INSERT on chat-log-N thread"
      via: "async executor"
      pattern: "http-nio-8089-exec-N → chat-log-N"
---

<objective>
Manual-smoke checklist verifying the combined effect of Plans 01–03 on a running backend + frontend. Captures pre/post informal latency eyeball on a handful of Vietnamese queries per CONTEXT.md D-06 — the explicit alternative to a `ChatLatencyBaselineIT` harness (D-05). Produces `07-SMOKE-REPORT.md` with observed timings, Prometheus metric readings, and any regressions found.

Purpose: Satisfies ROADMAP Phase 7 success criterion 1 (as revised by CONTEXT.md — no baseline snapshot file; informal manual check) + CACHE-02e (`cache_gets_total` visible via Prometheus) + PERF-03 observability (async thread-name evidence).

Split into two tasks: (Task 1) automated scaffold of the skeleton report; (Task 2) `checkpoint:manual-smoke` where the user (with Claude's assistance) runs the backend + curl loop and populates the report with observed values, then signals approval.

Output: `.planning/phases/07-chat-latency-foundation/07-SMOKE-REPORT.md` committed, fully populated, no unfilled placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-chat-latency-foundation/07-CONTEXT.md
@.planning/phases/07-chat-latency-foundation/07-RESEARCH.md
@.planning/phases/07-chat-latency-foundation/07-VALIDATION.md
@.planning/phases/07-chat-latency-foundation/07-01-SUMMARY.md
@.planning/phases/07-chat-latency-foundation/07-02-SUMMARY.md
@.planning/phases/07-chat-latency-foundation/07-03-SUMMARY.md

<interfaces>
<!-- Endpoints and metrics exposed by Plans 01-03 -->

Backend port: assumed 8089 (confirm via application.yaml `server.port`).

HTTP endpoints:
  POST /api/chat  body: {"question":"<Vietnamese text>"}  returns: ChatAnswerResponse JSON
  GET  /actuator/prometheus  returns: Prometheus text exposition (requires Plan 01 Task 1)

Prometheus metrics of interest (all auto-bound — no custom MeterBinder):
  http_server_requests_seconds{uri="/api/chat",quantile="0.95"}  (approximate; may be bucketized)
  cache_gets_total{cache="embedding",result="hit"}
  cache_gets_total{cache="embedding",result="miss"}
  cache_evictions_total{cache="embedding"}
  executor_pool_size{name="chatLogExecutor"}
  executor_queued_tasks{name="chatLogExecutor"}
  executor_completed_tasks_total{name="chatLogExecutor"}

Frontend: Next.js dev server, assumed `npm run dev` on default port per frontend CLAUDE.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the skeleton 07-SMOKE-REPORT.md (empty placeholders — no runtime observations yet)</name>
  <files>.planning/phases/07-chat-latency-foundation/07-SMOKE-REPORT.md</files>
  <read_first>
    - .planning/phases/07-chat-latency-foundation/07-RESEARCH.md §Code Examples §Micrometer observation (lines 554-574 — exact curl loop)
    - .planning/phases/07-chat-latency-foundation/07-CONTEXT.md D-06 (manual smoke is the sanctioned verification)
    - src/main/resources/application.yaml (confirm `server.port`)
  </read_first>
  <action>
    Create `.planning/phases/07-chat-latency-foundation/07-SMOKE-REPORT.md` with the skeleton below. Fill in today's date and the current `git rev-parse HEAD`. DO NOT populate any observation rows — those are Task 2's job. Every placeholder token (`<Xs>`, `<N>`, `<Y/N>`, `<first 200 chars>`) stays untouched; checkboxes remain unticked.

    ```markdown
    # Phase 7 Manual Smoke Report
    Date: <YYYY-MM-DD>
    Commit: <git sha>

    ## Boot Verification
    - [ ] No NoUniqueBeanDefinitionException on EmbeddingModel (Assumption A3): <Y/N + log excerpt>
    - [ ] Exactly one @EnableAsync / AsyncAnnotationBeanPostProcessor registration in logs: <Y/N>
    - [ ] /actuator/prometheus returns 200: <Y/N>

    ## Chitchat Smoke (PERF-02)
    Query: "Xin chào"
      Response 1: time_total=<Xs>, body excerpt: <first 200 chars>
      Response 2: time_total=<Xs>
      Response 3: time_total=<Xs>
    - [ ] Response is conversational, NOT a refusal template.
    - [ ] Backend logs show "Path: chitchat — skipping retrieval + LLM (P7)" for each.

    ## Legal Query Smoke (PERF-01, CACHE-02)
    Query: "Vượt đèn đỏ phạt bao nhiêu"
      Response 1 (cold): time_total=<Xs>
      Response 2 (warm, expect embedding cache hit): time_total=<Xs>
      Response 3 (warm): time_total=<Xs>

    Prometheus readings after warm:
      cache_gets_total{cache="embedding",result="hit"} = <N>
      cache_gets_total{cache="embedding",result="miss"} = <N>

    - [ ] Second+third calls are faster than first (embedding cache effect).
    - [ ] hit counter > 0 after warm.

    Query: "Thủ tục cấp lại GPLX"
      Response 1 (cold): time_total=<Xs>
      Response 2 (warm): time_total=<Xs>
      Response 3 (warm): time_total=<Xs>

    ## Async Chat-Log Evidence (PERF-03)
    - [ ] Log line for /api/chat request shows thread name `http-nio-...` BEFORE returning HTTP response.
    - [ ] Log line for chat_log INSERT shows thread name `chat-log-N` (different thread, AFTER response returned).
    - [ ] executor_completed_tasks_total{name="chatLogExecutor"} increments after each request.

    ## Eyeball p95 (CONTEXT.md D-06)
    - Approximate p95 across the 9 legal-query samples: <Xs>
    - PERF-01 target: <2.5s p95
    - Result: <PASS / FAIL / NEEDS-MORE-DATA>

    ## Notes / Regressions
    - <any surprises>
    ```

    **NEVER** create `.planning/phases/07-chat-latency-foundation/top20-queries.json` or any benchmark fixture file — that is explicit_non_scope (D-05).
  </action>
  <verify>
    <automated>test -f .planning/phases/07-chat-latency-foundation/07-SMOKE-REPORT.md &amp;&amp; grep -c "^- \[ \]" .planning/phases/07-chat-latency-foundation/07-SMOKE-REPORT.md</automated>
  </verify>
  <done>
    - `07-SMOKE-REPORT.md` exists with the full section structure.
    - Date and commit sha populated on lines 2-3.
    - All `<Xs>`, `<N>`, `<Y/N>`, `<first 200 chars>` placeholders still present (unfilled — Task 2 populates).
    - All checkboxes `- [ ]` unticked.
    - No `top20-queries.json` file created anywhere under `.planning/` or `src/test/resources/`.
    - No `ChatLatencyBaselineIT*` or `ChatV11Properties*` file created.
  </done>
</task>

<task type="checkpoint:manual-smoke" gate="blocking">
  <name>Task 2: Boot backend, run canonical curl smoke, populate observations in 07-SMOKE-REPORT.md, then user-verify</name>
  <what-built>
    Plans 01–03 landed the full Phase 7 implementation:
    - `/actuator/prometheus` exposed; Caffeine + cache starter + prometheus registry on classpath.
    - `@Primary CachingEmbeddingModel` decorator with Vietnamese-diacritic-preserving normalization, SHA-256 keys, dimension-mismatch guard, `EmbeddingModelChangedEvent` listener (event-clear auto-gated by `EmbeddingCacheContextLoadTest`).
    - Async `chat_log` persistence via `chatLogExecutor` (core=2 max=8 queue=1000 CallerRunsPolicy) + `@Transactional(REQUIRES_NEW)` + `List.copyOf` snapshots at 3 callsites.
    - `LegalAnswerDraft` slimmed from 12 to 8 fields; `ChatPromptFactory` prompt instruction trimmed; `AnswerComposer` passes `List.of()` for `scenarioFacts`; frontend rendering branches for the 3 removed fields deleted.
    - Vietnamese greeting short-circuit in `ChatService.doAnswer` (regex + length gate; not the P8 LLM classifier).
    - All Wave-0 tests went RED → GREEN (two previously @Disabled tests enabled).
    - `07-SMOKE-REPORT.md` skeleton scaffolded by Task 1.
  </what-built>
  <how-to-verify>
    **Step A — Boot backend in background and capture startup signals:**
    ```
    ./gradlew bootRun
    ```
    Wait for "Started ChatbotApplication" log line. Capture the startup log excerpt showing:
    - No `NoUniqueBeanDefinitionException` on `EmbeddingModel` (Assumption A3 verification).
    - `AsyncAnnotationBeanPostProcessor` registered exactly once (`grep` the log for it).

    Populate the Boot Verification checkboxes in `07-SMOKE-REPORT.md` — tick each box and replace `<Y/N>` with Y (and paste the relevant log excerpt) or N (and explain).

    **Step B — Canonical smoke loop (RESEARCH.md §Code Examples lines 562-572):**
    ```bash
    for q in "Xin chào" "Vượt đèn đỏ phạt bao nhiêu" "Thủ tục cấp lại GPLX"; do
      for i in 1 2 3; do
        curl -s -w "\nHTTP=%{http_code} time_total=%{time_total}s\n" \
          -X POST http://localhost:8089/api/chat \
          -H 'Content-Type: application/json' \
          -d "{\"question\":\"$q\"}" \
          -o /tmp/p7-smoke-$q-$i.json
      done
    done
    ```

    Replace every `<Xs>` token in the Chitchat Smoke and Legal Query Smoke sections with observed `time_total` values. Replace `<first 200 chars>` with the first 200 chars of the "Xin chào" response body.

    **Step C — Prometheus scrape:**
    ```bash
    curl -s http://localhost:8089/actuator/prometheus > /tmp/p7-prometheus.txt
    grep -E 'http_server_requests_seconds.*(/api/chat)|cache_gets_total\{cache="embedding"|cache_evictions_total\{cache="embedding"|executor_completed_tasks_total\{name="chatLogExecutor"' /tmp/p7-prometheus.txt
    ```

    Replace every `<N>` placeholder in the Prometheus readings block with the observed integer. Tick the associated checkboxes.

    **Step D — Async Chat-Log Evidence:**
    Grep the backend log for consecutive lines on different thread names (e.g., `http-nio-8089-exec-3` followed by `chat-log-1` for the chat_log INSERT). Tick the three Async Evidence checkboxes. If the observation is unambiguous, mark Y; if ambiguous, mark `SKIPPED: <reason>`.

    **Step E — Eyeball p95:**
    From the 9 legal-query samples in Step B, compute the approximate p95 (sort times, take the 8th-of-9 value or use the max of the last 3 samples as conservative estimate). Replace `<Xs>` in the "Approximate p95" line with the numeric value. Replace `<PASS / FAIL / NEEDS-MORE-DATA>` with exactly one of:
    - `PASS` → phase ready for `/gsd-verify-work`.
    - `FAIL` → P7's 3 levers (slim schema + embedding cache + async log) did not meet <2.5s. Record rationale + next step (accept for v1.1 / re-open Plan 03 / defer to P9).
    - `NEEDS-MORE-DATA` → rerun with larger query set before deciding.

    **Step F — Frontend manual check:**
    Submit a canonical legal question via Next.js dev server; confirm no React "undefined" renderings, no console errors, no missing sections that used to render the dropped scenario fields. Note any findings in `## Notes / Regressions`.

    **Step G — Populate "Notes / Regressions":**
    Replace the `<any surprises>` placeholder with either `None` or a bulleted list of observations. The token `<any surprises>` MUST NOT survive in the committed file.

    **Self-check before signalling:**
    - No token matching `<Xs>`, `<N>`, `<Y/N>`, `<first 200 chars>`, `<PASS / FAIL / NEEDS-MORE-DATA>`, `<any surprises>` remains in the file.
    - Every `SKIPPED:` marker (if any) is followed by an explicit reason string.
    - The Boot Verification `/actuator/prometheus` checkbox IS ticked (this row is mandatory — must not be SKIPPED).
    - At least one timing value in the Legal Query Smoke section is present (numeric seconds).

    Optionally test `EmbeddingModelChangedEvent` manually in production boot if an admin path exists that calls `applicationContext.publishEvent(new EmbeddingModelChangedEvent(this, "new-model-id"))`; confirm next same-query call MISSES the cache. If no admin path exists, skip (already auto-gated by `EmbeddingCacheContextLoadTest` from Plan 02).
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/07-chat-latency-foundation/07-SMOKE-REPORT.md &amp;&amp; ! grep -E '&lt;Xs&gt;|&lt;N&gt;|&lt;Y/N&gt;|&lt;first 200 chars&gt;|&lt;PASS / FAIL / NEEDS-MORE-DATA&gt;|&lt;any surprises&gt;' .planning/phases/07-chat-latency-foundation/07-SMOKE-REPORT.md &amp;&amp; ! grep -E 'SKIPPED:\s*$' .planning/phases/07-chat-latency-foundation/07-SMOKE-REPORT.md &amp;&amp; grep -qE '^- \[[xX]\].*actuator/prometheus returns 200' .planning/phases/07-chat-latency-foundation/07-SMOKE-REPORT.md</automated>
  </verify>
  <done>
    - `07-SMOKE-REPORT.md` has no remaining `<Xs>` / `<N>` / `<Y/N>` / `<first 200 chars>` / `<PASS / FAIL / NEEDS-MORE-DATA>` / `<any surprises>` placeholder tokens.
    - Every `SKIPPED:` marker has a non-empty reason.
    - Boot Verification row for `/actuator/prometheus returns 200` is ticked (`- [x]` or `- [X]`) — non-negotiable.
    - Chitchat section: 3 timings captured + "Path: chitchat" log marker observed (or explicit SKIPPED with reason for the log-observation checkbox only — NEVER for the timing rows).
    - Legal Query section: 3 timings per query captured; Prometheus hit counter recorded as integer.
    - Async Evidence section: thread-name evidence captured or each row marked `SKIPPED: <reason>`.
    - Eyeball p95 section: result is exactly one of PASS / FAIL / NEEDS-MORE-DATA with a numeric value.
    - No `top20-queries.json` file created.
    - User typed approval (resume-signal below).
  </done>
  <resume-signal>Type "approved" if smoke observations are acceptable (PASS or acceptable-FAIL with rationale), "rework" with a note if Plan 03 must be revisited.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (Claude/user) → running backend via curl | Local-only smoke; no auth change. |
| /actuator/prometheus exposure | Already catalogued in Plan 01 T-07-01. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | I (Information Disclosure) — Smoke report may leak internal thread names / metric values via git commit | 07-SMOKE-REPORT.md | accept | Report contains no secrets, no PII, no API keys — only Vietnamese query strings, timing numbers, and metric names. Safe to commit to private git repo. |
| T-07-16 | R (Repudiation) — Undocumented smoke result | smoke report completeness | mitigate | Report template requires explicit PASS/FAIL/NEEDS-MORE-DATA and numeric p95. Task 2 `<verify>` rejects any file containing unfilled placeholder tokens or trailing `SKIPPED:` with empty reason, so an incomplete report cannot pass the gate. |
</threat_model>

<verification>
- Task 1 scaffold exists with all placeholders present (skeleton check).
- Task 2 observations fully populated; placeholder tokens all replaced.
- `/actuator/prometheus returns 200` boot-verification checkbox ticked.
- Checkpoint task receives "approved" signal from user.
- No benchmark fixture, baseline IT, or feature-flag file created.
</verification>

<success_criteria>
- PERF-01 verified (PASS) or explicitly deferred with rationale in retrospective.
- PERF-02 verified — chitchat bypasses refusal.
- PERF-03 verified — thread-name evidence in logs; executor metric increments.
- CACHE-02 verified — hit counter > 0 after warm; metric visible via `/actuator/prometheus`.
- ROADMAP criterion 1 satisfied via informal manual check (CONTEXT.md D-06, supersedes baseline snapshot).
</success_criteria>

<output>
After completion, create `.planning/phases/07-chat-latency-foundation/07-04-SUMMARY.md`.
</output>
