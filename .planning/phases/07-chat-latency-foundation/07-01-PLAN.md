---
phase: 07
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/main/resources/application.yaml
  - build.gradle
  - src/test/java/com/vn/traffic/chatbot/ai/embedding/CachingEmbeddingModelTest.java
  - src/test/java/com/vn/traffic/chatbot/ai/embedding/CacheKeyNormalizerTest.java
  - src/test/java/com/vn/traffic/chatbot/chatlog/service/ChatLogServiceAsyncTest.java
  - src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerSlimSchemaTest.java
autonomous: true
requirements: [PERF-01, PERF-03, CACHE-02]
requirements_addressed: [PERF-01, PERF-03, CACHE-02]
tags: [spring-ai, spring-boot, actuator, caffeine, async, testing, nyquist-wave-0]
user_setup: []

must_haves:
  truths:
    - "HTTP GET /actuator/prometheus returns 200 with text/plain exposition"
    - "Wave-0 test files exist and compile (may fail at runtime — classes under test not yet implemented; this is expected RED state)"
    - "Caffeine 3.2.0 and spring-boot-starter-cache are resolvable on the runtime classpath"
  artifacts:
    - path: "src/main/resources/application.yaml"
      provides: "Prometheus endpoint exposure"
      contains: "prometheus"
    - path: "build.gradle"
      provides: "Caffeine + cache-starter + prometheus-registry runtime deps"
      contains: "caffeine:3.2.0"
    - path: "src/test/java/com/vn/traffic/chatbot/ai/embedding/CachingEmbeddingModelTest.java"
      provides: "CACHE-02a/b/c RED tests (hit on repeat, normalize, dimension-mismatch)"
    - path: "src/test/java/com/vn/traffic/chatbot/ai/embedding/CacheKeyNormalizerTest.java"
      provides: "D-15 normalization RED test (lowercase + NFC + trim; diacritics preserved)"
    - path: "src/test/java/com/vn/traffic/chatbot/chatlog/service/ChatLogServiceAsyncTest.java"
      provides: "PERF-03 RED test — @Async proxy + REQUIRES_NEW + List.copyOf snapshot"
    - path: "src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerSlimSchemaTest.java"
      provides: "D-03 RED test — composer handles 8-field LegalAnswerDraft"
  key_links:
    - from: "application.yaml management.endpoints.web.exposure.include"
      to: "Spring Boot Actuator PrometheusScrapeEndpoint"
      via: "exposure.include list membership"
      pattern: "include:.*prometheus"
    - from: "build.gradle"
      to: "CaffeineCacheManager on classpath"
      via: "implementation 'com.github.ben-manes.caffeine:caffeine:3.2.0'"
      pattern: "caffeine"
---

<objective>
Wave 0 infrastructure + Nyquist RED test scaffolds. Expose `/actuator/prometheus` (currently missing per RESEARCH.md Pitfall D — application.yaml:122 lists only `health,info,metrics`), add Caffeine + cache starter + Prometheus registry dependencies to build.gradle, and author four test files that encode the expected behaviors of Wave 1 implementations. Tests are allowed to fail-on-run (classes-under-test do not yet exist); they MUST compile.

Purpose: Satisfies Nyquist gate for Phase 7 — every Wave-1 task has an `<automated>` command that can actually be run. Satisfies CACHE-02 / PERF-03 observability precondition (Prometheus endpoint must be exposed before Wave-1 implementations can be smoke-verified).

Output: Prometheus endpoint exposed, build deps resolvable, 4 test files committed (compile-only at this point).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-chat-latency-foundation/07-CONTEXT.md
@.planning/phases/07-chat-latency-foundation/07-RESEARCH.md
@.planning/phases/07-chat-latency-foundation/07-VALIDATION.md
@.planning/phases/07-chat-latency-foundation/07-PATTERNS.md
@src/main/resources/application.yaml
@build.gradle

<interfaces>
<!-- Behaviors the Wave-1 implementations must satisfy (these drive the RED tests). -->

CachingEmbeddingModel (to be implemented in Wave 1 Plan 02):
- Constructor: (EmbeddingModel delegate, CacheManager cm, String modelId, int configuredDim)
- Overrides only call(EmbeddingRequest) from org.springframework.ai.embedding.EmbeddingModel
- Single-instruction requests: cache lookup by key = modelId + ":" + sha256(normalize(text))
- Multi-instruction requests: bypass cache, delegate directly (batch ingestion path)
- Cached value shape: record CachedVector(float[] vector, int dim)
- Dimension mismatch (cached.dim != configuredDim): evict entry, delegate, repopulate
- normalize(s): Normalizer.normalize(s, Form.NFC).trim().toLowerCase(Locale.ROOT) — NO accent stripping

ChatLogService.save (to be modified in Wave 1 Plan 03):
- Annotated @Async("chatLogExecutor") @Transactional(propagation=REQUIRES_NEW)
- When invoked from a different bean, runs on thread whose name starts with "chat-log-"
- Caller must pass a pre-snapshotted pipelineLog (via String.join("\n", List.copyOf(logMessages)))

LegalAnswerDraft (to be modified in Wave 1 Plan 03):
- 8-field record: (conclusion, answer, uncertaintyNotice, legalBasis, penalties, requiredDocuments, procedureSteps, nextSteps)
- The 4 dropped fields: scenarioFacts, scenarioRule, scenarioOutcome, scenarioActions

AnswerComposer (to be modified in Wave 1 Plan 03):
- compose(...) still returns ChatAnswerResponse
- ChatAnswerResponse.scenarioFacts populated with List.of() (not null) — honours D-04 "no null tolerance fill"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose /actuator/prometheus + add Caffeine/cache/prometheus runtime deps</name>
  <files>src/main/resources/application.yaml, build.gradle</files>
  <read_first>
    - src/main/resources/application.yaml (lines 118-125 — current management.endpoints.web.exposure.include)
    - build.gradle (lines 28-50 — dependencies block)
    - .planning/phases/07-chat-latency-foundation/07-RESEARCH.md §Pitfall D and §Standard Stack "Installation" block
    - .planning/phases/07-chat-latency-foundation/07-PATTERNS.md §application.yaml section
  </read_first>
  <action>
    In `src/main/resources/application.yaml`, locate `management.endpoints.web.exposure.include: health,info,metrics` (application.yaml:122 per RESEARCH.md Pitfall D) and change the value to exactly `health,info,metrics,prometheus`. No other YAML changes.

    In `build.gradle`, add three dependencies inside the existing `dependencies { ... }` block (per RESEARCH.md §Standard Stack Installation, D-18 locks Caffeine and D-19 requires Prometheus exposure):

    ```groovy
    // --- v1.1 P7: Embedding cache (CACHE-02) ---
    implementation 'com.github.ben-manes.caffeine:caffeine:3.2.0'
    implementation 'org.springframework.boot:spring-boot-starter-cache'

    // --- v1.1 P7: Prometheus metrics export (PERF-01 observation) ---
    runtimeOnly 'io.micrometer:micrometer-registry-prometheus'
    ```

    Verify resolution:
    ```
    ./gradlew dependencies --configuration runtimeClasspath | grep -E "caffeine|spring-boot-starter-cache|micrometer-registry-prometheus"
    ```
    All three coordinates MUST appear. If Caffeine 3.2.0 fails to resolve, bump to latest 3.2.x per Assumption A1.

    Do NOT introduce any `app.chat.v11.*` namespace, `ChatV11Properties`, Actuator refresh, or `spring.cache.caffeine.spec` auto-wiring (D-01; explicit CacheManager bean comes in Plan 02).
  </action>
  <verify>
    <automated>./gradlew dependencies --configuration runtimeClasspath | grep -E "caffeine|spring-boot-starter-cache|micrometer-registry-prometheus"</automated>
  </verify>
  <done>
    - `grep -n "prometheus" src/main/resources/application.yaml` shows the token inside the `exposure.include` list (exactly `health,info,metrics,prometheus`).
    - `grep -n "caffeine:3.2.0" build.gradle` returns one hit.
    - `grep -n "spring-boot-starter-cache" build.gradle` returns one hit.
    - `grep -n "micrometer-registry-prometheus" build.gradle` returns one hit.
    - `./gradlew dependencies --configuration runtimeClasspath` lists all three artifacts without ResolutionException.
    - No file under `src/main/java` was modified by this task.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author 4 Wave-0 RED test files (compile-only, must not require unreleased production classes)</name>
  <files>
    src/test/java/com/vn/traffic/chatbot/ai/embedding/CachingEmbeddingModelTest.java,
    src/test/java/com/vn/traffic/chatbot/ai/embedding/CacheKeyNormalizerTest.java,
    src/test/java/com/vn/traffic/chatbot/chatlog/service/ChatLogServiceAsyncTest.java,
    src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerSlimSchemaTest.java
  </files>
  <read_first>
    - .planning/phases/07-chat-latency-foundation/07-VALIDATION.md §Wave 0 Requirements
    - .planning/phases/07-chat-latency-foundation/07-RESEARCH.md §Pattern 1 (CachingEmbeddingModel shape) + §Validation Architecture + §Pitfalls 7/8/9/A/B
    - .planning/phases/07-chat-latency-foundation/07-PATTERNS.md §CachingEmbeddingModelTest + §ChatLogServiceAsyncIT sections
    - src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerTest.java (analog: pure-unit shape with MockitoExtension)
    - src/test/java/com/vn/traffic/chatbot/chatlog/ChatLogServiceTest.java (analog: ArgumentCaptor + @InjectMocks)
    - src/main/java/com/vn/traffic/chatbot/chatlog/service/ChatLogService.java (current save() signature)
    - src/main/java/com/vn/traffic/chatbot/chat/service/LegalAnswerDraft.java (current 12-field record — tests target future 8-field shape)
    - src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java (current compose() contract)
  </read_first>
  <behavior>
    <!-- Four files, one focused RED spec each. All must COMPILE; run-time failures are expected RED state. -->

    CachingEmbeddingModelTest (covers CACHE-02a, CACHE-02c — D-14/D-16):
      - Test: hitOnRepeat — first call delegates to mocked EmbeddingModel.call(...), second identical call returns cached vector without re-delegating (verify(delegate, times(1))).
      - Test: missOnDifferentText — two different query texts both delegate once each.
      - Test: dimensionMismatchEvictsAndRefetches — seed cache with CachedVector(wrongDimVec, dim=512); call with configuredDim=1536; verify entry evicted and delegate re-invoked.
      - Test: batchRequestBypassesCache — EmbeddingRequest with instructions.size() > 1 delegates every time, never writes to cache.
      - Uses `ConcurrentMapCacheManager` test double; mocks `org.springframework.ai.embedding.EmbeddingModel` via Mockito.
      - @ExtendWith(MockitoExtension.class); NO @SpringBootTest.

    CacheKeyNormalizerTest (covers CACHE-02b — D-15):
      - Test: lowercasesAsciiAndVietnamese — "Vượt Đèn Đỏ" → "vượt đèn đỏ" (diacritics preserved, lowercase).
      - Test: nfcNormalizesComposedVsDecomposed — "à" (NFC) and "a\u0300" (NFD) normalize to identical string.
      - Test: trimsLeadingTrailingWhitespace — "  xin chào  " → "xin chào".
      - Test: preservesDiacritics — assert that normalize("Hà Nội").contains("à") && .contains("ộ").
      - Test: sha256ProducesHexString — sha256(normalize("test")) is 64 lowercase hex chars, deterministic.
      - If the production helper class does not yet exist, reference its intended FQN `com.vn.traffic.chatbot.ai.embedding.CachingEmbeddingModel` static methods `normalize(String)` and `sha256(String)` — BUT keep them package-private so the test can reference them. The test WILL fail to compile until Plan 02 lands the class; this is acceptable only if compile failure is isolated to these two method references. Alternative: extract an explicit helper class `CacheKeyNormalizer` under the same package and target its static methods — preferred, keeps this file compile-green today.
      - Chosen approach: create a tiny stub class `src/main/java/com/vn/traffic/chatbot/ai/embedding/CacheKeyNormalizer.java` with two methods that throw `UnsupportedOperationException("Wave 1 — not implemented")`. This lets the test compile AND fail at runtime (true RED). Plan 02 will replace the body.

    ChatLogServiceAsyncTest (covers PERF-03a/c — D-10/D-11, Pitfall A/B):
      - Test: asyncSnapshotImmutability — builds a mutable `List<String> logMessages = new ArrayList<>()`, snapshots via `List.copyOf(logMessages)`, mutates the original, asserts snapshot unchanged — this tests the PATTERN the caller must follow (pure Java, no Spring).
      - Test: saveMethodCarriesAsyncAnnotation — uses reflection on `ChatLogService.class.getMethod("save", ...)` to assert `isAnnotationPresent(org.springframework.scheduling.annotation.Async.class)` AND that annotation value equals `"chatLogExecutor"`.
      - Test: saveMethodCarriesRequiresNewPropagation — reflection assert `@Transactional(propagation = REQUIRES_NEW)`.
      - NO @SpringBootTest — keep these three assertions as pure reflection + pure JDK. A deeper IT (actual thread-name assertion) is deferred to manual smoke (VALIDATION.md manual-only row). Per D-07, if a future integration test is added for Spring AI components, fetch Context7 `/spring-projects/spring-boot` task-execution docs first.
      - @ExtendWith(MockitoExtension.class); uses `@InjectMocks ChatLogService` style from ChatLogServiceTest.java:22-53.

    AnswerComposerSlimSchemaTest (covers D-03/D-04):
      - Test: composeWithEightFieldDraftProducesResponse — constructs `new LegalAnswerDraft(conclusion, answer, uncertainty, legalBasis, penalties, requiredDocuments, procedureSteps, nextSteps)` (8 args) and verifies `answerComposer.compose(...)` returns a non-null `ChatAnswerResponse` whose `scenarioFacts()` is `List.of()` (NOT null) per D-04.
      - Test: chatAnswerResponseHonoursNullFillRule — asserts the ChatAnswerResponse.scenarioFacts field equals `List.of()` exactly, never null.
      - Relies on the future 8-field LegalAnswerDraft. Today's record has 12 fields — the test WILL not compile until Plan 03 trims the record. Acceptable RED state; compile error is isolated to the `new LegalAnswerDraft(...)` call. The plan's `done` criterion accounts for this (see below).
  </behavior>
  <action>
    Create four test files listed in `<files>`. Apply the exact test shapes described in `<behavior>`. Use the analog patterns from `src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerTest.java` (class-level `@ExtendWith(MockitoExtension.class)`, field-init composer via `new AnswerComposer(...)`) and `src/test/java/com/vn/traffic/chatbot/chatlog/ChatLogServiceTest.java` (Mockito `@Mock` + `@InjectMocks` + `ArgumentCaptor`).

    Also create ONE production stub:
    - `src/main/java/com/vn/traffic/chatbot/ai/embedding/CacheKeyNormalizer.java`
      - Package: `com.vn.traffic.chatbot.ai.embedding`
      - Two public static methods: `public static String normalize(String s) { throw new UnsupportedOperationException("Wave 1 — not implemented"); }` and `public static String sha256(String s) { throw new UnsupportedOperationException("Wave 1 — not implemented"); }`
      - This keeps `CacheKeyNormalizerTest` COMPILE-GREEN while failing at runtime (true RED).

    For `CachingEmbeddingModelTest`: the production class `CachingEmbeddingModel` does NOT yet exist, so this file will NOT compile until Plan 02 creates the class. To keep this test file compile-green today, guard it by:
    - Option chosen: Put the full test body under import of `com.vn.traffic.chatbot.ai.embedding.CachingEmbeddingModel` with a TODO header comment `// PLAN-02-DEPENDENCY: compiles once CachingEmbeddingModel class exists.`
    - Accept that `./gradlew compileTestJava` will fail today on THIS file only — this is the canonical Nyquist RED state per VALIDATION.md. Plan 02's Task 1 fixes compile by landing the class; Plan 02's Task 1 `done` criterion requires this test class to compile and run green.

    For `AnswerComposerSlimSchemaTest`: same RED principle — the `new LegalAnswerDraft(8-args)` literal compiles only after Plan 03 trims the record. Header comment: `// PLAN-03-DEPENDENCY: compiles once LegalAnswerDraft is trimmed to 8 fields.`

    For `ChatLogServiceAsyncTest` and `CacheKeyNormalizerTest`: these MUST compile today (they reference either a today-present class `ChatLogService` via reflection string literals, or the new stub `CacheKeyNormalizer`).

    Do NOT create: `ChatLatencyBaselineIT.java`, `top20-queries.json`, `ChatV11Properties`, any custom `LoadGenerator`/`Timer` utility (D-01/D-05, explicit_non_scope).
  </action>
  <verify>
    <automated>./gradlew compileTestJava --tests "com.vn.traffic.chatbot.chatlog.service.ChatLogServiceAsyncTest" --tests "com.vn.traffic.chatbot.ai.embedding.CacheKeyNormalizerTest"</automated>
  </verify>
  <done>
    - File `src/test/java/com/vn/traffic/chatbot/ai/embedding/CachingEmbeddingModelTest.java` exists; contains the 4 tests listed in `<behavior>`; header has `// PLAN-02-DEPENDENCY:` comment.
    - File `src/test/java/com/vn/traffic/chatbot/ai/embedding/CacheKeyNormalizerTest.java` exists; contains the 5 tests; compiles green today against the `CacheKeyNormalizer` stub.
    - File `src/main/java/com/vn/traffic/chatbot/ai/embedding/CacheKeyNormalizer.java` stub exists with two methods throwing `UnsupportedOperationException`.
    - File `src/test/java/com/vn/traffic/chatbot/chatlog/service/ChatLogServiceAsyncTest.java` exists; contains the 3 tests; compiles green today.
    - File `src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerSlimSchemaTest.java` exists; contains the 2 tests; header has `// PLAN-03-DEPENDENCY:` comment.
    - `grep -c "@Test" src/test/java/com/vn/traffic/chatbot/ai/embedding/CachingEmbeddingModelTest.java` returns 4.
    - `grep -c "@Test" src/test/java/com/vn/traffic/chatbot/ai/embedding/CacheKeyNormalizerTest.java` returns 5.
    - `./gradlew test --tests "com.vn.traffic.chatbot.ai.embedding.CacheKeyNormalizerTest"` fails at runtime with `UnsupportedOperationException: Wave 1 — not implemented` (RED state confirmed).
    - `./gradlew test --tests "com.vn.traffic.chatbot.chatlog.service.ChatLogServiceAsyncTest.saveMethodCarriesAsyncAnnotation"` fails because today's `ChatLogService.save` has NO `@Async` annotation (RED state confirmed).
    - NO file named `ChatLatencyBaselineIT*`, `top20-queries.json`, or `ChatV11Properties*` anywhere under `src/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External scraper → /actuator/prometheus | Previously-closed endpoint now exposes metrics text. In production this crosses an operator-trust boundary. |

## STRIDE Threat Register (ASVS L1 — per RESEARCH.md §Security Domain, this phase is operational-only)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | I (Information Disclosure) | `/actuator/prometheus` endpoint exposes internal metric names (URI templates, cache names, JVM stats) | accept | Dev-scope only per RESEARCH.md §Security Domain hardening note. No PII or secret values in Micrometer metrics. Prod hardening (basic auth or network ACL) is out-of-P7 scope; documented for v1.2 ops hardening. |
| T-07-02 | T (Tampering) | build.gradle new transitive deps (Caffeine, cache starter, prometheus registry) | mitigate | Versions pinned (Caffeine `3.2.0`); Spring Boot BOM governs cache-starter and prometheus-registry; `./gradlew dependencies` is part of Task 1 verify gate. No `latest.release` or `+`. |
| T-07-03 | D (Denial of Service) | Prometheus endpoint scrape cost | accept | Micrometer registry is passive; cost bounded by Spring Boot defaults. Solo-dev pre-production (CONTEXT.md D-01 scope note). |
</threat_model>

<verification>
- `./gradlew dependencies --configuration runtimeClasspath` lists caffeine:3.2.0, spring-boot-starter-cache, micrometer-registry-prometheus.
- `grep -n "prometheus" src/main/resources/application.yaml` shows inclusion inside `exposure.include`.
- All four test files exist; the two dependency-free ones (`CacheKeyNormalizerTest`, `ChatLogServiceAsyncTest`) compile green today and fail at runtime (RED).
- `CacheKeyNormalizer.java` stub exists throwing `UnsupportedOperationException` in both methods.
- No files in explicit_non_scope list created.
</verification>

<success_criteria>
- Prometheus endpoint now in exposure.include (enables CACHE-02 hit/miss metrics visibility).
- Caffeine + cache-starter + prometheus-registry resolvable on runtime classpath.
- 4 Wave-0 test files committed; dependency-free ones in RED; dependent ones carry compile TODO markers referencing Plan 02 / Plan 03.
- Stub `CacheKeyNormalizer` class committed (will be fleshed out in Plan 02).
- No feature-flag, baseline harness, or benchmark fixture files created (explicit_non_scope respected).
</success_criteria>

<output>
After completion, create `.planning/phases/07-chat-latency-foundation/07-01-SUMMARY.md`.
</output>
