---
phase: 07
plan: 03
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - src/main/java/com/vn/traffic/chatbot/chat/config/ChatLogAsyncConfig.java
  - src/main/java/com/vn/traffic/chatbot/chatlog/service/ChatLogService.java
  - src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java
  - src/main/java/com/vn/traffic/chatbot/chat/service/LegalAnswerDraft.java
  - src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java
  - src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java
  - src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerSlimSchemaTest.java
  - frontend/types/api.ts
  - frontend/components/chat/message-bubble.tsx
  - frontend/components/chat/scenario-accordion.tsx
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03]
requirements_addressed: [PERF-01, PERF-02, PERF-03]
tags: [spring, async, transactional, chat, slim-schema, chitchat, frontend, nextjs]
user_setup: []

must_haves:
  truths:
    - "HTTP POST /api/chat returns the ChatAnswerResponse before the chat_log row is persisted (async handoff happens)"
    - "ChatLogService.save runs on a thread whose name starts with 'chat-log-' when invoked from ChatService"
    - "A Vietnamese greeting like 'Xin chào' short-circuits past containsAnyLegalCitation and receives a conversational reply without vector retrieval"
    - "LegalAnswerDraft record has exactly 8 fields; prompt no longer instructs the LLM to emit scenarioFacts/scenarioRule/scenarioOutcome/scenarioActions"
    - "Frontend does not render branches for the 4 dropped scenario fields; no 'undefined' appears in chat bubbles"
    - "pipelineLog passed to ChatLogService.save is always a String produced from List.copyOf(logMessages) snapshot (never the mutable list)"
  artifacts:
    - path: "src/main/java/com/vn/traffic/chatbot/chat/config/ChatLogAsyncConfig.java"
      provides: "chatLogExecutor ThreadPoolTaskExecutor bean"
      contains: "CHAT_LOG_EXECUTOR"
    - path: "src/main/java/com/vn/traffic/chatbot/chatlog/service/ChatLogService.java"
      provides: "@Async @Transactional(REQUIRES_NEW) save"
      contains: "@Async(ChatLogAsyncConfig.CHAT_LOG_EXECUTOR)"
    - path: "src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java"
      provides: "Chitchat short-circuit + List.copyOf snapshots at all 3 callsites"
      contains: "isGreetingOrChitchat"
    - path: "src/main/java/com/vn/traffic/chatbot/chat/service/LegalAnswerDraft.java"
      provides: "8-field record"
    - path: "frontend/types/api.ts"
      provides: "Slim ChatAnswerResponse type (scenarioRule/Outcome/Actions removed)"
  key_links:
    - from: "ChatService.doAnswer"
      to: "ChatLogService.save (via @Async proxy)"
      via: "cross-bean invocation through injected ChatLogService field"
      pattern: "chatLogService\\.save\\("
    - from: "ChatLogAsyncConfig.CHAT_LOG_EXECUTOR"
      to: "@Async value on ChatLogService.save"
      via: "string constant reference"
      pattern: "CHAT_LOG_EXECUTOR"
    - from: "ChatService isGreetingOrChitchat branch"
      to: "AnswerComposer.composeChitchat"
      via: "new pure method"
      pattern: "composeChitchat"
---

<objective>
Three coordinated backend changes + one frontend removal, merged into one plan because all three touch `ChatService.java` and the `List.copyOf` discipline spans both existing callsites (ChatService.java:133 refusal, ChatService.java:181 grounded) plus the new chitchat branch:

1. **Async chat-log infra (PERF-03):** New `ChatLogAsyncConfig` bean (`chatLogExecutor` core=2 max=8 queue=1000 CallerRunsPolicy), annotate `ChatLogService.save` with `@Async("chatLogExecutor") @Transactional(propagation=REQUIRES_NEW)`, wrap all three callsites with `List.copyOf(logMessages)` snapshot. **Decision per RESEARCH.md A6 default:** all three save-callsites go through the async annotated method; D-12 concern (hiding grounding regressions) is mitigated because refusals still write to the same `chat_log` table via a bounded `CallerRunsPolicy` queue.

2. **Slim schema (PERF-01, D-03/D-04):** Drop 4 scenario fields from `LegalAnswerDraft`, trim `ChatPromptFactory.buildPrompt` instruction lines, update `AnswerComposer` to pass `List.of()` for `ChatAnswerResponse.scenarioFacts` (public DTO shape preserved; no null-fill per D-04). Frontend removes rendering branches for the 4 dropped fields simultaneously. Un-@Disable the Wave-0 `AnswerComposerSlimSchemaTest`.

3. **Chitchat short-circuit (PERF-02, D-02):** Direct code in `ChatService.doAnswer` — `isGreetingOrChitchat(question)` heuristic (Vietnamese greeting regex + short-length gate, keep tiny; not the P8 LLM classifier) routes past `containsAnyLegalCitation` to `answerComposer.composeChitchat(...)`; log as `GroundingStatus.GROUNDED` with 0 tokens per RESEARCH.md Open Question #4 resolution.

Purpose: Satisfies PERF-01 (schema/prompt trim win), PERF-02 (greetings never refused), PERF-03 (HTTP returns before chat_log write).

Output: 3 new or modified backend Java files (plus ChatPromptFactory trim + AnswerComposer update + LegalAnswerDraft record trim), 1 new config file, 3 frontend TS/TSX files edited. Wave-0 `ChatLogServiceAsyncTest` and `AnswerComposerSlimSchemaTest` go RED→GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-chat-latency-foundation/07-CONTEXT.md
@.planning/phases/07-chat-latency-foundation/07-RESEARCH.md
@.planning/phases/07-chat-latency-foundation/07-PATTERNS.md
@.planning/phases/07-chat-latency-foundation/07-01-SUMMARY.md
@src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java
@src/main/java/com/vn/traffic/chatbot/chatlog/service/ChatLogService.java
@src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java
@src/main/java/com/vn/traffic/chatbot/chat/service/LegalAnswerDraft.java
@src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java
@src/main/java/com/vn/traffic/chatbot/common/config/AsyncConfig.java
@frontend/types/api.ts
@frontend/components/chat/message-bubble.tsx
@frontend/components/chat/scenario-accordion.tsx

<interfaces>
<!-- Current production shapes (pre-P7) — executors use these as the source-of-truth contracts. -->

ChatLogService.save (current — ChatLogService.java:29-ish):
  public void save(
    String question, ChatAnswerResponse response, GroundingStatus groundingStatus,
    String conversationId, int promptTokens, int completionTokens, int responseTime,
    String pipelineLog
  )
  // After P7: same signature; adds @Async("chatLogExecutor") @Transactional(REQUIRES_NEW).

LegalAnswerDraft (current — 12 fields):
  public record LegalAnswerDraft(
    String conclusion, String answer, String uncertaintyNotice,
    List<String> legalBasis, List<String> penalties, List<String> requiredDocuments,
    List<String> procedureSteps, List<String> nextSteps,
    List<String> scenarioFacts, List<String> scenarioRule,
    List<String> scenarioOutcome, List<String> scenarioActions
  ) {}
  // After P7: 8 fields (drop last 4).

AnswerComposer.compose: returns ChatAnswerResponse (public DTO unchanged in shape).
ChatAnswerResponse.scenarioFacts: stays as List<String>; populated by List.of() post-P7 (D-04).

ScenarioAnswerComposer (separate path — multi-turn scenario feature):
  - Per RESEARCH.md Open Question #2, this path is a SEPARATE LLM call producing ScenarioAnalysisResponse.
  - Executor MUST read ScenarioAnswerComposer.java in Task 2 to confirm NO reference to the 4 dropped LegalAnswerDraft fields.
  - If no references found, leave ScenarioAnswerComposer untouched.

Existing shared infra:
  - AsyncConfig.java:28 already declares @EnableAsync (CLASS-LEVEL — covers ALL executors).
  - AsyncConfig implements AsyncConfigurer with global uncaught-exception handler (AsyncConfig.java:65-70) — auto-applies to chatLogExecutor.
  - Therefore: ChatLogAsyncConfig must NOT redeclare @EnableAsync (PATTERNS.md key insight #1).

Frontend types (frontend/types/api.ts current):
  - ChatAnswerResponse includes {scenarioFacts, scenarioRule, scenarioOutcome, scenarioActions} fields.
  - ScenarioAnalysisResponse (separate) is untouched — the multi-turn scenario feature uses its own DTO.

Wave-0 tests (from Plan 01):
  - ChatLogServiceAsyncTest — reflection assertions on save's @Async / @Transactional annotations. NOT @Disabled.
  - AnswerComposerSlimSchemaTest — requires 8-field LegalAnswerDraft constructor. Currently class-level @Disabled with `/* ... */`-commented body + `Assertions.fail(...)` placeholder. Plan 03 Task 2 un-@Disables AND uncomments the real body.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Async chat-log infra — ChatLogAsyncConfig + ChatLogService annotations + List.copyOf snapshots at 2 existing ChatService callsites</name>
  <files>
    src/main/java/com/vn/traffic/chatbot/chat/config/ChatLogAsyncConfig.java,
    src/main/java/com/vn/traffic/chatbot/chatlog/service/ChatLogService.java,
    src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java
  </files>
  <read_first>
    - .planning/phases/07-chat-latency-foundation/07-RESEARCH.md §Pattern 2 (lines 316-365 — Context7-verified @Async + REQUIRES_NEW) + §Pitfalls A/B (lines 453-470)
    - .planning/phases/07-chat-latency-foundation/07-PATTERNS.md §ChatLogAsyncConfig.java + §ChatLogService.java + §Cross-bean @Async invocation
    - src/main/java/com/vn/traffic/chatbot/common/config/AsyncConfig.java (entire file — confirm @EnableAsync already present at line 28; DO NOT DUPLICATE)
    - src/main/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestrator.java (lines 60-63 — existing @Async("ingestionExecutor") cross-bean invocation analog)
    - src/main/java/com/vn/traffic/chatbot/chatlog/service/ChatLogService.java (entire file — identify save() method line range)
    - src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java (lines 129-140 refusal branch, 175-190 grounded branch — two existing chatLogService.save callsites)
    - src/test/java/com/vn/traffic/chatbot/chatlog/service/ChatLogServiceAsyncTest.java (from Plan 01 — its reflection assertions drive this impl)
  </read_first>
  <action>
    **Step 1 — Create `ChatLogAsyncConfig.java`** at `src/main/java/com/vn/traffic/chatbot/chat/config/`. Exact content skeleton:

    ```java
    package com.vn.traffic.chatbot.chat.config;

    import java.util.concurrent.ThreadPoolExecutor;
    import org.springframework.context.annotation.Bean;
    import org.springframework.context.annotation.Configuration;
    import org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor;

    @Configuration
    public class ChatLogAsyncConfig {

        public static final String CHAT_LOG_EXECUTOR = "chatLogExecutor";

        @Bean(name = CHAT_LOG_EXECUTOR)
        public ThreadPoolTaskExecutor chatLogExecutor() {
            ThreadPoolTaskExecutor ex = new ThreadPoolTaskExecutor();
            ex.setCorePoolSize(2);
            ex.setMaxPoolSize(8);
            ex.setQueueCapacity(1000);
            ex.setThreadNamePrefix("chat-log-");
            ex.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
            ex.initialize();
            return ex;
        }
    }
    ```

    **DO NOT** add `@EnableAsync` — already declared on `AsyncConfig.java:28` (PATTERNS.md key insight #1; adding a second causes duplicate `AsyncAnnotationBeanPostProcessor` registration).

    **Step 2 — Modify `ChatLogService.save`** (ChatLogService.java). Add at method level:

    ```java
    @Async(com.vn.traffic.chatbot.chat.config.ChatLogAsyncConfig.CHAT_LOG_EXECUTOR)
    @Transactional(propagation = Propagation.REQUIRES_NEW)
    public void save( /* unchanged args */ ) { /* unchanged body */ }
    ```

    Imports to add: `org.springframework.scheduling.annotation.Async`, `org.springframework.transaction.annotation.Propagation`. The `@Transactional` import (`org.springframework.transaction.annotation.Transactional`) may already exist — verify. Class-level `@Service @RequiredArgsConstructor @Slf4j` annotations stay (PATTERNS.md §ChatLogService.java).

    **Step 3 — Wrap both existing callsites in `ChatService.java`** with `List.copyOf(logMessages)` snapshots (D-11):

    Refusal callsite (around ChatService.java:131-139):
    ```java
    // D-11: snapshot BEFORE async handoff — prevents ConcurrentModificationException (Pitfall 7)
    String pipelineLog = String.join("\n", List.copyOf(logMessages));
    chatLogService.save(question, refused, GroundingStatus.REFUSED, null, 0, 0, 0, pipelineLog);
    ```

    Grounded callsite (around ChatService.java:175-190): identical `List.copyOf(logMessages)` wrap.

    Add `import java.util.List;` if absent. DO NOT introduce a wrapper method in `ChatLogService` that calls `this.save(...)` (Pitfall A — would bypass the proxy). `ChatService → chatLogService.save` is already cross-bean via the injected field at ChatService.java:49 (PATTERNS.md §Cross-bean invocation).

    **Step 4 — Run Wave-0 reflection tests to confirm proxy metadata:**
    ```
    ./gradlew test --tests "com.vn.traffic.chatbot.chatlog.service.ChatLogServiceAsyncTest"
    ```
    All 3 tests (saveMethodCarriesAsyncAnnotation, saveMethodCarriesRequiresNewPropagation, asyncSnapshotImmutability) MUST go GREEN.

    **Decision trace (per RESEARCH.md Assumption A6):** Refusal callsite also flows through the now-async save. D-12's intent ("refusals stay synchronous") is preserved at the observability level — refusals still reach the DB via the same bounded `CallerRunsPolicy` queue, so grounding regressions in P8/P9 remain visible via `chat_log` row count. If this turns out wrong in Plan 04 manual smoke, revisit before merging to main.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.traffic.chatbot.chatlog.service.ChatLogServiceAsyncTest"</automated>
  </verify>
  <done>
    - File `src/main/java/com/vn/traffic/chatbot/chat/config/ChatLogAsyncConfig.java` exists.
    - `grep -n "CHAT_LOG_EXECUTOR = \"chatLogExecutor\"" src/main/java/com/vn/traffic/chatbot/chat/config/ChatLogAsyncConfig.java` returns one hit.
    - `grep -n "setCorePoolSize(2)" src/main/java/com/vn/traffic/chatbot/chat/config/ChatLogAsyncConfig.java` returns one hit.
    - `grep -n "setMaxPoolSize(8)" src/main/java/com/vn/traffic/chatbot/chat/config/ChatLogAsyncConfig.java` returns one hit.
    - `grep -n "setQueueCapacity(1000)" src/main/java/com/vn/traffic/chatbot/chat/config/ChatLogAsyncConfig.java` returns one hit.
    - `grep -n "CallerRunsPolicy" src/main/java/com/vn/traffic/chatbot/chat/config/ChatLogAsyncConfig.java` returns one hit.
    - `grep -rn "@EnableAsync" src/main/java` returns exactly ONE hit (still only on `AsyncConfig.java`).
    - `grep -n "@Async" src/main/java/com/vn/traffic/chatbot/chatlog/service/ChatLogService.java` returns one hit on the save method.
    - `grep -n "REQUIRES_NEW" src/main/java/com/vn/traffic/chatbot/chatlog/service/ChatLogService.java` returns one hit.
    - `grep -c "List.copyOf(logMessages)" src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java` returns at least 2 (refusal + grounded; the chitchat third callsite is added in Task 3).
    - Wave-0 test `ChatLogServiceAsyncTest` (all 3 tests) GREEN.
    - `./gradlew compileJava` green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Slim LegalAnswerDraft (drop 4 scenario fields), trim ChatPromptFactory, update AnswerComposer + ChatService draft builders, update frontend, un-@Disable AnswerComposerSlimSchemaTest</name>
  <files>
    src/main/java/com/vn/traffic/chatbot/chat/service/LegalAnswerDraft.java,
    src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java,
    src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java,
    src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java,
    src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerSlimSchemaTest.java,
    frontend/types/api.ts,
    frontend/components/chat/message-bubble.tsx,
    frontend/components/chat/scenario-accordion.tsx
  </files>
  <read_first>
    - .planning/phases/07-chat-latency-foundation/07-PATTERNS.md §LegalAnswerDraft.java + §ChatPromptFactory.java + §ChatService.java (draft-builder cascade)
    - .planning/phases/07-chat-latency-foundation/07-RESEARCH.md §Pitfall 9 + §Open Question #1 resolution (pass List.of() for scenarioFacts in ChatAnswerResponse; D-04 no-null-fill)
    - src/main/java/com/vn/traffic/chatbot/chat/service/LegalAnswerDraft.java (current 12-field record)
    - src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java (lines 60, 62 — instruction lines listing all 12 keys)
    - src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java (lines 49, 79 — fallback draft constructor + scenarioFacts read site)
    - src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java (lines 324-337 fallbackDraft, 340-342 emptyDraft — both construct LegalAnswerDraft with 12 args)
    - src/main/java/com/vn/traffic/chatbot/chat/service/ScenarioAnswerComposer.java (FULL READ — confirm it does NOT read LegalAnswerDraft.scenarioRule/Outcome/Actions per RESEARCH.md Open Question #2)
    - src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerSlimSchemaTest.java (from Plan 01 — currently class-level @Disabled with `/* ... */` body + Assertions.fail placeholder; Task 2 un-@Disables AND uncomments)
    - src/test/java/com/vn/traffic/chatbot/chat/contract/ChatContractSerializationTest.java (existing — will need update if it asserts dropped keys)
    - frontend/types/api.ts (identify scenarioRule/Outcome/Actions fields on ChatAnswerResponse)
    - frontend/components/chat/message-bubble.tsx (identify render branches for the 4 fields)
    - frontend/components/chat/scenario-accordion.tsx (determine whether the whole component is dead or still used for ScenarioAnalysisResponse.{facts,rule,outcome,actions})
    - frontend/AGENTS.md AND frontend/CLAUDE.md (both exist — honour their conventions before editing any TSX/TS file)
    - Context7 `/vercel/next.js` (global context7.md rule applies — if unfamiliar with current Next.js 15 patterns, fetch docs before editing TSX)
  </read_first>
  <action>
    **Step 1 — Trim `LegalAnswerDraft`** to 8 fields:

    ```java
    public record LegalAnswerDraft(
        String conclusion,
        String answer,
        String uncertaintyNotice,
        List<String> legalBasis,
        List<String> penalties,
        List<String> requiredDocuments,
        List<String> procedureSteps,
        List<String> nextSteps
    ) {}
    ```

    Remove the 4 trailing `List<String>` fields (`scenarioFacts`, `scenarioRule`, `scenarioOutcome`, `scenarioActions`).

    **Step 2 — Trim `ChatPromptFactory.buildPrompt`** (lines 60, 62). Replace verbatim (PATTERNS.md §ChatPromptFactory.java):

    From:
    ```java
    prompt.append("Tất cả các khóa conclusion, answer, uncertaintyNotice, legalBasis, penalties, requiredDocuments, procedureSteps, nextSteps, scenarioFacts, scenarioRule, scenarioOutcome, scenarioActions phải luôn xuất hiện trong JSON.").append("\n");
    // ...
    prompt.append("Trả về JSON hợp lệ với đúng các khóa sau: conclusion, answer, uncertaintyNotice, legalBasis, penalties, requiredDocuments, procedureSteps, nextSteps, scenarioFacts, scenarioRule, scenarioOutcome, scenarioActions.").append("\n");
    ```

    To (scenario keys stripped):
    ```java
    prompt.append("Tất cả các khóa conclusion, answer, uncertaintyNotice, legalBasis, penalties, requiredDocuments, procedureSteps, nextSteps phải luôn xuất hiện trong JSON.").append("\n");
    // ...
    prompt.append("Trả về JSON hợp lệ với đúng các khóa sau: conclusion, answer, uncertaintyNotice, legalBasis, penalties, requiredDocuments, procedureSteps, nextSteps.").append("\n");
    ```

    **DO NOT touch** `SYSTEM_CONTEXT_FALLBACK` (ChatPromptFactory.java:18-21) or `CLARIFICATION_RULES` (lines 23-28) per D-21 — retiring `SYSTEM_CONTEXT_FALLBACK` is P9 scope.

    **Step 3 — Update `AnswerComposer`:**
    - AnswerComposer.java:49 — fallback draft constructor: trim last 4 args so it matches the new 8-field record.
    - AnswerComposer.java:79 — replace `safeDraft.scenarioFacts()` with literal `List.of()` when populating `ChatAnswerResponse.scenarioFacts` (D-04: no null fill; `List.of()` is an explicit empty, not null).

    **Step 4 — Update `ChatService` draft builders:**
    - `fallbackDraft()` (ChatService.java:324-337) — trim trailing `List.of(), null, null, List.of()` to match new 8-arg constructor.
    - `emptyDraft()` (ChatService.java:340-342) — same trim.

    **Step 5 — Verify `ScenarioAnswerComposer` is untouched.** Grep the file for any reference to `.scenarioRule()`, `.scenarioOutcome()`, `.scenarioActions()`, `.scenarioFacts()` on a `LegalAnswerDraft` receiver. If ANY found, STOP and escalate — this violates RESEARCH.md Open Question #2 default ("separate LLM path"). Expected outcome: zero hits; leave file untouched.

    **Step 6 — Un-@Disable `AnswerComposerSlimSchemaTest`:**
    - Remove class-level `@Disabled("Enabled by Plan 07-03 Task 2 when LegalAnswerDraft is slimmed")`.
    - Remove `import org.junit.jupiter.api.Disabled;` if it becomes unused.
    - Uncomment the real test bodies (the `/* ... */`-wrapped 8-arg constructor blocks) and delete the `Assertions.fail("Plan 03 Task 2 will enable this")` placeholder bodies.
    - Verify `! grep -q '@Disabled' src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerSlimSchemaTest.java`.

    **Step 7 — Update `ChatContractSerializationTest`** if it asserts JSON keys for the 4 dropped fields — remove those assertions; add an assertion that `LegalAnswerDraft` serialized JSON does NOT contain `scenarioRule`/`scenarioOutcome`/`scenarioActions`/`scenarioFacts` keys. (If the test does not exist or does not reference these keys, skip.)

    **Step 8 — Frontend (coordinated per D-04):**
    - `frontend/types/api.ts`: remove the four fields `scenarioRule`, `scenarioOutcome`, `scenarioActions` from `ChatAnswerResponse`. For `scenarioFacts`: per D-04 + RESEARCH.md Open Question #1, backend still emits `List.of()` — KEEP `scenarioFacts: string[]` in TS type.
    - `frontend/components/chat/message-bubble.tsx`: delete any JSX branches referencing `answer.scenarioRule`, `answer.scenarioOutcome`, `answer.scenarioActions`. If a branch depends on `scenarioFacts`, leave it (BE still sends []).
    - `frontend/components/chat/scenario-accordion.tsx`: inspect. If it renders ONLY `response.scenarioAnalysis.{facts,rule,outcome,actions}` (multi-turn scenario feature via `ScenarioAnalysisResponse`), LEAVE UNTOUCHED — that path is separate. If it renders `ChatAnswerResponse.scenarioRule/Outcome/Actions`, strip those branches. If the whole component becomes dead, delete.

    Run:
    ```
    ./gradlew test --tests "com.vn.traffic.chatbot.chat.service.AnswerComposerSlimSchemaTest" --tests "com.vn.traffic.chatbot.chat.contract.*"
    cd frontend && npx tsc --noEmit
    ```

    Wave-0 `AnswerComposerSlimSchemaTest` (2 tests) MUST go GREEN. Frontend `npx tsc --noEmit` MUST succeed with no TS errors.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.traffic.chatbot.chat.service.AnswerComposerSlimSchemaTest" --tests "com.vn.traffic.chatbot.chat.contract.*" &amp;&amp; cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    - `grep -c "scenarioFacts\|scenarioRule\|scenarioOutcome\|scenarioActions" src/main/java/com/vn/traffic/chatbot/chat/service/LegalAnswerDraft.java` returns 0.
    - `grep -cE "^\s*(String|List<String>)\s+\w+[;,]" src/main/java/com/vn/traffic/chatbot/chat/service/LegalAnswerDraft.java` returns exactly 8 (the 8 record fields).
    - `grep -c "scenarioFacts\|scenarioRule\|scenarioOutcome\|scenarioActions" src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java` returns 0.
    - `grep -n "SYSTEM_CONTEXT_FALLBACK" src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java` STILL returns one hit (D-21 preserved).
    - `grep -n "List.of()" src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java` shows the `List.of()` literal passed for `scenarioFacts` in the compose call.
    - `grep -c "scenarioRule\|scenarioOutcome\|scenarioActions" src/main/java/com/vn/traffic/chatbot/chat/service/ScenarioAnswerComposer.java` returns 0 (no unintended coupling — Open Question #2 default holds).
    - `! grep -q '@Disabled' src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerSlimSchemaTest.java` (annotation fully removed).
    - `grep -c "scenarioRule\|scenarioOutcome\|scenarioActions" frontend/types/api.ts` returns 0.
    - `grep -c "scenarioRule\|scenarioOutcome\|scenarioActions" frontend/components/chat/message-bubble.tsx` returns 0.
    - Wave-0 test `AnswerComposerSlimSchemaTest` GREEN (2 tests).
    - `./gradlew compileJava compileTestJava` green (all test files that referenced 12-arg LegalAnswerDraft constructor now compile because record arity matches).
    - Frontend strict typecheck (`cd frontend && npx tsc --noEmit`) succeeds with no TS errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Chitchat short-circuit in ChatService — isGreetingOrChitchat + AnswerComposer.composeChitchat + third List.copyOf callsite</name>
  <files>
    src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java,
    src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java
  </files>
  <read_first>
    - .planning/phases/07-chat-latency-foundation/07-CONTEXT.md D-02, D-12
    - .planning/phases/07-chat-latency-foundation/07-RESEARCH.md §Code Examples §Chitchat short-circuit (lines 515-530) + §Open Question #4 resolution
    - .planning/phases/07-chat-latency-foundation/07-PATTERNS.md §ChatService.java (chitchat placement at line 94, after `logger.accept("User prompt: ...")`)
    - src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java (current doAnswer method; confirm line 94 is the right insertion point after re-reading post-Task-1 edits)
    - src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java (identify existing `compose(...)` signature to model `composeChitchat(...)` after)
    - src/main/java/com/vn/traffic/chatbot/chat/service/containsAnyLegalCitation (or wherever it lives — find via grep)
  </read_first>
  <action>
    **Step 1 — Add `AnswerComposer.composeChitchat(String question)`** method returning `ChatAnswerResponse`. Behavior:
    - Produces a short conversational Vietnamese reply (e.g., for "Xin chào" → "Xin chào! Tôi có thể giúp bạn tra cứu luật giao thông Việt Nam. Bạn muốn hỏi gì?").
    - Sets `sources = List.of()`, `citations = List.of()`, `scenarioFacts = List.of()` (preserves D-04 no-null-fill for the surviving field).
    - Uses whatever defaults the existing `compose(...)` method uses for the other DTO fields.
    - Pure method; no LLM call, no retrieval.

    **Step 2 — Add `isGreetingOrChitchat(String question)`** private helper in `ChatService`. Heuristic (D-02, keep tiny — NOT the P8 classifier):

    ```java
    private static final java.util.regex.Pattern CHITCHAT_PATTERN =
        java.util.regex.Pattern.compile("^\\s*(xin chào|chào|hi|hello|hey|chào bạn|cảm ơn|thanks|tạm biệt|bye)\\b.*",
            java.util.regex.Pattern.CASE_INSENSITIVE | java.util.regex.Pattern.UNICODE_CASE);

    private boolean isGreetingOrChitchat(String question) {
        if (question == null) return false;
        String trimmed = question.trim();
        if (trimmed.isEmpty()) return false;
        if (CHITCHAT_PATTERN.matcher(trimmed).matches() && trimmed.split("\\s+").length <= 8) {
            return true;
        }
        return false;
    }
    ```

    **Step 3 — Insert short-circuit branch in `doAnswer`** immediately after `logger.accept("User prompt: ...")` (ChatService.java:94 per PATTERNS.md — re-confirm line number after Task 1 edits):

    ```java
    // D-02 / PERF-02: Chitchat bypass — direct code, no flag. Rollback = git revert.
    if (isGreetingOrChitchat(question)) {
        logger.accept("Path: chitchat — skipping retrieval + LLM (P7)");
        ChatAnswerResponse chitchat = answerComposer.composeChitchat(question);
        // D-11: snapshot BEFORE async handoff
        String pipelineLog = String.join("\n", List.copyOf(logMessages));
        chatLogService.save(question, chitchat, GroundingStatus.GROUNDED, null, 0, 0, 0, pipelineLog);
        return chitchat;
    }
    ```

    This is the THIRD `chatLogService.save` callsite and the THIRD `List.copyOf(logMessages)` snapshot.

    **Step 4 — Manual Vietnamese behaviour smoke (quick):**
    - `isGreetingOrChitchat("Xin chào")` → true.
    - `isGreetingOrChitchat("Vượt đèn đỏ phạt bao nhiêu")` → false.
    - `isGreetingOrChitchat("hi there what is the fine for running a red light")` → false (9+ words).
    - `isGreetingOrChitchat("")` → false.
    - `isGreetingOrChitchat(null)` → false.

    Add a small unit test `src/test/java/com/vn/traffic/chatbot/chat/service/ChatServiceChitchatTest.java` covering the 5 cases above. Use Mockito `@Mock` for `ChatLogService`, `AnswerComposer`, and any other dependencies; verify that for the chitchat case, `retrievalPolicy` and `vectorStore` are NEVER called (via `verifyNoInteractions(...)`).

    Run:
    ```
    ./gradlew test --tests "com.vn.traffic.chatbot.chat.service.ChatServiceChitchatTest" --tests "com.vn.traffic.chatbot.chat.service.AnswerComposerSlimSchemaTest" --tests "com.vn.traffic.chatbot.chat.service.ChatServiceTest" --tests "com.vn.traffic.chatbot.chatlog.service.ChatLogServiceAsyncTest"
    ```

    All MUST be GREEN.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.traffic.chatbot.chat.service.ChatServiceChitchatTest" --tests "com.vn.traffic.chatbot.chat.service.ChatServiceTest" --tests "com.vn.traffic.chatbot.chat.service.AnswerComposerSlimSchemaTest" --tests "com.vn.traffic.chatbot.chatlog.service.ChatLogServiceAsyncTest"</automated>
  </verify>
  <done>
    - `grep -n "isGreetingOrChitchat" src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java` returns at least 2 hits (method decl + call site).
    - `grep -n "composeChitchat" src/main/java/com/vn/traffic/chatbot/chat/service/AnswerComposer.java` returns one hit.
    - `grep -n "composeChitchat" src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java` returns one hit.
    - `grep -c "List.copyOf(logMessages)" src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java` returns exactly 3 (refusal, grounded, chitchat).
    - `grep -n "Path: chitchat" src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java` returns one hit (pipeline-log marker per Open Question #4).
    - File `src/test/java/com/vn/traffic/chatbot/chat/service/ChatServiceChitchatTest.java` exists with 5 tests.
    - All listed tests in the verify command GREEN.
    - `./gradlew compileJava compileTestJava test` full-module run GREEN end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP POST /api/chat request thread → chatLogExecutor background thread | Async handoff crosses thread-trust boundary; no user auth change. |
| ChatService → ChatLogService async proxy | Proxy boundary; `@Async` annotation must be on a cross-bean-invoked method (Pitfall A). |
| User question → isGreetingOrChitchat regex | Untrusted input; regex must be anchored + bounded. |
| LLM JSON response (now slim) → AnswerComposer → ChatAnswerResponse DTO | Shape change; no trust escalation. |
| Frontend render of ChatAnswerResponse | Removed branches; no XSS surface change. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | T (Tampering) — Log race via mutable list shared across threads | ChatService.doAnswer → ChatLogService.save(pipelineLog) | mitigate | `String.join("\n", List.copyOf(logMessages))` on the request thread BEFORE the async call (D-11). `List.copyOf` returns an immutable snapshot — mutating the original list afterwards has no effect on the passed String. Enforced at three callsites. |
| T-07-10 | R (Repudiation) — Lost `chat_log` rows under executor saturation | chatLogExecutor bounded queue | mitigate | `CallerRunsPolicy` (D-09) — when queue full, the task runs synchronously on the request thread as backstop. No silent drop. Bounded queue + 2/8 pool + 1000-cap sized for solo-dev load. Micrometer `executor.queued.tasks{name=chatLogExecutor}` visible via Prometheus for ops. |
| T-07-11 | D (Denial of Service) — Regex ReDoS on chitchat pattern | isGreetingOrChitchat | mitigate | Pattern is anchored at start (`^\s*`), uses literal alternation with no nested quantifiers, bounded by word-count check (≤ 8 words). No catastrophic backtracking surface. |
| T-07-12 | I (Information Disclosure) — Missing chat_log rows for refusals hide grounding regressions (D-12 concern) | Async refusal save | accept | Refusal callsite flows through the same annotated async method; writes still complete via `CallerRunsPolicy` + bounded queue. Regression visibility preserved through Micrometer `chat_log` count vs HTTP-200 count (Plan 04 manual smoke). Per RESEARCH.md Assumption A6 default; revisit if smoke shows divergence. |
| T-07-13 | T (Tampering) — LLM emits scenario keys that AnswerComposer no longer parses | LLM JSON response after prompt trim | mitigate | Prompt trim removes the INSTRUCTION to emit keys; most compliant models will not emit them. If a model does, Jackson on `LegalAnswerDraft` record will either ignore-unknown (default) or fail — this is a Jackson config detail. Per D-04 "no null/[] tolerance fill", rely on Jackson's default FAIL_ON_UNKNOWN_PROPERTIES=false to ignore. Verify default in Plan 04 manual smoke. |
| T-07-14 | E (Elevation of Privilege) — Chitchat short-circuit bypasses grounding policy for legal queries | isGreetingOrChitchat false-positive | mitigate | Regex requires a greeting prefix AND ≤8 words. Legal queries like "Vượt đèn đỏ phạt bao nhiêu" do not match. Test `ChatServiceChitchatTest` explicitly asserts the legal-query negative case. If false-positive rate is measurable in Plan 04 smoke, tighten regex or add a deny-list of legal-keyword tokens (P8 classifier is the permanent fix). |
</threat_model>

<verification>
- All three sub-changes land in the same plan for file-ownership reasons (ChatService.java is the serializing file).
- Wave-0 tests `ChatLogServiceAsyncTest` (3) + `AnswerComposerSlimSchemaTest` (2) GREEN.
- New `ChatServiceChitchatTest` (5) GREEN.
- Existing `ChatServiceTest` + `ChatContractSerializationTest` still GREEN (no regression).
- `./gradlew test` full backend suite GREEN.
- Frontend `npx tsc --noEmit` succeeds (strict typecheck).
- `grep -c "@EnableAsync" src/main/java` returns exactly 1 (still only AsyncConfig).
- `grep -c "List.copyOf(logMessages)" ChatService.java` returns exactly 3.
- No references to `scenarioRule/scenarioOutcome/scenarioActions` remain in Java or TS.
</verification>

<success_criteria>
- PERF-02: Greetings never invoke retrieval/LLM; ChatServiceChitchatTest verifies `verifyNoInteractions(vectorStore, ...)`.
- PERF-03: `ChatLogService.save` annotated `@Async @Transactional(REQUIRES_NEW)`; reflection tests pass; `List.copyOf` snapshot at all 3 callsites; `@EnableAsync` NOT duplicated.
- PERF-01 (schema trim contribution): `LegalAnswerDraft` is 8 fields; `ChatPromptFactory` no longer instructs LLM to emit the 4 dropped keys; `AnswerComposer` passes `List.of()` for `scenarioFacts` per D-04.
- D-21 preserved: `SYSTEM_CONTEXT_FALLBACK` untouched.
- No `ChatV11Properties`, no feature-flag wiring, no keyword-list deletion (P8), no advisor changes (P8/P9).
- Frontend rendering branches for 3 dropped fields removed; strict TS typecheck green.
- `ScenarioAnswerComposer` untouched (Open Question #2 default held).
- `AnswerComposerSlimSchemaTest` un-@Disabled.
</success_criteria>

<output>
After completion, create `.planning/phases/07-chat-latency-foundation/07-03-SUMMARY.md`.
</output>
