---
phase: "4.1"
plan: "04.1-03"
type: execute
wave: 3
depends_on:
  - "04.1-01"
  - "04.1-02"
files_modified:
  - .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md
autonomous: true
requirements:
  - PLAT-01

must_haves:
  truths:
    - "A written inventory document exists that lists every hardcoded string, keyword pattern, and rule found in ClarificationPolicy, AnswerCompositionPolicy, and RetrievalPolicy"
    - "Each gap entry is classified by gap level (NONE/LOW/MEDIUM/HIGH) and includes an action recommendation"
    - "RetrievalPolicy.RETRIEVAL_FILTER is explicitly documented as intentionally hardcoded for safety reasons"
    - "ChatService.containsLegalSignal() keyword list is flagged as a MEDIUM gap for future admin config"
    - "ClarificationPolicy.toPendingFact() user-facing question strings are flagged as a MEDIUM gap"
    - "AnswerCompositionPolicy is documented as having no gaps — all strings are already configurable via AiParameterSet"
  artifacts:
    - path: ".planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md"
      provides: "Configurability gap inventory for Track C"
      contains: "RetrievalPolicy\|ClarificationPolicy\|AnswerCompositionPolicy"
  key_links:
    - from: "04.1-CONFIGURABILITY-AUDIT.md"
      to: "ClarificationPolicy.java, AnswerCompositionPolicy.java, RetrievalPolicy.java, ChatService.java"
      via: "direct codebase read during audit"
      pattern: "toPendingFact|containsLegalSignal|RETRIEVAL_FILTER|paramProvider"
---

<objective>
Track C: Read ClarificationPolicy, AnswerCompositionPolicy, RetrievalPolicy, and ChatService. Produce 04.1-CONFIGURABILITY-AUDIT.md as the single deliverable — an inventory of all remaining hardcoded strings, keyword patterns, and rules that could become admin-configurable.

Purpose: Provide a written record of configurability gaps to guide future phases. No production code changes. Depends on Plans A and B being complete so the audit can reference their AppProperties migration as resolved.
Output: One markdown file — 04.1-CONFIGURABILITY-AUDIT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONTEXT.md
@.planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-RESEARCH.md
@.planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-01-SUMMARY.md
@.planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-02-SUMMARY.md

<interfaces>
<!-- Pre-research findings from 04.1-RESEARCH.md Track C section -->

ClarificationPolicy findings:
- builtInRequiredFactKeys(): hardcoded Vietnamese keyword triggers (tai nạn, va chạm, nồng độ cồn, etc.)
  → fallback-only when paramProvider.getList("caseAnalysis.requiredFacts") returns empty. LOW gap.
- toPendingFact(): switch on fact key, returns user-facing Vietnamese question strings.
  These are NEVER configurable at runtime. MEDIUM gap.

AnswerCompositionPolicy findings:
- All user-facing strings read from paramProvider.getString(key, DEFAULT). Static constants are fallbacks only.
  NONE — fully configurable.

RetrievalPolicy findings:
- RETRIEVAL_FILTER = "approvalState == 'APPROVED' && trusted == 'true' && active == 'true'"
  Intentionally hardcoded — safety-critical per Phase 01 decision D-13. NONE (by design).
- topK and similarityThreshold read from active parameter set. Configurable.

ChatService findings:
- @Value fields for app.chat.retrieval.top-k and app.chat.grounding.limited-threshold
  → Tracked in Track A (D-05) for AppProperties migration. LOW (resolved by Plan A).
- containsLegalSignal(): hardcoded Vietnamese legal signal keywords (nghị định, luật, thông tư, etc.)
  Citation quality heuristics, not user-facing strings. MEDIUM gap — flag for future admin config.
</interfaces>
</context>

<tasks>

<task id="04.1-03-T1" title="Read source files and produce 04.1-CONFIGURABILITY-AUDIT.md">
  <read_first>
    - src/main/java/com/vn/traffic/chatbot/chat/service/ClarificationPolicy.java — read entire file for hardcoded patterns
    - src/main/java/com/vn/traffic/chatbot/chat/service/AnswerCompositionPolicy.java — read entire file for configurable vs hardcoded strings
    - src/main/java/com/vn/traffic/chatbot/retrieval/RetrievalPolicy.java — read entire file for hardcoded RETRIEVAL_FILTER and configurable fields
    - src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java — read for @Value fields and containsLegalSignal() method
    - .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-01-SUMMARY.md — confirm AppProperties migration status
  </read_first>
  <description>
Read all four source files directly. Produce the audit report as `.planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md`.

The audit report must include:

**Section 1 — Scope:** Which files were audited and which commit/date they represent.

**Section 2 — Findings Table:** Full inventory using this format:

| Location | Item | Type | Gap Level | Rationale | Action |
|----------|------|------|-----------|-----------|--------|
| ClarificationPolicy.builtInRequiredFactKeys() | Vietnamese keyword triggers for vehicleType, violationType, alcoholLevel, etc. | Fallback-only — bypassed when paramProvider returns values | LOW | Operators can override via AiParameterSet; hardcoded list is a reasonable fallback | Document as intentional fallback; low priority for admin config |
| ClarificationPolicy.toPendingFact() | User-facing Vietnamese clarification question strings per fact key | Not configurable at any layer | MEDIUM | Admin may need to customize question wording for different legal contexts | Flag for future phase: add to AiParameterSet as a parameterized prompt template per fact key |
| AnswerCompositionPolicy | All user-facing message strings | Fully configurable via paramProvider | NONE | No gap | No action |
| RetrievalPolicy.RETRIEVAL_FILTER | Safety-critical retrieval gate expression | Intentionally hardcoded (Phase 01 decision D-13) | NONE (by design) | Changing this would allow unvetted sources into retrieval — security-critical | Document as permanent hardcode; do NOT make configurable |
| ChatService.containsLegalSignal() | Vietnamese legal signal keyword list (nghị định, luật, thông tư, etc.) | Citation quality heuristic, not user-facing | MEDIUM | Keyword list determines when citation grounding is enforced; wrong keywords reduce answer quality | Flag for future phase: expose as editable keyword list in AiParameterSet |
| ChatService @Value fields (top-k, limited-threshold) | Scattered @Value injection for app.chat.* | Resolved by Plan A (AppProperties migration) | RESOLVED | Migrated to AppProperties in Phase 4.1 Track A | Closed |

**Section 3 — Security Note:** Explain why RetrievalPolicy.RETRIEVAL_FILTER must remain hardcoded. The filter enforces the approvalState == APPROVED && trusted == true && active == true gate. Making this admin-configurable would create a security boundary bypass allowing unapproved sources into retrieval.

**Section 4 — Priority Recommendations:**
- P1 (next phase): ClarificationPolicy.toPendingFact() question strings → parameterize per fact key in AiParameterSet
- P2 (future): ChatService.containsLegalSignal() keyword list → admin-editable list in AiParameterSet
- Permanent: RetrievalPolicy.RETRIEVAL_FILTER → hardcoded, never expose as configurable

**Section 5 — Coverage Summary:** Confirm that AnswerCompositionPolicy has zero gaps and is fully covered by the existing AiParameterSet parameter system.

After writing the file, verify the content is accurate against the actual source code (re-read the files if needed). The audit must reflect what the code actually contains on the day of execution, not assumptions.
  </description>
  <acceptance_criteria>
    - File exists: .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md
    - `grep "RETRIEVAL_FILTER" .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md` returns a match
    - `grep -i "intentionally hardcoded\|safety-critical\|by design" .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md` returns a match
    - `grep "toPendingFact\|MEDIUM" .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md` returns at least 2 matches
    - `grep "containsLegalSignal" .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md` returns a match
    - `grep "AnswerCompositionPolicy" .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md` returns a match
    - `grep -c "NONE\|LOW\|MEDIUM\|HIGH\|RESOLVED" .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md` returns >= 5 (multiple gap level entries)
    - File is not empty and contains at least 80 lines
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit document → reader | Document may surface information about internal heuristics; file is internal .planning artifact only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.1-03-01 | Information Disclosure | Audit document contains internal keyword lists and heuristics | accept | .planning/ is a development artifact, not deployed. No user-facing exposure. Residual risk: low. |
| T-4.1-03-02 | Tampering | Audit document could incorrectly classify RetrievalPolicy.RETRIEVAL_FILTER as configurable | mitigate | Acceptance criteria explicitly verify that the document contains the hardcoded/safety-critical designation for RETRIEVAL_FILTER. |
</threat_model>

<verification>
Run after the task completes:

```bash
# Audit file exists
ls -la .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md

# Critical content checks
grep "RETRIEVAL_FILTER" .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md
grep "toPendingFact" .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md
grep "containsLegalSignal" .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md
grep -i "intentionally hardcoded" .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md

# Manual check: open the file and confirm the findings table is present and has correct gap levels
```
</verification>

<success_criteria>
1. File `.planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md` exists and is non-empty
2. Document includes findings for ClarificationPolicy (LOW + MEDIUM), AnswerCompositionPolicy (NONE), RetrievalPolicy (NONE/by design), and ChatService (MEDIUM + RESOLVED)
3. RetrievalPolicy.RETRIEVAL_FILTER is explicitly documented as intentionally hardcoded with security rationale
4. Priority recommendations section guides the next phase on what to parameterize
5. No production code changes in this plan — audit is documentation only
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-03-SUMMARY.md`
</output>
