---
phase: 05-quality-operations-evaluation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/layout/app-sidebar.tsx
autonomous: true
requirements: [ADMIN-04, ADMIN-05]
gap_closure: true

must_haves:
  truths:
    - "Navigating to /checks/runs highlights only 'Lịch sử chạy', not 'Kiểm tra chất lượng'"
  artifacts:
    - path: "frontend/components/layout/app-sidebar.tsx"
      provides: "Sidebar nav with exact-match active logic"
      contains: "pathname === item.href"
  key_links:
    - from: "frontend/components/layout/app-sidebar.tsx"
      to: "SidebarMenuButton isActive prop"
      via: "pathname === item.href"
      pattern: "pathname === item\\.href"
---

<objective>
Fix sidebar double-highlight: when on /checks/runs, both "Kiểm tra chất lượng" (/checks) and
"Lịch sử chạy" (/checks/runs) are highlighted simultaneously.

Purpose: Correct navigation feedback so exactly one nav item is active at a time.
Output: Single-line change in app-sidebar.tsx; no other files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace startsWith active-check with exact pathname match</name>
  <files>frontend/components/layout/app-sidebar.tsx</files>
  <action>
    On line 70 of frontend/components/layout/app-sidebar.tsx, the isActive prop currently reads:

      isActive={pathname === item.href || pathname.startsWith(item.href + '/')}

    Replace it with exact-match only:

      isActive={pathname === item.href}

    Rationale: every entry in adminNavItems is a distinct leaf route — no item is a logical
    parent of another that should stay highlighted on child pages. The "/checks" item is a
    standalone CRUD page; "/checks/runs" is its own independent entry. Prefix matching is
    wrong here and causes the double-highlight. Exact match is correct for all items in this list.

    Do NOT change any other line. Do NOT reformat the file.
  </action>
  <verify>
    <automated>cd frontend && npx jest __tests__/app-sidebar.test.tsx --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Line 70 reads `isActive={pathname === item.href}` with no startsWith branch.
    - Existing sidebar tests pass (or no test exists for this specific case — see human verify).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Single-line fix to sidebar active-link logic — exact pathname match replaces prefix match.</what-built>
  <how-to-verify>
    1. Start the frontend dev server: cd frontend && npm run dev
    2. Navigate to http://localhost:3000/checks/runs
    3. Inspect the sidebar: only "Lịch sử chạy" should have the active background/highlight.
       "Kiểm tra chất lượng" (/checks) must NOT be highlighted.
    4. Navigate to http://localhost:3000/checks
       "Kiểm tra chất lượng" should now be highlighted; "Lịch sử chạy" should NOT.
    5. Navigate to /sources, /index, /parameters — each highlights only its own item.
  </how-to-verify>
  <resume-signal>Type "approved" if the sidebar highlights exactly one item on each route, or describe any remaining issue.</resume-signal>
</task>

</tasks>

<verification>
- `isActive={pathname === item.href}` on line 70 of app-sidebar.tsx (no startsWith).
- All other lines in the file are unchanged.
- Sidebar tests pass: `npx jest __tests__/app-sidebar.test.tsx`.
</verification>

<success_criteria>
On /checks/runs: only "Lịch sử chạy" is highlighted.
On /checks: only "Kiểm tra chất lượng" is highlighted.
No other nav item changes behaviour.
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-operations-evaluation/05-04-SUMMARY.md`
</output>
