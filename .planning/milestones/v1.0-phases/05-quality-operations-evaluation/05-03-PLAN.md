---
phase: 05-quality-operations-evaluation
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01-PLAN.md
  - 05-02-PLAN.md
files_modified:
  - frontend/types/api.ts
  - frontend/lib/api/chat-logs.ts
  - frontend/lib/api/check-defs.ts
  - frontend/lib/api/check-runs.ts
  - frontend/hooks/use-chat-logs.ts
  - frontend/hooks/use-check-defs.ts
  - frontend/hooks/use-check-runs.ts
  - frontend/app/(admin)/layout.tsx
  - frontend/app/(admin)/chat-logs/page.tsx
  - frontend/app/(admin)/chat-logs/[id]/page.tsx
  - frontend/app/(admin)/checks/page.tsx
  - frontend/app/(admin)/checks/runs/page.tsx
  - frontend/app/(admin)/checks/runs/[id]/page.tsx
  - frontend/app/(admin)/parameters/page.tsx
autonomous: false
requirements:
  - ADMIN-04
  - ADMIN-05

must_haves:
  truths:
    - "Admin can browse and filter chat logs (by date range, grounding status, question text) in the admin UI"
    - "Admin can click a chat log row and see full question, answer, citations panel, token counts, and response time"
    - "Admin can create, edit, delete, and toggle active status of check definitions in a master-detail form"
    - "Admin can click 'Chạy kiểm tra' to trigger a check run; the run appears in the history list with status RUNNING then COMPLETED"
    - "Admin can click a check run to see per-check results with question, reference answer, actual answer, and score badge"
    - "Admin can select chatModel and evaluatorModel from dropdowns in the parameters admin screen"
  artifacts:
    - path: "frontend/app/(admin)/chat-logs/page.tsx"
      provides: "DataTable with filter toolbar — date range, grounding status select, question text search"
      contains: "groundingStatus"
    - path: "frontend/app/(admin)/chat-logs/[id]/page.tsx"
      provides: "Read-only detail view with question card, answer card, citations panel, metadata row"
      contains: "Lịch sử hội thoại"
    - path: "frontend/app/(admin)/checks/page.tsx"
      provides: "Master-detail CRUD form for CheckDef; 'Chạy kiểm tra' trigger button"
      contains: "Chạy kiểm tra"
    - path: "frontend/app/(admin)/checks/runs/page.tsx"
      provides: "DataTable of run history with auto-poll while RUNNING"
      contains: "refetchInterval"
    - path: "frontend/app/(admin)/checks/runs/[id]/page.tsx"
      provides: "Run summary card + per-check results DataTable with Sheet row expansion"
      contains: "actualAnswer"
    - path: "frontend/app/(admin)/parameters/page.tsx"
      provides: "Extended with chatModel and evaluatorModel Select dropdowns"
      contains: "Mô hình trò chuyện"
    - path: "frontend/types/api.ts"
      provides: "ChatLog*, CheckDef*, CheckRun*, AllowedModel TypeScript types"
      contains: "groundingStatus"
  key_links:
    - from: "frontend/app/(admin)/chat-logs/page.tsx"
      to: "/api/v1/admin/chat-logs"
      via: "TanStack Query useQuery + use-chat-logs.ts"
      pattern: "useChatLogs"
    - from: "frontend/app/(admin)/checks/page.tsx"
      to: "/api/v1/admin/check-runs/trigger"
      via: "useMutation in use-check-runs.ts"
      pattern: "triggerCheckRun"
    - from: "frontend/app/(admin)/checks/runs/page.tsx"
      to: "refetchInterval"
      via: "TanStack Query auto-poll: 5000ms while any run has status RUNNING"
      pattern: "refetchInterval.*5000"
    - from: "frontend/app/(admin)/parameters/page.tsx"
      to: "/api/v1/admin/allowed-models"
      via: "useAllowedModels() hook loading model options for Select dropdowns"
      pattern: "useAllowedModels"
---

<objective>
Build all six Phase 5 admin UI screens in Next.js. Screens follow established shadcn/ui + TanStack Query patterns from existing admin pages. All copy is Vietnamese-first per UI-SPEC. Includes: chat log list/detail, check definitions CRUD, check run history/detail, and parameters extension for model dropdowns.

Purpose: Delivers the admin-facing ADMIN-04 (chat log review) and ADMIN-05 (check definition management and run review) requirements.
Output: 6 screen files, 3 API client files, 3 hook files, extended types, updated sidebar nav, updated parameters page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-quality-operations-evaluation/05-CONTEXT.md
@.planning/phases/05-quality-operations-evaluation/05-UI-SPEC.md
@.planning/phases/05-quality-operations-evaluation/05-01-SUMMARY.md
@.planning/phases/05-quality-operations-evaluation/05-02-SUMMARY.md

<interfaces>
<!-- Key existing patterns and contracts the executor must replicate exactly. -->

Existing admin patterns to follow (READ THESE before writing any screen):
- frontend/app/(admin)/sources/page.tsx — DataTable list page structure (space-y-4 p-6, filter bar)
- frontend/app/(admin)/parameters/page.tsx — master-detail form, AlertDialog delete, react-hook-form + zod
- frontend/app/(admin)/trust-policy/page.tsx — semantic badge colors, w-64 left panel + flex-1 right, active item highlight
- frontend/hooks/use-parameters.ts — TanStack Query hook pattern (useQuery + useMutation)
- frontend/lib/api/parameters.ts — API client function pattern (fetch wrapper)
- frontend/components/admin/data-table.tsx — DataTable component props and column definition pattern
- frontend/components/layout/query-boundary.tsx — QueryBoundary wrapping pattern

Backend REST endpoints (from Plans 01 + 02):
```
GET  /api/v1/admin/chat-logs                    → Page<ChatLogResponse>
     params: groundingStatus, from, to, q, page, size, sort
GET  /api/v1/admin/chat-logs/{logId}            → ChatLogDetailResponse
GET  /api/v1/admin/check-defs                   → List<CheckDefResponse>
POST /api/v1/admin/check-defs                   → 201 CheckDefResponse
PUT  /api/v1/admin/check-defs/{defId}           → 200 CheckDefResponse
DELETE /api/v1/admin/check-defs/{defId}         → 204
POST /api/v1/admin/check-runs/trigger            → 202 {runId: UUID}
GET  /api/v1/admin/check-runs                   → List<CheckRunResponse>
GET  /api/v1/admin/check-runs/{runId}           → CheckRunDetailResponse
GET  /api/v1/admin/check-runs/{runId}/results   → List<CheckResultResponse>
GET  /api/v1/admin/allowed-models               → List<{modelId, displayName}>
```

TypeScript types needed (add to frontend/types/api.ts):
```typescript
export type GroundingStatus = 'GROUNDED' | 'LIMITED_GROUNDING' | 'REFUSED';
export type CheckRunStatus = 'RUNNING' | 'COMPLETED' | 'FAILED';

export interface ChatLogListItem {
  id: string;
  conversationId: string | null;
  question: string;
  groundingStatus: GroundingStatus;
  promptTokens: number;
  completionTokens: number;
  responseTime: number;
  createdDate: string;
}

export interface ChatLogDetail extends ChatLogListItem {
  answer: string;
  sources: string | null;
}

export interface ChatLogPage {
  content: ChatLogListItem[];
  totalElements: number;
  totalPages: number;
  number: number;
}

export interface CheckDef {
  id: string;
  question: string;
  referenceAnswer: string;
  category: string | null;
  active: boolean;
  createdAt: string;
}

export interface CheckRun {
  id: string;
  status: CheckRunStatus;
  averageScore: number | null;
  parameterSetName: string | null;
  checkCount: number | null;
  createdDate: string;
}

export interface CheckResult {
  id: string;
  question: string;
  referenceAnswer: string;
  actualAnswer: string | null;
  score: number | null;
  log: string | null;
  createdDate: string;
}

export interface AllowedModel {
  modelId: string;
  displayName: string;
}
```

UI-SPEC badge classes (from 05-UI-SPEC.md):
```
GROUNDED       → bg-green-100 text-green-800 border-green-200
LIMITED_GROUNDING → bg-yellow-100 text-yellow-800 border-yellow-200
REFUSED        → bg-red-100 text-red-800 border-red-200
score >= 0.8   → bg-green-100 text-green-800 border-green-200
score 0.5–0.79 → bg-yellow-100 text-yellow-800 border-yellow-200
score < 0.5    → bg-red-100 text-red-800 border-red-200
```

Sidebar nav entries to add (in layout.tsx):
- "Lịch sử hội thoại" icon: MessageSquare href: /chat-logs
- "Kiểm tra chất lượng" icon: ClipboardCheck href: /checks
- "Lịch sử chạy" icon: History href: /checks/runs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, API clients, hooks, and Chat Log screens (list + detail)</name>
  <files>
    frontend/types/api.ts
    frontend/lib/api/chat-logs.ts
    frontend/hooks/use-chat-logs.ts
    frontend/app/(admin)/chat-logs/page.tsx
    frontend/app/(admin)/chat-logs/[id]/page.tsx
    frontend/app/(admin)/layout.tsx
  </files>
  <read_first>
    - frontend/types/api.ts (existing types — append new types, do not overwrite)
    - frontend/lib/api/parameters.ts (fetch wrapper pattern to replicate for chat-logs.ts)
    - frontend/hooks/use-parameters.ts (TanStack Query hook pattern to replicate)
    - frontend/app/(admin)/sources/page.tsx (DataTable + filter bar page structure to follow exactly)
    - frontend/app/(admin)/trust-policy/page.tsx (semantic badge pattern, active item highlight)
    - frontend/app/(admin)/layout.tsx (existing sidebar nav — add new entries here)
    - frontend/components/admin/data-table.tsx (DataTable component interface)
    - frontend/components/layout/query-boundary.tsx (QueryBoundary usage pattern)
    - .planning/phases/05-quality-operations-evaluation/05-UI-SPEC.md (Screen 1 and Screen 2 specs in full detail)
  </read_first>
  <action>
Step 1 — Extend frontend/types/api.ts:
Append the TypeScript types listed in the interfaces section above (ChatLogListItem, ChatLogDetail, ChatLogPage, GroundingStatus, CheckDef, CheckRun, CheckResult, CheckRunStatus, AllowedModel). Do NOT remove existing types.

Step 2 — Create frontend/lib/api/chat-logs.ts:
```typescript
import { apiClient } from './client';
import type { ChatLogPage, ChatLogDetail, GroundingStatus } from '@/types/api';

export interface ChatLogFilters {
  groundingStatus?: GroundingStatus;
  from?: string;       // ISO datetime string
  to?: string;         // ISO datetime string
  q?: string;          // question text search
  page?: number;
  size?: number;
}

export async function getChatLogs(filters: ChatLogFilters = {}): Promise<ChatLogPage> {
  const params = new URLSearchParams();
  if (filters.groundingStatus) params.set('groundingStatus', filters.groundingStatus);
  if (filters.from) params.set('from', filters.from);
  if (filters.to) params.set('to', filters.to);
  if (filters.q) params.set('q', filters.q);
  if (filters.page !== undefined) params.set('page', String(filters.page));
  if (filters.size !== undefined) params.set('size', String(filters.size));
  params.set('sort', 'createdDate,desc');
  return apiClient(`/api/v1/admin/chat-logs?${params.toString()}`);
}

export async function getChatLogById(id: string): Promise<ChatLogDetail> {
  return apiClient(`/api/v1/admin/chat-logs/${id}`);
}
```

Step 3 — Create frontend/hooks/use-chat-logs.ts:
```typescript
import { useQuery } from '@tanstack/react-query';
import { getChatLogs, getChatLogById, type ChatLogFilters } from '@/lib/api/chat-logs';

export function useChatLogs(filters: ChatLogFilters = {}) {
  return useQuery({
    queryKey: ['chat-logs', filters],
    queryFn: () => getChatLogs(filters),
  });
}

export function useChatLogDetail(id: string) {
  return useQuery({
    queryKey: ['chat-logs', id],
    queryFn: () => getChatLogById(id),
    enabled: !!id,
  });
}
```

Step 4 — Create frontend/app/(admin)/chat-logs/page.tsx (Screen 1 per UI-SPEC):
- Container: `<div className="space-y-4 p-6">`
- Heading: `<h1 className="text-xl font-semibold">Lịch sử hội thoại</h1>`
- Filter bar: `<div className="bg-muted flex items-center gap-3 rounded-md border px-4 py-2">`
  - Date range: two `<Input type="date">` with `<Label>` "Từ ngày" / "Đến ngày" — stored in useState, passed to useChatLogs filters
  - Grounding status: `<Select>` with options: all value="" "Tất cả", "GROUNDED", "LIMITED_GROUNDING", "REFUSED"
  - Text search: `<Input type="text" placeholder="Tìm kiếm theo câu hỏi...">` — debounced 300ms before updating filter state (use a local debounced state)
- DataTable columns (per UI-SPEC Screen 1):
  - "Ngày tạo" 160px — createdDate formatted with date-fns format(parseISO(createdDate), 'dd/MM/yyyy HH:mm')
  - "Câu hỏi" auto — question truncated with className "truncate max-w-xs"
  - "Trạng thái" 140px — groundingStatus as Badge variant="outline" with semantic color class:
    - GROUNDED: bg-green-100 text-green-800 border-green-200
    - LIMITED_GROUNDING: bg-yellow-100 text-yellow-800 border-yellow-200
    - REFUSED: bg-red-100 text-red-800 border-red-200
    Display text (per UI-SPEC copywriting): "Đã dẫn nguồn" / "Dẫn nguồn hạn chế" / "Từ chối trả lời"
  - "Token (P+C)" 120px — `P: {promptTokens} / C: {completionTokens}`
  - "Thời gian (ms)" 120px — responseTime value
- Row click: useRouter().push(`/chat-logs/${row.id}`)
- Pagination: page/totalPages state, "Trang {n} / {total} ({count} bản ghi)" with Trước/Sau buttons
- Empty state: heading "Chưa có lịch sử hội thoại", body "Các cuộc hội thoại sẽ xuất hiện ở đây khi người dùng bắt đầu trò chuyện."
- Wrap DataTable in `<QueryBoundary>` — shows skeleton 8 rows of h-12 while loading
- Error state text: "Không thể tải dữ liệu. Vui lòng thử lại."

Step 5 — Create frontend/app/(admin)/chat-logs/[id]/page.tsx (Screen 2 per UI-SPEC):
- Container: `<div className="p-6 space-y-6 max-w-4xl">`
- Back nav: `<Button variant="ghost" size="sm"><ChevronLeft size={16}/> Quay lại danh sách</Button>` onClick: router.back()
- Header row: createdDate (text-sm text-muted-foreground) + groundingStatus Badge + conversationId label (text-xs text-muted-foreground)
- Question `<Card>`: heading "Câu hỏi" (text-sm font-medium text-muted-foreground), question text (text-sm)
- Answer `<Card>`: heading "Câu trả lời" (text-sm font-medium text-muted-foreground), answer text (text-sm whitespace-pre-wrap)
- Citations `<Card>`: heading "Nguồn trích dẫn", sources rendered as `<ul>` list items; if sources null/empty: "Không có nguồn trích dẫn" (text-sm text-muted-foreground)
  Sources field is a comma-separated string — split on ", " and render each as `<li>`.
- Metadata `<dl>` grid 4 items: "Prompt tokens", "Completion tokens", "Tổng tokens" (sum), "Thời gian phản hồi" "{responseTime} ms"
  All labels: text-xs text-muted-foreground; all values: text-sm font-medium
- No edit/delete buttons (append-only per D-04)
- Use useChatLogDetail(params.id) from use-chat-logs.ts
- Loading: show skeleton while query is loading; 404: show "Không tìm thấy bản ghi" with back button

Step 6 — Update frontend/app/(admin)/layout.tsx:
Read the existing layout.tsx file first. Add 3 nav items to the sidebar nav items array:
- { title: "Lịch sử hội thoại", href: "/chat-logs", icon: MessageSquare }
- { title: "Kiểm tra chất lượng", href: "/checks", icon: ClipboardCheck }
- { title: "Lịch sử chạy", href: "/checks/runs", icon: History }
Import MessageSquare, ClipboardCheck, History from lucide-react.
Match exactly the pattern used for existing nav items (title, href, icon shape).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/types/api.ts contains "GroundingStatus" and "groundingStatus" and "ChatLogListItem" and "CheckRun"
    - frontend/app/(admin)/chat-logs/page.tsx exists and contains "Lịch sử hội thoại" and "groundingStatus" and "refetchInterval" is NOT present (only check-runs page polls)
    - frontend/app/(admin)/chat-logs/page.tsx contains "Chưa có lịch sử hội thoại" (empty state copy)
    - frontend/app/(admin)/chat-logs/[id]/page.tsx exists and contains "Nguồn trích dẫn" and "Quay lại danh sách"
    - frontend/app/(admin)/layout.tsx contains "Lịch sử hội thoại" and "ClipboardCheck"
    - cd frontend && npm run build exits 0 (no TypeScript or compilation errors)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Check Definitions screen (master-detail CRUD) + Check Run History screen</name>
  <files>
    frontend/lib/api/check-defs.ts
    frontend/lib/api/check-runs.ts
    frontend/hooks/use-check-defs.ts
    frontend/hooks/use-check-runs.ts
    frontend/app/(admin)/checks/page.tsx
    frontend/app/(admin)/checks/runs/page.tsx
  </files>
  <read_first>
    - frontend/app/(admin)/parameters/page.tsx (master-detail layout: w-64 left + flex-1 right, react-hook-form + zod, AlertDialog, form footer pattern)
    - frontend/app/(admin)/trust-policy/page.tsx (same master-detail pattern, active item highlight: bg-primary/10 border-l-2 border-l-primary)
    - frontend/lib/api/parameters.ts (API client pattern to replicate for check-defs and check-runs)
    - frontend/hooks/use-parameters.ts (useMutation + useQuery pattern with queryClient.invalidateQueries)
    - frontend/components/admin/data-table.tsx (for check run history DataTable)
    - .planning/phases/05-quality-operations-evaluation/05-UI-SPEC.md (Screen 3: Check Definitions and Screen 4: Check Run History — read both in full)
  </read_first>
  <action>
Step 1 — Create frontend/lib/api/check-defs.ts:
```typescript
import { apiClient } from './client';
import type { CheckDef } from '@/types/api';

export interface CheckDefPayload {
  question: string;
  referenceAnswer: string;
  category?: string;
  active?: boolean;
}

export async function getCheckDefs(): Promise<CheckDef[]> {
  return apiClient('/api/v1/admin/check-defs');
}
export async function createCheckDef(payload: CheckDefPayload): Promise<CheckDef> {
  return apiClient('/api/v1/admin/check-defs', { method: 'POST', body: JSON.stringify(payload) });
}
export async function updateCheckDef(id: string, payload: CheckDefPayload): Promise<CheckDef> {
  return apiClient(`/api/v1/admin/check-defs/${id}`, { method: 'PUT', body: JSON.stringify(payload) });
}
export async function deleteCheckDef(id: string): Promise<void> {
  await apiClient(`/api/v1/admin/check-defs/${id}`, { method: 'DELETE' });
}
```

Step 2 — Create frontend/lib/api/check-runs.ts:
```typescript
import { apiClient } from './client';
import type { CheckRun, CheckResult } from '@/types/api';

export async function getCheckRuns(): Promise<CheckRun[]> {
  return apiClient('/api/v1/admin/check-runs');
}
export async function triggerCheckRun(): Promise<{ runId: string }> {
  return apiClient('/api/v1/admin/check-runs/trigger', { method: 'POST' });
}
export async function getCheckRunById(id: string): Promise<CheckRun> {
  return apiClient(`/api/v1/admin/check-runs/${id}`);
}
export async function getCheckRunResults(runId: string): Promise<CheckResult[]> {
  return apiClient(`/api/v1/admin/check-runs/${runId}/results`);
}
```

Step 3 — Create frontend/hooks/use-check-defs.ts:
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { getCheckDefs, createCheckDef, updateCheckDef, deleteCheckDef, type CheckDefPayload } from '@/lib/api/check-defs';

export function useCheckDefs() {
  return useQuery({ queryKey: ['check-defs'], queryFn: getCheckDefs });
}

export function useCreateCheckDef() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: createCheckDef,
    onSuccess: () => qc.invalidateQueries({ queryKey: ['check-defs'] }),
  });
}

export function useUpdateCheckDef() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ id, payload }: { id: string; payload: CheckDefPayload }) => updateCheckDef(id, payload),
    onSuccess: () => qc.invalidateQueries({ queryKey: ['check-defs'] }),
  });
}

export function useDeleteCheckDef() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: deleteCheckDef,
    onSuccess: () => qc.invalidateQueries({ queryKey: ['check-defs'] }),
  });
}
```

Step 4 — Create frontend/hooks/use-check-runs.ts:
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { getCheckRuns, triggerCheckRun, getCheckRunById, getCheckRunResults } from '@/lib/api/check-runs';
import type { CheckRun } from '@/types/api';

export function useCheckRuns() {
  return useQuery({
    queryKey: ['check-runs'],
    queryFn: getCheckRuns,
    refetchInterval: (query) => {
      const runs: CheckRun[] | undefined = query.state.data;
      if (runs?.some(r => r.status === 'RUNNING')) return 5000;
      return false;
    },
  });
}

export function useTriggerCheckRun() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: triggerCheckRun,
    onSuccess: () => qc.invalidateQueries({ queryKey: ['check-runs'] }),
  });
}

export function useCheckRunDetail(id: string) {
  return useQuery({ queryKey: ['check-runs', id], queryFn: () => getCheckRunById(id), enabled: !!id });
}

export function useCheckRunResults(runId: string) {
  return useQuery({ queryKey: ['check-runs', runId, 'results'], queryFn: () => getCheckRunResults(runId), enabled: !!runId });
}
```

Step 5 — Create frontend/app/(admin)/checks/page.tsx (Screen 3: Check Definitions per UI-SPEC):

Master-detail layout identical to parameters/page.tsx. Container: `<div className="p-6 flex flex-col gap-4 h-screen">`.

Header row (flex justify-between items-center):
- Left: `<h1 className="text-xl font-semibold">Định nghĩa kiểm tra</h1>`
- Right: two buttons side by side:
  - `<Button variant="outline" disabled={!hasActiveDefs || triggerMutation.isPending} onClick={handleTrigger}>` with Play icon — "Chạy kiểm tra" (shows "Đang chạy..." while pending)
  - `<Button onClick={handleNew}>+ Tạo mới</Button>`

Left panel (w-64, border-r, overflow-y-auto):
- List of checkDefs from useCheckDefs()
- Each item: `<div className="p-3 border-b cursor-pointer hover:bg-muted" ...>` — selected: add "bg-primary/10 border-l-2 border-l-primary"
  - question text (text-sm font-medium, truncate)
  - category label (text-xs text-muted-foreground)
  - active badge: Badge variant="outline" — "Đang hoạt động" (shown only if active=true) or "Tắt" (if false)
- Empty state: "Chưa có định nghĩa kiểm tra" (text-sm text-muted-foreground text-center p-4)

Right panel (flex-1):
- When nothing selected: `<div className="flex items-center justify-center h-full text-sm text-muted-foreground">Chọn định nghĩa để chỉnh sửa hoặc nhấn '+ Tạo mới'</div>`
- When selected or creating: Form with react-hook-form + zod schema:
  ```typescript
  const schema = z.object({
    question: z.string().min(10, 'Tối thiểu 10 ký tự'),
    referenceAnswer: z.string().min(10, 'Tối thiểu 10 ký tự'),
    category: z.string().optional(),
    active: z.boolean(),
  });
  ```
  Fields:
  - "Câu hỏi": `<Textarea rows={3}>` — required, min 10 chars
  - "Câu trả lời tham chiếu": `<Textarea rows={4}>` — required, min 10 chars
  - "Danh mục": `<Input>` — optional
  - "Đang hoạt động": `<Checkbox>` + `<Label>` inline (flex items-center gap-2), default true

  Form footer (mt-auto flex gap-2 justify-between):
  - Left: delete button (only when editing) — `<Button variant="outline" className="text-destructive border-destructive hover:bg-destructive/10" onClick={() => setDeleteOpen(true)}>Xóa</Button>`
  - Right: "Giữ lại" (outline, onClick: handleCancel) + "Tạo mới"/"Lưu thay đổi" (primary, type submit)

Delete `<AlertDialog>`:
- Title: "Xác nhận xóa"
- Body: `Xóa định nghĩa kiểm tra '${question.slice(0,40)}'? Dữ liệu sẽ bị xóa vĩnh viễn.`
- Cancel: "Giữ lại" / Confirm: "Xóa" (destructive)

handleTrigger: call triggerMutation.mutate(), then on success navigate to /checks/runs.

hasActiveDefs: checkDefs.filter(d => d.active).length > 0

Step 6 — Create frontend/app/(admin)/checks/runs/page.tsx (Screen 4: Check Run History per UI-SPEC):

Container: `<div className="space-y-4 p-6">`.
Heading: `<h1 className="text-xl font-semibold">Lịch sử chạy kiểm tra</h1>`.
No primary CTA.

DataTable columns:
- "Ngày chạy" 160px — createdDate formatted dd/MM/yyyy HH:mm
- "Điểm trung bình" 100px — averageScore as percentage "87%" — Badge variant="outline" with score color: >= 0.8 green, 0.5–0.79 yellow, < 0.5 red; null shows "—"
- "Bộ tham số" auto — parameterSetName (text-sm)
- "Số kiểm tra" 100px — checkCount
- "Trạng thái" 120px — status Badge: RUNNING=yellow, COMPLETED=green, FAILED=red

Row click: router.push(`/checks/runs/${row.id}`)

Auto-poll: useCheckRuns() already has refetchInterval logic (5000ms while any RUNNING).

Empty state: heading "Chưa có lần chạy kiểm tra", body "Nhấn 'Chạy kiểm tra' trong màn hình Định nghĩa kiểm tra để bắt đầu lần chạy đầu tiên."

No pagination needed (no controls for v1).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/(admin)/checks/page.tsx exists and contains "Định nghĩa kiểm tra" and "Chạy kiểm tra" and "AlertDialog"
    - frontend/app/(admin)/checks/page.tsx contains "triggerMutation" or "useTriggerCheckRun" call
    - frontend/app/(admin)/checks/runs/page.tsx exists and contains "refetchInterval" or "useCheckRuns" (hook has poll logic)
    - frontend/app/(admin)/checks/runs/page.tsx contains "Lịch sử chạy kiểm tra" and "Chưa có lần chạy kiểm tra"
    - frontend/hooks/use-check-runs.ts contains "refetchInterval" and "RUNNING"
    - cd frontend && npm run build exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Check Run Detail screen + AiParameterSet model selection extension</name>
  <files>
    frontend/app/(admin)/checks/runs/[id]/page.tsx
    frontend/app/(admin)/parameters/page.tsx
  </files>
  <read_first>
    - frontend/app/(admin)/checks/runs/[id]/page.tsx (create new — does not exist yet)
    - frontend/app/(admin)/parameters/page.tsx (MUST read current implementation before modifying — add new fields below existing ones)
    - frontend/hooks/use-check-runs.ts (useCheckRunDetail + useCheckRunResults hooks just created in Task 2)
    - frontend/hooks/use-parameters.ts (how existing parameters page uses hooks — extend, do not break)
    - frontend/lib/api/parameters.ts (existing API client — check if updateParameter includes new model fields or needs updating)
    - frontend/components/admin/data-table.tsx (DataTable to reuse for per-check results table in run detail)
    - .planning/phases/05-quality-operations-evaluation/05-UI-SPEC.md (Screen 5: Check Run Detail and Screen 6: AiParameterSet Extension — read both in full)
    - frontend/types/api.ts (AllowedModel type just added in Task 1)
  </read_first>
  <action>
Step 1 — Create frontend/app/(admin)/checks/runs/[id]/page.tsx (Screen 5 per UI-SPEC):

Container: `<div className="p-6 space-y-6">`.
Back nav: `<Button variant="ghost" size="sm"><ChevronLeft size={16}/> Quay lại danh sách</Button>` onClick: router.back().

Run summary `<Card className="p-4">`:
4-item grid (grid grid-cols-2 gap-4 or grid-cols-4):
- "Ngày chạy": formatted datetime
- "Điểm trung bình": averageScore badge (same score color logic as run history list)
- "Bộ tham số": parameterSetName
- "Số kiểm tra": checkCount

Per-check results DataTable:
Columns:
- "Câu hỏi" 200px — question truncated
- "Câu trả lời tham chiếu" 200px — referenceAnswer truncated
- "Câu trả lời thực tế" 200px — actualAnswer truncated (show "—" if null)
- "Điểm" 100px — score (0.00–1.00) as Badge with semantic color; null shows "—"

Row click: open `<Sheet>` side drawer on the right:
- Sheet title: question text (truncated to 60 chars + "...")
- Sheet content with `<ScrollArea className="h-full">`:
  - `<Card className="p-4 mb-3">` — heading "Câu hỏi" (text-xs text-muted-foreground), body: full question
  - `<Card className="p-4 mb-3">` — heading "Câu trả lời tham chiếu", body: full referenceAnswer
  - `<Card className="p-4 mb-3">` — heading "Câu trả lời thực tế", body: full actualAnswer or "Không có câu trả lời"
- Selected row state: useState<CheckResult | null>(null); Sheet open={selectedResult !== null} onOpenChange sets to null

Use useCheckRunDetail(params.id) for run summary card.
Use useCheckRunResults(params.id) for the DataTable.
Loading state: skeleton rows while either query is loading.

Step 2 — Extend frontend/app/(admin)/parameters/page.tsx:

IMPORTANT: Read the entire current parameters/page.tsx before making any changes. Only add new fields — do not change existing form fields, list panel, header, or action buttons.

Changes required per UI-SPEC Screen 6:
a. Add AllowedModel fetch:
   In the component or in a new hook addition to use-parameters.ts, fetch allowed models:
   ```typescript
   const { data: allowedModels = [] } = useQuery({
     queryKey: ['allowed-models'],
     queryFn: () => fetch('/api/v1/admin/allowed-models').then(r => r.json()),
   });
   ```

b. Extend the form zod schema to include:
   ```typescript
   chatModel: z.string().optional(),
   evaluatorModel: z.string().optional(),
   ```

c. Extend the form defaultValues to include:
   ```typescript
   chatModel: selected?.chatModel ?? '',
   evaluatorModel: selected?.evaluatorModel ?? '',
   ```

d. Add to the form, below the YAML textarea and above the form footer:
   ```tsx
   <Separator className="my-2" />
   <p className="text-xs font-medium text-muted-foreground uppercase tracking-wide">Cấu hình mô hình</p>

   <div className="space-y-1">
     <Label>Mô hình trò chuyện</Label>
     <Controller
       name="chatModel"
       control={control}
       render={({ field }) => (
         <Select value={field.value ?? ''} onValueChange={field.onChange}>
           <SelectTrigger><SelectValue placeholder="Chọn mô hình" /></SelectTrigger>
           <SelectContent>
             {allowedModels.map(m => (
               <SelectItem key={m.modelId} value={m.modelId}>{m.displayName}</SelectItem>
             ))}
           </SelectContent>
         </Select>
       )}
     />
     {errors.chatModel && <p className="text-xs text-destructive">{errors.chatModel.message}</p>}
   </div>

   <div className="space-y-1">
     <Label>Mô hình đánh giá</Label>
     <Controller
       name="evaluatorModel"
       control={control}
       render={({ field }) => (
         <Select value={field.value ?? ''} onValueChange={field.onChange}>
           <SelectTrigger><SelectValue placeholder="Chọn mô hình" /></SelectTrigger>
           <SelectContent>
             {allowedModels.map(m => (
               <SelectItem key={m.modelId} value={m.modelId}>{m.displayName}</SelectItem>
             ))}
           </SelectContent>
         </Select>
       )}
     />
     {errors.evaluatorModel && <p className="text-xs text-destructive">{errors.evaluatorModel.message}</p>}
   </div>
   ```

e. Ensure the form submit handler passes chatModel and evaluatorModel fields to the update API call. Verify that the existing parameters.ts API client update function or similar includes these fields in the PUT body (update the API call if needed).

f. Ensure the AiParameterSet type in api.ts includes chatModel and evaluatorModel optional string fields:
   ```typescript
   // If AiParameterSet type exists in api.ts, add:
   chatModel?: string;
   evaluatorModel?: string;
   ```

Step 3 — Frontend build verification:
Run `cd frontend && npm run build` and fix any TypeScript errors before marking complete.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/(admin)/checks/runs/[id]/page.tsx exists and contains "Sheet" and "ScrollArea" and "actualAnswer"
    - frontend/app/(admin)/checks/runs/[id]/page.tsx contains "Câu trả lời tham chiếu" and "Câu trả lời thực tế"
    - frontend/app/(admin)/parameters/page.tsx contains "Mô hình trò chuyện" and "Mô hình đánh giá" and "Cấu hình mô hình"
    - frontend/app/(admin)/parameters/page.tsx contains "allowedModels" and "SelectItem"
    - frontend/app/(admin)/parameters/page.tsx contains "Separator" import
    - cd frontend && npm run build exits 0 (zero TypeScript errors, zero compilation errors)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All 6 Phase 5 admin UI screens:
1. /chat-logs — DataTable with date range, grounding status filter, question search
2. /chat-logs/[id] — read-only log detail with question, answer, citations, tokens, response time
3. /checks — master-detail CheckDef CRUD with "Chạy kiểm tra" trigger button
4. /checks/runs — run history DataTable with auto-poll while RUNNING
5. /checks/runs/[id] — run summary card + per-check results table with Sheet row expansion
6. /parameters — extended with chatModel + evaluatorModel Select dropdowns

Backend from Plans 01+02: chat log capture, ChatLog REST, CheckDef CRUD, CheckRunner async, LlmSemanticEvaluator, CheckRun REST, AllowedModel endpoint.
  </what-built>
  <how-to-verify>
Start the backend with `./mvnw spring-boot:run`, then `cd frontend && npm run dev`.

1. CHAT LOG CAPTURE:
   a. Ask a question via the chat UI or POST to /api/v1/chat — confirm GET /api/v1/admin/chat-logs returns at least one entry
   b. Open /chat-logs in the admin UI — verify the log row appears with date, truncated question, grounding status badge, token counts
   c. Click the row — verify detail view shows full question, answer, citations panel (or "Không có nguồn"), metadata row with token counts
   d. Test grounding status filter: select "REFUSED" — verify only refused answers show

2. CHECK DEFINITIONS:
   a. Open /checks — verify "Chưa có định nghĩa kiểm tra" empty state
   b. Click "+ Tạo mới" — fill in question (10+ chars), referenceAnswer (10+ chars), category, check active checkbox — save
   c. Verify item appears in left panel with "Đang hoạt động" badge
   d. Click item — edit question — "Lưu thay đổi" — verify updated text in panel
   e. Click "Xóa" — AlertDialog appears — confirm — item disappears

3. CHECK RUNS:
   a. Create at least 1 active CheckDef
   b. Click "Chạy kiểm tra" — button shows "Đang chạy..." briefly, then navigates to /checks/runs
   c. Verify run appears with status RUNNING, then transitions to COMPLETED (auto-poll every 5s)
   d. Click run row — verify summary card shows date, average score badge, parameter set name
   e. Verify per-check results table shows question, reference answer, actual answer, score badge
   f. Click a result row — Sheet opens with full text for all 3 fields

4. PARAMETERS EXTENSION:
   a. Open /parameters — select any parameter set
   b. Verify "Cấu hình mô hình" section appears below YAML textarea with Separator
   c. Verify "Mô hình trò chuyện" and "Mô hình đánh giá" dropdowns appear with Claude model options
   d. Select a model in each dropdown — save — reload — verify selections persisted
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 verification areas pass, or describe which specific behaviors failed for gap closure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js admin screens | Client renders sensitive chat content (user questions, full answers) — no auth gate in v1 |
| Next.js client → Spring REST | Direct API calls to /api/v1/admin/* — CORS configured; no token auth in v1 |
| parameters page → /allowed-models | Dropdown model list loaded from backend enum — no user input, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Information Disclosure | /chat-logs admin screen exposes all user questions and answers | accept | Admin-only UI; no auth in v1; same data available via REST; scoped to internal admin use |
| T-05-12 | Tampering | CheckDef form — question field content is sent as-is to ChatService.answer() | accept | Same input surface as end-user chat; existing input handling in ChatService applies |
| T-05-13 | Denial of Service | "Chạy kiểm tra" button — rapid clicking could trigger multiple concurrent runs | mitigate | Button disabled while triggerMutation.isPending; server-side only concern beyond that |
| T-05-14 | Spoofing | No CSRF protection on POST /check-runs/trigger | accept | No auth in v1; CSRF only meaningful with session cookies which this API does not use |
| T-05-15 | Information Disclosure | Check run detail exposes full reference answers and LLM evaluator rationale | accept | Admin-only screen; reference answers authored by admin; appropriate access level |
</threat_model>

<verification>
After all 4 tasks complete (including human checkpoint approval):
1. `cd frontend && npm run build` exits 0 — zero TypeScript errors
2. All 6 screen files exist at their specified paths
3. frontend/app/(admin)/layout.tsx contains all 3 new nav entries
4. Human checkpoint approved — all 4 functional verification areas pass
5. `./mvnw test -q` still exits 0 — no backend regressions from frontend changes
</verification>

<success_criteria>
1. Admins can browse, filter, and search chat logs in the admin UI (ADMIN-04)
2. Admins can click any log row to view full question, answer, citations, and token metadata (ADMIN-04)
3. Admins can create, edit, delete, and toggle check definitions in a master-detail form (ADMIN-05)
4. "Chạy kiểm tra" triggers an async run and run history auto-polls until COMPLETED (ADMIN-05)
5. Check run detail shows per-check results with Sheet expansion for full text review (ADMIN-05)
6. Parameters admin screen exposes chatModel and evaluatorModel dropdowns loaded from AllowedModel enum (D-14, D-15)
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-operations-evaluation/05-03-SUMMARY.md`
</output>
