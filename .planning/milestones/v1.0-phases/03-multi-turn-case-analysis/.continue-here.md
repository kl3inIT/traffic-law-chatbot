---
context: phase
phase: 03-multi-turn-case-analysis
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-04-10T14:57:44.164Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Launch from Windows toolchain path — Starting the app through bash/WSL caused Java 25 toolchain detection failures and wrapper path issues — use `gradlew.bat` from Windows with `JAVA_HOME=C:/Users/admin/.jdks/temurin-25.0.2`.
- [ ] CONSTRAINT: Do not plain-source `.env` — `D:/ai/traffic-law-chatbot/.env` contains an extra trailing invalid line (`to`) that breaks shell sourcing — parse only valid `KEY=value` lines or inject env vars directly.
- [ ] CONSTRAINT: UTF-8 verification must use explicit UTF-8 request bytes — naïve Windows/curl/Python invocations can still send bad bytes from the client side even though the server-side fix is in — use `Content-Type: application/json; charset=utf-8` and explicit UTF-8 encoding for live checks.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| WSL toolchain launch | Launching Gradle through bash/WSL could not find the Windows Java 25 installation and blocked boot | blocking | Launch via Windows `gradlew.bat` with explicit `JAVA_HOME` set to the Temurin 25 path |
| Plain `.env` sourcing | `source .env` failed because the file has an invalid trailing line | blocking | Parse only valid `KEY=value` lines or inject env vars directly |
| Ambiguous client encoding during live checks | Vietnamese payloads failed or the local client itself mis-encoded bytes unless UTF-8 was forced explicitly | blocking | Send explicit UTF-8 bytes and `charset=utf-8` for live thread API verification |
| Assuming tests prove live acceptance | Focused tests passed after mapper fixes, but the live CLARIFICATION_NEEDED -> FINAL_ANALYSIS acceptance gate still failed | advisory | Re-run the actual HTTP workflow after every fix; do not stop at green unit/integration tests |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 3 implementation remains merged on `main`. This session fixed the UTF-8 transport bug for Vietnamese thread requests and fixed a real mapper bug in `ChatThreadMapper`, but the live acceptance gate is still not satisfied. The live workflow now reaches `CLARIFICATION_NEEDED` correctly for an ambiguous first turn, but after the missing fact reply it still falls back to `SCENARIO_ANALYSIS` with `scenarioAnalysis=null`.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Phase 3 Wave 0/01 thread foundation and persistence scaffolding - Done
- Task 2: Explicit fact memory and clarification policy - Done
- Task 3: Grounded final scenario composition and serialization lock - Done
- Task 4: Merge Wave 3 to `main` - Done
- Task 5: UTF-8 thread request fix - Done, added servlet UTF-8 config and Vietnamese JSON controller regressions; live Vietnamese thread requests now parse successfully on port 18090
- Task 6: Clarification-to-final-analysis mapper fix - Done, preserved structured scenario fields in `ChatThreadMapper` and tightened focused integration tests; focused tests are green
</completed_work>

<remaining_work>

- Diagnose why the live post-clarification path still returns fallback-style output from `ChatService`/model parsing
- Fix the live `CLARIFICATION_NEEDED -> FINAL_ANALYSIS` behavior so the second response returns populated `scenarioAnalysis`
- Re-run full live Phase 3 verification and update planning/state artifacts if the acceptance gate finally passes
</remaining_work>

<decisions_made>

- Decided to use Windows `gradlew.bat` with explicit Java 25 for all live verification because bash/WSL launch paths were operationally unreliable.
- Added explicit servlet UTF-8 encoding before touching broader request logic because the first reproducible live failure was byte-decoding, not business logic.
- Fixed `ChatThreadMapper` before broadening scope because it was definitely discarding structured scenario fields during thread remapping, even though that alone did not fix the remaining live bug.
</decisions_made>

<blockers>
- Live post-clarification responses still return `LIMITED_GROUNDING` + `SCENARIO_ANALYSIS` + `scenarioAnalysis=null` after the user supplies the missing fact.
- `.env` still contains an invalid trailing line and must not be sourced directly.
- There is an unexpected modified file `src/main/java/com/vn/traffic/chatbot/common/config/AsyncConfig.java` that was not part of this work and should be reviewed before any broader commit.
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-multi-turn-case-analysis/03-03-PLAN.md` — final Phase 3 verification gate and what the live workflow must prove
2. `.planning/HANDOFF.json` — structured paused state for this exact session
3. `.planning/phases/03-multi-turn-case-analysis/03-02-PLAN.md` — clarification/fact-memory invariants to preserve
4. `src/main/java/com/vn/traffic/chatbot/chat/service/ChatThreadMapper.java` — mapper fix applied this session; do not regress it
5. `src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java` — likely next investigation point for fallback drafting / parseDraft behavior
6. `src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java` — prompt contract that shapes model JSON output
7. `src/test/java/com/vn/traffic/chatbot/chat/ChatScenarioAnalysisIntegrationTest.java` — tightened scenario fixture used to validate the mapper fix
8. `src/test/java/com/vn/traffic/chatbot/chat/ChatThreadFlowIntegrationTest.java` — focused thread-flow regression for clarification exit
9. `src/main/resources/application.yaml` — UTF-8 config and current runtime thresholds

## Critical Anti-Patterns (do NOT repeat these)
- Launching from bash/WSL for runtime verification → use Windows `gradlew.bat` with explicit Java 25 `JAVA_HOME`
- Plain `source .env` → parse only valid `KEY=value` lines or inject env vars directly
- Sending Vietnamese live-check payloads without explicit UTF-8 bytes → force UTF-8 in the client and set `charset=utf-8`
- Treating green focused tests as proof of live acceptance → always re-run the actual HTTP thread flow

## Infrastructure State
- Local app was verified on `http://127.0.0.1:18090`
- Health endpoint on `18090` returned `UP`
- Live verified thread flow on `18090`:
  - create with `Tôi vượt đèn đỏ thì bị phạt thế nào?` → `CLARIFICATION_NEEDED`, pending fact = `vehicleType`
  - follow-up with `Tôi đi xe máy.` → still `SCENARIO_ANALYSIS`, `groundingStatus=LIMITED_GROUNDING`, `scenarioAnalysis=null`
- UTF-8 live create/post now work when using explicit UTF-8 encoded request bodies

## Pre-Execution Critique Required
- Design artifact: N/A
- Critique focus: N/A
- Gate: N/A

<context>
The key mental model shift from this session: the problem is no longer transport or thread remapping. The remaining live bug sits in the answer-generation path after clarification. Focus on real model output, `parseDraft(...)`, and fallback-draft behavior in `ChatService`, plus whether `ScenarioAnswerComposer` is being handed enough structured information on real live responses. Do not reopen broad architecture work.
</context>

<next_action>
Start with: boot or confirm the app on port 18090 using Windows `gradlew.bat`, then reproduce the clarified thread scenario with explicit UTF-8 request bytes and inspect `ChatService` / `ChatPromptFactory` behavior to learn why the follow-up still produces fallback-style LIMITED_GROUNDING output instead of a structured final scenario.
</next_action>
