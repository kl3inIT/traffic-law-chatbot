# Phase 06.1 Security Audit

**Phase:** 06.1 -- Multi-Provider AI Model Selection
**ASVS Level:** 1
**Audit Date:** 2026-04-15
**Auditor:** GSD Security Auditor (automated)

## Threat Verification Summary

**Threats Closed:** 8/8
**Threats Open:** 0/8

## Threat Register

| Threat ID | Category | Component | Disposition | Status | Evidence |
|-----------|----------|-----------|-------------|--------|----------|
| T-06.1-01 | Tampering | ChatService.resolveClient() | mitigate | CLOSED | `ChatService.java:202-221` -- modelId used only as `chatClientMap.containsKey()` / `chatClientMap.get()` lookup. Unknown keys trigger `log.warn` at line 207 and fallback to default model. Never interpolated into SQL, shell, or prompt. |
| T-06.1-02 | Information Disclosure | GET /api/v1/admin/allowed-models | accept | CLOSED | Endpoint is admin-prefixed (`/api/v1/admin/allowed-models`) consistent with all other admin endpoints. Exposing available model IDs is intentional and low-risk. Accepted risk. |
| T-06.1-03 | Information Disclosure | application.yaml: OPENAI_API_KEY | mitigate | CLOSED | `application.yaml:40,43` -- API key referenced via `${OPENAI_API_KEY:none}` env var substitution. No hardcoded key value. No log statements reference API key (grep confirmed zero matches for `log.*(api.?key\|OPENAI)` in Java source). |
| T-06.1-04 | Elevation of Privilege | LlmSemanticEvaluator -> AiParameterSetService.getActive() | accept | CLOSED | `LlmSemanticEvaluator.java:107` -- evaluatorModel read from active param set via `paramProvider.getString("model.evaluatorModel", ...)`. Admin-controlled, not user-controllable. Accepted risk. |
| T-06.1-05 | Denial of Service | Empty chatClientMap at startup | mitigate | CLOSED | `ChatClientConfig.java:90-91` -- `log.warn("ChatClientMap is empty")` at startup if no models configured. `ChatService.java:214-216` -- resolveClient throws `IllegalStateException` with descriptive message if map empty (prevents NPE). Application starts even without 9router. |
| T-06.1-06 | Tampering | Frontend chat request -- modelId field | accept | CLOSED | Frontend populates modelId from allowed-models endpoint. Arbitrary modelId from crafted request hits backend `ChatService.resolveClient()` warn+fallback (line 207). Accepted risk. |
| T-06.1-07 | Information Disclosure | Model list visible in browser network tab | accept | CLOSED | Model IDs and display names are non-sensitive operational information. No credentials or internal configuration exposed. Accepted risk. |
| T-06.1-08 | Spoofing | Context state lost on browser refresh | accept | CLOSED | `app/(chat)/layout.tsx` uses `useState<string | undefined>(undefined)` -- selectedModelId resets to undefined on refresh, treated as "use default". Intentional. Accepted risk. |

## Accepted Risks Log

| Threat ID | Risk Description | Justification |
|-----------|------------------|---------------|
| T-06.1-02 | Admin endpoint exposes model list | Model IDs are non-sensitive; endpoint follows existing admin prefix convention |
| T-06.1-04 | Evaluator model selection is admin-controlled | Parameter sets are admin-only; no user path to override evaluator model |
| T-06.1-06 | User can craft arbitrary modelId in chat request | Backend resolveClient() has warn+fallback; no execution of arbitrary values |
| T-06.1-07 | Model list visible in network tab | Model IDs and display names contain no secrets or internal config |
| T-06.1-08 | Model selection lost on browser refresh | Intentional UX -- undefined means "use server default" |

## Unregistered Flags

None. Both SUMMARY files report no new threat flags beyond the planned threat model.

## Verdict

SECURED -- all 8 threats verified closed.
