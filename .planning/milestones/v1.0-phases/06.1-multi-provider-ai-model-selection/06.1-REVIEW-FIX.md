---
phase: 06.1-multi-provider-ai-model-selection
fixed_at: 2026-04-15T11:00:00Z
review_path: .planning/phases/06.1-multi-provider-ai-model-selection/06.1-REVIEW.md
iteration: 1
findings_in_scope: 3
fixed: 3
skipped: 0
status: all_fixed
---

# Phase 6.1: Code Review Fix Report

**Fixed at:** 2026-04-15T11:00:00Z
**Source review:** .planning/phases/06.1-multi-provider-ai-model-selection/06.1-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 3
- Fixed: 3
- Skipped: 0

## Fixed Issues

### WR-01: NoSuchElementException when chatClientMap is empty

**Files modified:** `src/main/java/com/vn/traffic/chatbot/chat/service/ChatService.java`, `src/main/java/com/vn/traffic/chatbot/checks/evaluator/LlmSemanticEvaluator.java`
**Commit:** 2db9775
**Applied fix:** Added empty-map guard (`chatClientMap.isEmpty()`) before `iterator().next()` in both `ChatService.resolveClient()` and `LlmSemanticEvaluator.resolveEvaluatorClient()`. When the map is empty, an `IllegalStateException` is thrown with a clear diagnostic message instead of the cryptic `NoSuchElementException`.

### WR-02: Stale LIMITED_GROUNDING references in tests

**Files modified:** `src/main/java/com/vn/traffic/chatbot/chat/service/GroundingStatus.java`, `src/test/java/com/vn/traffic/chatbot/chat/service/AnswerComposerTest.java`, `src/test/java/com/vn/traffic/chatbot/chat/service/ScenarioAnswerComposerTest.java`, `src/test/java/com/vn/traffic/chatbot/chat/ChatScenarioAnalysisIntegrationTest.java`, `src/test/java/com/vn/traffic/chatbot/chat/service/ChatServiceTest.java`
**Commit:** 6c95bcc
**Applied fix:** Removed the `LIMITED_GROUNDING` value from the `GroundingStatus` enum since no production code path can produce it after the phase 6.1 changes to `determineGroundingStatus()`. Updated all test files:
- `AnswerComposerTest`: Removed the `composeLimitedGroundingOmitsUnsupportedSectionsAndPopulatesUncertaintyNotice` test (tested dead path with already-broken assertion on `uncertaintyNotice`). Changed `composeStripsDuplicatedConclusionLabel` to use `GROUNDED` since the test validates label normalization, not grounding status.
- `ScenarioAnswerComposerTest`: Changed both tests from `LIMITED_GROUNDING` to `GROUNDED` and renamed to `groundedWithScenarioFactsProducesFinalAnalysis` and `groundedWithoutFactsProducesScenarioAnalysisMode` to reflect actual semantics.
- `ChatScenarioAnalysisIntegrationTest`: Changed `limitedGroundingAnswer()` helper to `groundedAnswerWithScenarioFacts()` using `GROUNDED`, renamed test method accordingly.
- `ChatServiceTest`: Removed `answerReturnsLimitedGroundingForOneOrTwoDocumentsAndUsesLimitedNotice` test (tested unreachable production path). Simplified `standardResponse` helper to use `null` for `uncertaintyNotice` unconditionally.

**Status:** fixed: requires human verification (logic changes across multiple test files)

### WR-03: Radix UI SelectItem with empty string value

**Files modified:** `frontend/app/(admin)/checks/page.tsx`, `frontend/app/(admin)/parameters/page.tsx`
**Commit:** e7431de
**Applied fix:** Replaced empty string `<SelectItem value="">` with sentinel `<SelectItem value="__default__">` in all four Select components (2 in checks page, 2 in parameters page). Updated `value` props to map empty state to `'__default__'` (e.g., `value={chatModelId || '__default__'}`) and `onValueChange` handlers to map sentinel back to empty string (e.g., `v === '__default__' ? '' : v`). The API-facing state remains empty string internally, preserving the existing `|| undefined` conversion at API call sites.

---

_Fixed: 2026-04-15T11:00:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
