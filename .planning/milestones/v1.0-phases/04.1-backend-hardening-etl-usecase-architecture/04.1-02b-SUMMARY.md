---
phase: "4.1"
plan: "04.1-02b"
subsystem: frontend
tags: [trust-policy, admin-ui, tanstack-query, react-hook-form, zod]
dependency_graph:
  requires:
    - "04.1-02"
  provides:
    - trust-policy-admin-ui
  affects:
    - frontend/app/(admin)/trust-policy/page.tsx
    - frontend/hooks/use-trust-policy.ts
    - frontend/lib/api/trust-policy.ts
    - frontend/types/api.ts
    - frontend/lib/query-keys.ts
    - frontend/components/layout/app-sidebar.tsx
tech_stack:
  added: []
  patterns:
    - TanStack Query mutation + invalidation pattern (same as use-parameters.ts)
    - react-hook-form + zodResolver CRUD form pattern (same as parameters/page.tsx)
    - Two-panel list+editor admin page layout
key_files:
  created:
    - frontend/app/(admin)/trust-policy/page.tsx
    - frontend/hooks/use-trust-policy.ts
    - frontend/lib/api/trust-policy.ts
  modified:
    - frontend/types/api.ts
    - frontend/lib/query-keys.ts
    - frontend/components/layout/app-sidebar.tsx
decisions:
  - Used native <select> for trustTier instead of a custom Select component — avoids introducing new shadcn component patterns not present in existing admin pages; consistent with simple form field approach
  - Trust tier badge colors follow semantic meaning: PRIMARY=green (authoritative), SECONDARY=yellow (supplementary), MANUAL_REVIEW=gray (pending judgment)
metrics:
  duration: "~20 minutes"
  completed: "2026-04-11"
  tasks_completed: 1
  tasks_total: 1
  files_changed: 6
requirements:
  - KNOW-03
  - KNOW-04
---

# Phase 4.1 Plan 02b: Trust Policy Admin UI Summary

**One-liner:** Trust policy admin UI with TanStack Query CRUD, tier badges (PRIMARY=green, SECONDARY=yellow, MANUAL_REVIEW=gray), and sidebar wiring to `/trust-policy`.

## What Was Built

Track B (frontend) of the trust policy feature: a complete admin UI screen for managing source trust policies, following the same patterns as the existing `parameters/page.tsx` admin page.

### Files Created

- `frontend/app/(admin)/trust-policy/page.tsx` — Two-panel admin page with list (left) and CRUD form (right). Shows trust tier badges with semantic colors. Uses react-hook-form + zod validation. AlertDialog confirmation for deletes. All UI text in Vietnamese.
- `frontend/hooks/use-trust-policy.ts` — TanStack Query hooks: `useTrustPolicies`, `useCreateTrustPolicy`, `useUpdateTrustPolicy`, `useDeleteTrustPolicy`. Follows exact pattern from `use-parameters.ts`.
- `frontend/lib/api/trust-policy.ts` — API client for `/api/v1/admin/trust-policies` CRUD using `apiGet`, `apiPost`, `apiPut`, `apiDelete` from the existing client.

### Files Modified

- `frontend/types/api.ts` — Added `TrustTier`, `TrustPolicyResponse`, `CreateTrustPolicyRequest`, `UpdateTrustPolicyRequest`
- `frontend/lib/query-keys.ts` — Added `trustPolicies` and `trustPolicy` query keys
- `frontend/components/layout/app-sidebar.tsx` — Added `ShieldCheck` icon import and `{ title: 'Chính sách tin cậy', href: '/trust-policy', icon: ShieldCheck }` to `adminNavItems`

## Task Commits

| Task | Description | Commit |
|------|-------------|--------|
| 04.1-02b-T1 | Trust policy admin UI screen and sidebar wiring | `92f324a` |

## Verification Results

All acceptance criteria passed:

- `frontend/lib/api/trust-policy.ts` — exists
- `frontend/hooks/use-trust-policy.ts` — exists
- `frontend/app/(admin)/trust-policy/page.tsx` — exists
- `grep "TrustTier\|TrustPolicyResponse" frontend/types/api.ts` — 4 matches
- `grep "trustPolicies" frontend/lib/query-keys.ts` — 1 match
- `grep "ShieldCheck\|trust-policy\|Chính sách tin cậy" frontend/components/layout/app-sidebar.tsx` — 2 matches
- `grep "useTrustPolicies\|useCreateTrustPolicy" frontend/app/(admin)/trust-policy/page.tsx` — 4 matches
- `grep "zodResolver\|react-hook-form" frontend/app/(admin)/trust-policy/page.tsx` — 3 matches
- `npm run build` — succeeded, `/trust-policy` listed as static route, zero TypeScript errors

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. The page is fully wired to the REST API via TanStack Query hooks. No placeholder data.

## Threat Flags

No new security surface introduced beyond what was in the plan's threat model. The page is admin-only (under `(admin)` route group), uses React JSX escaping throughout (no `dangerouslySetInnerHTML`), and proxies only to the `/api/v1/admin/trust-policies` endpoint defined in Plan 02.

## Self-Check: PASSED

- FOUND: frontend/lib/api/trust-policy.ts
- FOUND: frontend/hooks/use-trust-policy.ts
- FOUND: frontend/app/(admin)/trust-policy/page.tsx
- FOUND: commit 92f324a
- Build: succeeded with /trust-policy route listed
