---
phase: "4.1"
plan: "04.1-03"
subsystem: configurability-audit
tags: [audit, configurability, clarification-policy, answer-composition-policy, retrieval-policy, chat-service, documentation]
dependency_graph:
  requires:
    - 04.1-01 (AppProperties migration — resolves @Value fields tracked in audit)
    - 04.1-02 (batch ingestion + trust policy — confirms D-13 hardcoded filter rationale)
  provides:
    - 04.1-CONFIGURABILITY-AUDIT.md (gap inventory for ClarificationPolicy, AnswerCompositionPolicy, RetrievalPolicy, ChatService)
  affects:
    - Future phases that parameterize ClarificationPolicy.toPendingFact() question strings
    - Future phases that expose ChatService.containsLegalSignal() keyword list as admin config
tech_stack:
  added: []
  patterns:
    - Configurability audit pattern: classify each hardcoded item by gap level (NONE/LOW/MEDIUM/HIGH/RESOLVED), state rationale, and prescribe action
key_files:
  created:
    - .planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md
  modified: []
decisions:
  - "RetrievalPolicy.RETRIEVAL_FILTER documented as NONE (by design) — permanent hardcode per D-13; exposing as configurable would create a security boundary bypass allowing unapproved/untrusted sources into retrieval"
  - "ClarificationPolicy.toPendingFact() user-facing question strings flagged as MEDIUM — P1 for next phase; proposed mechanism: per-fact-key named AiParameterSet keys with current strings as fallback constants"
  - "ChatService.containsLegalSignal() keyword list flagged as MEDIUM — P2 future phase; proposed mechanism: chat.legalSignalKeywords list in AiParameterSet"
  - "AnswerCompositionPolicy confirmed as zero-gap reference implementation — model for how user-facing strings should be handled elsewhere in the codebase"
  - "ChatService @Value fields (top-k, limited-threshold) documented as RESOLVED migration stubs — AppProperties equivalent exists from Plan 04.1-01; cleanup commit pending"
metrics:
  duration_minutes: 7
  completed_date: "2026-04-11"
  tasks_completed: 1
  files_created: 1
  files_modified: 0
---

# Phase 4.1 Plan 03: Configurability Audit Summary

**One-liner:** Written configurability gap inventory for four policy files — two MEDIUM gaps flagged for future parameterization, RETRIEVAL_FILTER documented as intentionally hardcoded safety gate, AnswerCompositionPolicy confirmed fully configurable.

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| T1 | Read ClarificationPolicy, AnswerCompositionPolicy, RetrievalPolicy, ChatService; produce 04.1-CONFIGURABILITY-AUDIT.md | b0568c7 |

## What Was Built

### 04.1-CONFIGURABILITY-AUDIT.md

A 221-line audit document that inventories every hardcoded string, keyword pattern, and rule found in the four audited files. Structure:

- **Section 1 — Scope:** Lists audited files and audit date (commit base: 8b8e891).
- **Section 2 — Findings Table:** 10-row inventory with gap levels, rationales, and action recommendations.
- **Section 3 — Security Note:** Full rationale for why `RetrievalPolicy.RETRIEVAL_FILTER` must remain hardcoded — the filter enforces three orthogonal trust gates (approvalState, trusted, active) simultaneously; making any condition configurable would allow unvetted content into legal answers.
- **Section 4 — Priority Recommendations:** P1 (toPendingFact), P2 (containsLegalSignal), and Permanent (RETRIEVAL_FILTER).
- **Section 5 — Coverage Summary:** Confirms AnswerCompositionPolicy has zero gaps and is the reference implementation.
- **Appendix — Raw Evidence:** Actual source code excerpts confirming each finding.

### Key Findings

| Item | Gap Level | Finding |
|------|-----------|---------|
| ClarificationPolicy.builtInRequiredFactKeys() keyword triggers | LOW | Fallback-only — bypassed by AiParameterSet caseAnalysis.requiredFacts list |
| ClarificationPolicy.toPendingFact() question/reason strings | MEDIUM | Not configurable at any layer — P1 for parameterization in next phase |
| AnswerCompositionPolicy all strings | NONE | Fully configurable via paramProvider — zero gaps |
| RetrievalPolicy.RETRIEVAL_FILTER | NONE (by design) | Intentionally hardcoded per D-13 — permanent security gate |
| RetrievalPolicy topK + similarityThreshold | NONE | Configurable via paramProvider |
| ChatService.containsLegalSignal() keyword list | MEDIUM | Hardcoded quality heuristic — P2 for future admin config |
| ChatService @Value fields | RESOLVED | Migration artifact from Plan 04.1-01 AppProperties |

## Deviations from Plan

None — plan executed exactly as written. No production code changes. Audit content matches pre-research findings from 04.1-RESEARCH.md with one additional observation: the ChatService `@Value` fields were confirmed as migration stubs (AppProperties equivalent exists but ChatService was not updated to consume it), documented as RESOLVED with a cleanup recommendation.

## Known Stubs

None — this plan produces a documentation artifact only; no code stubs introduced.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes. Audit document is a `.planning/` artifact; not deployed. T-4.1-03-02 mitigated: acceptance criteria explicitly verified that RETRIEVAL_FILTER is documented as hardcoded/safety-critical (grep confirmed 3 matches for "intentionally hardcoded|safety-critical|by design").

## Self-Check: PASSED

- [x] File exists: `.planning/phases/04.1-backend-hardening-etl-usecase-architecture/04.1-CONFIGURABILITY-AUDIT.md` (18376 bytes, 221 lines)
- [x] Task commit b0568c7 verified in git log
- [x] All 8 acceptance criteria passed (grep checks all returned matches; line count 221 >= 80; gap level count 15 >= 5)
