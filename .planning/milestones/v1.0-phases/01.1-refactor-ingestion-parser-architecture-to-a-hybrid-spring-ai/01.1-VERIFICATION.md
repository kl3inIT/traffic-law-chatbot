---
phase: 01.1-refactor-ingestion-parser-architecture-to-a-hybrid-spring-ai
verified: 2026-04-08T04:19:25Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 01.1: Refactor ingestion parser architecture to a hybrid Spring AI approach Verification Report

**Phase Goal:** Fix URL provenance first, then refactor URL HTML ingestion toward a hybrid Spring AI parser path without weakening SSRF safety, provenance guarantees, async job visibility, or retrieval-gating behavior.
**Verified:** 2026-04-08T04:19:25Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | URL ingestion persists both requested URL and redirect-resolved final URL. | ✓ VERIFIED | `SafeUrlFetcher.fetch()` returns `FetchResult(requestedUrl, finalUrl, ...)` and `IngestionOrchestrator.runPipeline()` saves both into `KbSourceFetchSnapshot.requestedUrl/finalUrl` before parsing. Verified in `/D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/ingestion/fetch/SafeUrlFetcher.java`, `/D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestrator.java`, and `/D:/ai/traffic-law-chatbot/src/test/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestratorUrlProvenanceTest.java`. |
| 2 | Unsafe URL targets are blocked before any network fetch occurs. | ✓ VERIFIED | `IngestionService.submitUrl()` calls `urlPageParser.validateHost()` before DB writes; `UrlPageParser.validateHost()` delegates to `SafeUrlFetcher.validateHost()`; `SafeUrlFetcherTest` asserts localhost, link-local, site-local, malformed, and non-http(s) URLs fail before HTTP executor use. |
| 3 | URL HTML ingestion uses a SafeUrlFetcher/FetchResult trust boundary and parses HTML through Spring AI behind the repo-owned domain model. | ✓ VERIFIED | `UrlPageParser` is a facade over `SafeUrlFetcher` + `SpringAiHtmlParser`; `SpringAiHtmlParser.parse(FetchResult)` maps Spring AI output into `ParsedDocument` and does not expose Spring AI `Document` outside the adapter. |
| 4 | Spring AI PDF adoption is bounded to the PDF file path and preserves page-aware provenance only behind `ParsedDocument`. | ✓ VERIFIED | `IngestionOrchestrator.selectFileParser()` routes to `SpringAiPdfParser` only when `isSupported(mimeType, fileName)` is true; parser maps each page into `ParsedDocument.PageSection(pageNumber, "page-n", text)`; non-PDFs fall back to Tika. Verified by `SpringAiPdfParserParityTest` and `IngestionOrchestratorPdfParserSelectionTest`. |
| 5 | Indexed metadata remains compatible with retrieval gating and chunk inspection. | ✓ VERIFIED | `buildMetadata()` still writes `approvalState`, `trusted`, `active`, `origin`, `chunkOrdinal`, `pageNumber`, `sectionRef`, `contentHash`, `processingVersion`; `RetrievalPolicy.RETRIEVAL_FILTER` still requires approved+trusted+active; `ChunkInspectionService` still reads the same metadata keys. |
| 6 | Chunking behavior remains unchanged in this phase. | ✓ VERIFIED | `TextChunker` chunk size, overlap, ordinal, pageNumber, sectionRef, and contentHash logic are unchanged; both HTML and PDF changes feed existing `ParsedDocument` sections into the same `textChunker.chunk(...)` path. |
| 7 | Async job visibility and external ingestion APIs remain unchanged. | ✓ VERIFIED | `IngestionAdminController` endpoints are unchanged; `IngestionService.submitUpload/submitUrl()` still create queued jobs and call `orchestrator.runPipeline(jobId)`; `IngestionOrchestrator` still transitions FETCH → PARSE → CHUNK → EMBED → INDEX → FINALIZE. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `/D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/ingestion/fetch/FetchResult.java` | Explicit requested/final URL provenance contract | ✓ VERIFIED | Exists, substantive record fields include requested/final URL, status, content type, title hint, body, etag, lastModified. Wired from `SafeUrlFetcher` to `IngestionOrchestrator`. |
| `/D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/ingestion/fetch/SafeUrlFetcher.java` | SSRF-safe fetch boundary with redirect-aware response capture | ✓ VERIFIED | Exists, validates scheme/host before fetch, manually follows bounded redirects, extracts title/headers, returns `FetchResult`. Used by `IngestionOrchestrator` and `UrlPageParser`. |
| `/D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiHtmlParser.java` | Spring AI HTML adapter behind `ParsedDocument` | ✓ VERIFIED | Exists, substantive mapping into repo-owned `ParsedDocument`, explicit parser name/version, single-section HTML semantics, wired through `UrlPageParser.parseFetchedPage()`. |
| `/D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/ingestion/parser/UrlPageParser.java` | URL parser facade consuming fetched content | ✓ VERIFIED | Exists, no longer owns provenance persistence, delegates validation to `SafeUrlFetcher` and parsing to `SpringAiHtmlParser`. |
| `/D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiPdfParser.java` | Page-aware Spring AI PDF parser behind `ParsedDocument` | ✓ VERIFIED | Exists, substantive PDF-only adapter, maps each Spring AI page document into `ParsedDocument.PageSection` with `page-n` refs, explicit parser name/version, wired via orchestrator selection. |
| `/D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestrator.java` | Stable orchestration, snapshot persistence, parser selection, metadata compatibility | ✓ VERIFIED | Exists, substantive async pipeline, URL fetch snapshot write, bounded parser selection, unchanged chunk/index flow. |
| `/D:/ai/traffic-law-chatbot/src/test/java/com/vn/traffic/chatbot/ingestion/fetch/SafeUrlFetcherTest.java` | Redirect and SSRF regression coverage | ✓ VERIFIED | Covers redirect provenance, pre-fetch unsafe host rejection, and fetch metadata fields. |
| `/D:/ai/traffic-law-chatbot/src/test/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestratorUrlProvenanceTest.java` | URL provenance persistence coverage | ✓ VERIFIED | Verifies requested/final URL snapshot persistence and step progression. |
| `/D:/ai/traffic-law-chatbot/src/test/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiHtmlParserTest.java` | HTML adapter mapping and parser provenance coverage | ✓ VERIFIED | Verifies title extraction, parser identity, single-page semantics, and repo-owned return type. |
| `/D:/ai/traffic-law-chatbot/src/test/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestratorUrlHtmlPipelineTest.java` | Hybrid URL HTML pipeline coverage | ✓ VERIFIED | Verifies fetch → snapshot save → parse → chunk → index order plus metadata compatibility. |
| `/D:/ai/traffic-law-chatbot/src/test/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiPdfParserParityTest.java` | PDF provenance and chunk metadata parity coverage | ✓ VERIFIED | Verifies page-aware section mapping, parser identity/version, and chunk metadata compatibility. |
| `/D:/ai/traffic-law-chatbot/src/test/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestratorPdfParserSelectionTest.java` | Bounded PDF parser-selection coverage | ✓ VERIFIED | Verifies Spring AI PDF for PDFs, Tika fallback for non-PDFs and unsupported paths, and unchanged metadata keys. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `SafeUrlFetcher.fetch` | `KbSourceFetchSnapshot.finalUrl` | `FetchResult` carried into orchestrator snapshot persistence | ✓ WIRED | `IngestionOrchestrator.runPipeline()` fetches URL, then saves `requestedUrl(fetchResult.requestedUrl())` and `finalUrl(fetchResult.finalUrl())`. |
| `UrlPageParser` | `ParsedDocument` | ParsedDocument built from already-fetched HTML | ✓ WIRED | `UrlPageParser.parseFetchedPage(FetchResult)` delegates to `SpringAiHtmlParser.parse(fetchResult)` which returns `new ParsedDocument(...)`. |
| `SafeUrlFetcher.fetch` | `SpringAiHtmlParser.parse` | `FetchResult` body/title/contentType passed into parser adapter | ✓ WIRED | URL_IMPORT branch does safe fetch first, then `urlPageParser.parseFetchedPage(fetchResult)`. |
| `SpringAiHtmlParser.parse` | `ParsedDocument` | Anti-corruption mapping preserving parser identity and sections | ✓ WIRED | Adapter joins Spring AI document text, selects title, and returns repo-owned `ParsedDocument` with `PageSection(1, "full", rawText)`. |
| `IngestionOrchestrator` | `TikaDocumentParser / SpringAiPdfParser` | Bounded parser selection for PDFs only | ✓ WIRED | `selectFileParser()` chooses Spring AI only when `springAiPdfParser.isSupported(...)` is true; otherwise returns Tika. |
| `SpringAiPdfParser.parse` | `ParsedDocument.PageSection` | Page-aware mapping of PDF reader output | ✓ WIRED | Each Spring AI PDF page becomes `new ParsedDocument.PageSection(pageNumber, "page-" + pageNumber, text)`. |
| `ParsedDocument sections` | `TextChunker.chunk` | Existing chunk/audit metadata path remains unchanged | ✓ WIRED | Both URL and file ingestion call `textChunker.chunk(parsedDoc, ...)` with no chunker changes. |
| `IngestionOrchestrator.buildMetadata` | `RetrievalPolicy.RETRIEVAL_FILTER` | approval/trusted/active metadata compatibility | ✓ WIRED | New chunks are indexed with `approvalState`, `trusted`, `active`; retrieval filter still depends on those exact keys/values. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `IngestionOrchestrator` URL path | `fetchResult.finalUrl`, `parsedDoc`, vector metadata | `SafeUrlFetcher.fetch()` then `UrlPageParser.parseFetchedPage(fetchResult)` | Yes | ✓ FLOWING |
| `SpringAiHtmlParser` | `rawText`, `title`, `sections` | `JsoupDocumentReader.get()` over fetched HTML body | Yes | ✓ FLOWING |
| `SpringAiPdfParser` | `sections`, `rawText` | `PagePdfDocumentReader.get()` over uploaded PDF bytes | Yes | ✓ FLOWING |
| `ChunkInspectionService` compatibility | metadata keys like `pageNumber`, `sectionRef`, `contentHash` | `IngestionOrchestrator.buildMetadata()` from `ChunkResult` | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Safe fetch boundary and redirect provenance | `./gradlew test --tests "com.vn.traffic.chatbot.ingestion.fetch.SafeUrlFetcherTest" --tests "com.vn.traffic.chatbot.ingestion.orchestrator.IngestionOrchestratorUrlProvenanceTest"` | User provided as already passed on current `main` | ✓ PASS |
| Hybrid URL HTML path | `./gradlew test --tests "com.vn.traffic.chatbot.ingestion.parser.springai.SpringAiHtmlParserTest" --tests "com.vn.traffic.chatbot.ingestion.orchestrator.IngestionOrchestratorUrlHtmlPipelineTest"` | User provided as already passed on current `main` | ✓ PASS |
| Bounded PDF parser path | `./gradlew test --tests "com.vn.traffic.chatbot.ingestion.parser.springai.SpringAiPdfParserParityTest" --tests "com.vn.traffic.chatbot.ingestion.orchestrator.IngestionOrchestratorPdfParserSelectionTest"` | User provided as already passed on current `main` | ✓ PASS |
| Service/API regression and compilation | `./gradlew test --tests "com.vn.traffic.chatbot.ingestion.service.IngestionServiceTest"` and `./gradlew compileJava` | User provided as already passed on current `main` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| PLAT-01 | 01.1-01, 01.1-02 | Backend exposes core chatbot/admin capabilities through REST APIs | ✓ SATISFIED | Ingestion REST controller/service signatures unchanged; async orchestration still behind existing admin endpoints. |
| KNOW-01 | 01.1-03 | Admin can ingest PDF legal documents | ✓ SATISFIED | PDF file path remains supported; Spring AI PDF parser selected only for PDFs with Tika fallback available. |
| KNOW-02 | 01.1-03 | Admin can ingest Word legal documents | ✓ SATISFIED | Non-PDF file uploads still fall back to `TikaDocumentParser`; `IngestionService.resolveSourceType()` and orchestrator selection preserve Word ingestion path. |
| KNOW-03 | 01.1-03 | Admin can ingest structured regulation documents | ✓ SATISFIED | Non-PDF/structured documents still go through Tika-backed upload path with unchanged public APIs. |
| KNOW-04 | 01.1-01, 01.1-02 | Admin can ingest website content from trusted sources | ✓ SATISFIED | URL_IMPORT still exists, SSRF validation preserved, safe fetch + HTML parse path verified. |
| KNOW-05 | 01.1-01, 01.1-02, 01.1-03 | Ingested knowledge items retain provenance metadata including source type and origin | ✓ SATISFIED | Requested/final URL persisted; parser name/version persisted; chunk metadata still includes origin and location fields. |
| KNOW-06 | 01.1-02 | Only active and trusted knowledge sources are eligible for retrieval | ✓ SATISFIED | `RetrievalPolicy.RETRIEVAL_FILTER` unchanged; new chunk metadata still writes `trusted=false` and `active=false` until approval/activation. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `/D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/ingestion/parser/DocumentParser.java` | 9-11 | Default `parse(FetchResult)` throws `UnsupportedOperationException` | ℹ️ Info | Intentional boundary: only HTML adapter supports `FetchResult`. Not a stub because wired callers use the correct implementation. |
| `/D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiHtmlParser.java` | 34-36 | `parse(InputStream, ...)` throws `UnsupportedOperationException` | ℹ️ Info | Intentional adapter narrowing. Not user-visible and not a blocker. |
| `/D:/ai/traffic-law-chatbot/src/main/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiPdfParser.java` | 31-34 | PDF-only parser rejects non-PDFs | ℹ️ Info | Intentional bounded adoption. Backed by orchestrator selection tests. |

### Human Verification Required

None.

### Gaps Summary

No blocking gaps found against the current `main` branch state in `D:/ai/traffic-law-chatbot`.

Requested verdicts:
- Final URL provenance fix: confirmed.
- `SafeUrlFetcher` / `FetchResult` trust boundary: confirmed.
- Spring AI HTML parser URL path: confirmed.
- Spring AI PDF bounded path: confirmed.
- Metadata / retrieval compatibility: confirmed.
- Unchanged chunking and external ingestion APIs: confirmed.

Disconfirmation notes from the verification pass:
1. `IngestionOrchestratorPdfParserSelectionTest` proves parser-selection wiring with mocks, but not real file I/O through `loadFileStream()`; that is a test-scope limitation, not a current gap.
2. No focused test was found for redirect loops or missing `Location` headers in `SafeUrlFetcher`; current redirect handling exists in code, but that specific error path is not directly covered.
3. The helper `gsd-tools verify artifacts/key-links` reported false negatives because it resolved plan paths relative to a different working directory; manual file/path verification against `D:/ai/traffic-law-chatbot` confirmed the actual artifacts and wiring on disk.

---

_Verified: 2026-04-08T04:19:25Z_
_Verifier: Claude (gsd-verifier)_
