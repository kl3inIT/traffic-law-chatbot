---
phase: 01.1-refactor-ingestion-parser-architecture-to-a-hybrid-spring-ai
plan: 03
type: execute
wave: 3
depends_on: ["01.1-02"]
files_modified:
  - build.gradle
  - src/main/java/com/vn/traffic/chatbot/ingestion/parser/DocumentParser.java
  - src/main/java/com/vn/traffic/chatbot/ingestion/parser/TikaDocumentParser.java
  - src/main/java/com/vn/traffic/chatbot/ingestion/parser/ParsedDocument.java
  - src/main/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiPdfParser.java
  - src/main/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestrator.java
  - src/test/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiPdfParserParityTest.java
  - src/test/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestratorPdfParserSelectionTest.java
autonomous: true
requirements: [KNOW-01, KNOW-02, KNOW-03, KNOW-05]
must_haves:
  truths:
    - "PDF and other file ingestion paths keep page/location provenance and parser auditability after the evaluation work."
    - "The system adopts a Spring AI PDF path only if parity tests prove it preserves page-aware provenance and existing admin/retrieval compatibility."
    - "Chunking behavior remains unchanged in this phase."
  artifacts:
    - path: "src/main/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiPdfParser.java"
      provides: "Optional page-aware Spring AI PDF parser behind ParsedDocument"
      exports: ["SpringAiPdfParser"]
    - path: "src/test/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiPdfParserParityTest.java"
      provides: "Automated parity checks for page metadata and parser output compatibility"
      exports: ["SpringAiPdfParserParityTest"]
    - path: "src/main/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestrator.java"
      provides: "Deterministic parser selection that keeps APIs stable"
      exports: ["IngestionOrchestrator"]
  key_links:
    - from: "SpringAiPdfParser.parse"
      to: "ParsedDocument.PageSection"
      via: "page-aware mapping of PDF reader output"
      pattern: "PageSection"
    - from: "IngestionOrchestrator"
      to: "TikaDocumentParser / SpringAiPdfParser"
      via: "bounded parser selection for PDFs only"
      pattern: "application/pdf"
    - from: "ParsedDocument sections"
      to: "TextChunker.chunk"
      via: "existing chunk/audit metadata path remains unchanged"
      pattern: "chunk(ParsedDocument"
---

<objective>
Run the bounded PDF follow-on from D-04 and D-15: evaluate and optionally adopt a page-aware Spring AI PDF parser behind the existing `ParsedDocument` boundary only if automated parity checks prove provenance compatibility.

Purpose: Explore Spring AI PDF ETL value without stacking a broad rewrite onto the urgent URL work, and without changing the chunker or public APIs.
Output: PDF parser parity tests, a guarded Spring AI PDF adapter, and deterministic parser selection that preserves current metadata guarantees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-refactor-ingestion-parser-architecture-to-a-hybrid-spring-ai/01.1-CONTEXT.md
@.planning/phases/01.1-refactor-ingestion-parser-architecture-to-a-hybrid-spring-ai/01.1-RESEARCH.md
@.planning/phases/01.1-refactor-ingestion-parser-architecture-to-a-hybrid-spring-ai/01.1-02-PLAN.md
@src/main/java/com/vn/traffic/chatbot/ingestion/parser/TikaDocumentParser.java
@src/main/java/com/vn/traffic/chatbot/ingestion/parser/ParsedDocument.java
@src/main/java/com/vn/traffic/chatbot/ingestion/chunking/TextChunker.java
@src/main/java/com/vn/traffic/chatbot/chunk/service/ChunkInspectionService.java

<interfaces>
From `src/main/java/com/vn/traffic/chatbot/ingestion/parser/TikaDocumentParser.java`:
```java
private List<ParsedDocument.PageSection> buildSections(String rawText, String detectedMime)
```
Current behavior: PDFs split on `\f` into `page-{n}` sections; non-PDFs fall back to one `full` section.

From `src/main/java/com/vn/traffic/chatbot/ingestion/chunking/TextChunker.java`:
```java
public List<ChunkResult> chunk(ParsedDocument doc, String sourceId, String sourceVersionId,
                               String processingVersion)
```

From `src/main/java/com/vn/traffic/chatbot/chunk/service/ChunkInspectionService.java`:
```java
metadata keys read: chunkOrdinal, pageNumber, sectionRef, contentHash, processingVersion
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PDF provenance parity tests before changing parser selection</name>
  <files>src/test/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiPdfParserParityTest.java, src/test/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestratorPdfParserSelectionTest.java</files>
  <behavior>
    - Test 1: Spring AI PDF parsing must produce page-aware `ParsedDocument.PageSection` entries for multipage PDFs, with stable page numbers and section references.
    - Test 2: parser identity/version must be explicit and persistable on `KbSourceVersion`.
    - Test 3: chunking of parsed PDF output still yields non-zero `chunkOrdinal`, `pageNumber`, `sectionRef`, and `contentHash` values through the existing path.
    - Test 4: non-PDF and unsupported file paths continue to use the current Tika parser.
  </behavior>
  <action>Write parity tests first, per D-04 and D-13, around representative PDF fixtures already available in test resources or add minimal fixtures if missing. The tests must define the adoption bar explicitly: Spring AI PDF output is acceptable only if page provenance, parser audit fields, and downstream chunk metadata remain compatible with current inspection/admin expectations. Do not modify chunking behavior, metadata keys, or retrieval filters in this task.</action>
  <verify>
    <automated>./gradlew test --tests "com.vn.traffic.chatbot.ingestion.parser.springai.SpringAiPdfParserParityTest" --tests "com.vn.traffic.chatbot.ingestion.orchestrator.IngestionOrchestratorPdfParserSelectionTest"</automated>
  </verify>
  <done>Automated parity gates exist and fail unless a Spring AI PDF path preserves the current provenance/audit contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add guarded Spring AI PDF adapter and deterministic parser selection</name>
  <files>build.gradle, src/main/java/com/vn/traffic/chatbot/ingestion/parser/DocumentParser.java, src/main/java/com/vn/traffic/chatbot/ingestion/parser/TikaDocumentParser.java, src/main/java/com/vn/traffic/chatbot/ingestion/parser/ParsedDocument.java, src/main/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiPdfParser.java, src/main/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestrator.java, src/test/java/com/vn/traffic/chatbot/ingestion/parser/springai/SpringAiPdfParserParityTest.java, src/test/java/com/vn/traffic/chatbot/ingestion/orchestrator/IngestionOrchestratorPdfParserSelectionTest.java</files>
  <behavior>
    - Test 1: when the file is a PDF and the Spring AI adapter satisfies parity, the orchestrator/parser-selection path uses it and persists parser name/version accordingly.
    - Test 2: when the file is not a PDF, or parity-required behavior is unavailable, the code continues using `TikaDocumentParser`.
    - Test 3: existing file ingestion service/orchestrator tests keep passing, proving no API or job-flow regression.
  </behavior>
  <action>Implement a bounded parser-selection seam for file ingestion only. In `build.gradle`, add the explicit dependency line `implementation 'org.springframework.ai:spring-ai-pdf-document-reader'` because the current build does not include the Spring AI PDF reader module. Add `SpringAiPdfParser` behind the existing parser abstraction, using Spring AI's page-aware PDF reader only if its output can be mapped into `ParsedDocument.PageSection` with page numbers and section refs that satisfy Task 1 tests. Do not add `spring-ai-tika-document-reader` in this phase unless the plan is explicitly expanded to migrate the non-PDF Tika file path; this plan is PDF evaluation only. If the adapter cannot satisfy the parity tests cleanly, keep `TikaDocumentParser` as the active production parser and land the adapter/evaluation harness as non-selected code with explicit comments explaining why; do not weaken metadata or hide the outcome. Preserve all external ingestion endpoints and keep `TextChunker` unchanged per D-04 and D-06.</action>
  <verify>
    <automated>./gradlew test --tests "com.vn.traffic.chatbot.ingestion.parser.springai.SpringAiPdfParserParityTest" --tests "com.vn.traffic.chatbot.ingestion.orchestrator.IngestionOrchestratorPdfParserSelectionTest" --tests "com.vn.traffic.chatbot.ingestion.service.IngestionServiceTest"</automated>
  </verify>
  <done>The codebase has a deterministic, provenance-safe PDF parser selection path, adoption is allowed only when parity gates pass, and chunker behavior remains unchanged in this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Uploaded file bytes -> PDF parser | Untrusted document content crosses into file parsing internals. |
| PDF parser -> ParsedDocument | Library-specific page output is translated into repo-owned provenance structures. |
| ParsedDocument -> TextChunker | Page/location information must survive into chunk audit metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-12 | T | PDF page provenance mapping | mitigate | Require parity tests for page numbers, section refs, and parser audit fields before any Spring AI PDF path becomes active. |
| T-01.1-13 | D | PDF parser resource usage | mitigate | Keep current bounded parsing safeguards and verify the new adapter does not bypass existing file-ingestion failure handling. |
| T-01.1-14 | T | chunk metadata compatibility | mitigate | Leave `TextChunker` unchanged and assert chunk metadata fields remain populated from parsed PDF sections. |
| T-01.1-15 | R | parser selection outcome | mitigate | Persist parser name/version so admins can see whether Tika or Spring AI produced a source version. |
| T-01.1-16 | E | broad rewrite creep | mitigate | Restrict this plan to PDF evaluation/selection only; do not change URL fetch, HTML parsing, endpoints, or chunker strategy. |
</threat_model>

<verification>
- `./gradlew test --tests "com.vn.traffic.chatbot.ingestion.parser.springai.SpringAiPdfParserParityTest" --tests "com.vn.traffic.chatbot.ingestion.orchestrator.IngestionOrchestratorPdfParserSelectionTest"`
- `./gradlew test --tests "com.vn.traffic.chatbot.ingestion.service.IngestionServiceTest"`
- `./gradlew compileJava`
</verification>

<success_criteria>
- PDF evaluation is automated and provenance-focused rather than a blind parser swap.
- Spring AI PDF adoption happens only if page provenance and downstream metadata parity are proven.
- Non-PDF file ingestion and current APIs remain stable.
- Chunker behavior is unchanged and explicitly deferred beyond this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-refactor-ingestion-parser-architecture-to-a-hybrid-spring-ai/01.1-03-SUMMARY.md`.
</output>
