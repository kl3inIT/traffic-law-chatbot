---
phase: 06-audit-real-data-validation-and-stabilization
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02-PLAN.md"
files_modified:
  - src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java
  - src/main/resources/default-parameter-set.yml
autonomous: false
requirements:
  - ADMIN-04
  - ADMIN-05
  - CHAT-01
  - CHAT-03
  - LEGAL-01
  - LEGAL-02
  - LEGAL-03
  - LEGAL-04

must_haves:
  truths:
    - "20+ check definitions exist, covering all 5 D-05 categories with at least 3 definitions each"
    - "A check run completes with status COMPLETED and averageScore computed"
    - "All check results with score < 0.6 have been investigated to a named root cause layer"
    - "AUDIT.md exists in the phase directory with one row per investigated result"
    - "All investigated results with fixable root causes have been fixed (D-08 — nothing deferred if fixable)"
  artifacts:
    - path: ".planning/phases/06-audit-real-data-validation-and-stabilization/AUDIT.md"
      provides: "Investigation findings per low-scoring check result"
      contains: "| Root cause layer |"
    - path: ".planning/phases/06-audit-real-data-validation-and-stabilization/AUDIT.md"
      contains: "| question |"
  key_links:
    - from: "POST /api/v1/admin/check-runs/trigger"
      to: "CheckRunner.runAll()"
      via: "async ingestionExecutor"
      pattern: "Returns 202; poll GET /check-runs/{id} for status=COMPLETED"
    - from: "CheckRunner"
      to: "ChatService.answer()"
      via: "per-def question execution"
      pattern: "Reads active AiParameterSet at trigger time"
    - from: "ChatLog.pipelineLog"
      to: "root cause investigation"
      via: "ChatLog lookup by question + timestamp"
      pattern: "grounding_status + retrieved_chunks fields"
---

<objective>
Author 20+ production-grade check definitions (D-04, D-05), trigger a check run against real ingested data (D-06), investigate all scores below the 0.6 floor (D-07), remediate root causes (D-08), and produce AUDIT.md (D-09).

Purpose: This is the core quality gate. The check run measures whether the system can accurately answer Vietnamese traffic law questions against the real legal sources ingested in Plan 02. Low-scoring results reveal real defects that must be fixed before milestone close.

Output:
- 20+ check definitions in the database covering all D-05 categories
- At least one completed check run with averageScore >= 0.75 (D-06 threshold)
- AUDIT.md with one investigation row per result below 0.6
- Any config/prompt/code fixes applied and documented
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-audit-real-data-validation-and-stabilization/06-CONTEXT.md
@.planning/phases/06-audit-real-data-validation-and-stabilization/06-RESEARCH.md
@.planning/phases/06-audit-real-data-validation-and-stabilization/06-02-SUMMARY.md

@src/main/java/com/vn/traffic/chatbot/checks/service/CheckRunner.java
@src/main/java/com/vn/traffic/chatbot/checks/evaluator/LlmSemanticEvaluator.java
@src/main/java/com/vn/traffic/chatbot/chat/service/ChatPromptFactory.java
@src/main/java/com/vn/traffic/chatbot/checks/api/CheckDefAdminController.java
@src/main/java/com/vn/traffic/chatbot/checks/api/dto/CheckDefRequest.java

<interfaces>
<!-- CheckDef REST API -->
POST /api/v1/admin/check-defs
Content-Type: application/json
Body: { "question": "...", "referenceAnswer": "...", "category": "...", "active": true }
Response 201: { "id": "uuid", "question": "...", "referenceAnswer": "...", "active": true }

PUT /api/v1/admin/check-defs/{id}
Body: { "question": "...", "referenceAnswer": "...", "category": "...", "active": true }
Response 200: updated CheckDefResponse

<!-- CheckRun REST API -->
POST /api/v1/admin/check-runs/trigger
Response 202: { "runId": "uuid" }

GET /api/v1/admin/check-runs/{runId}
Response: { "id": "uuid", "status": "RUNNING|COMPLETED|FAILED", "averageScore": 0.0-1.0, "totalDefs": N }

GET /api/v1/admin/check-runs/{runId}/results
Response: [ { "checkDefId": "uuid", "question": "...", "actualAnswer": "...", "score": 0.0-1.0, "log": "..." } ]

<!-- ChatLog lookup for root cause investigation -->
GET /api/v1/admin/chat-logs?question={encoded-question}
Response: [ { "id": "uuid", "question": "...", "groundingStatus": "...", "retrievedChunks": N, "pipelineLog": "...", "sources": "..." } ]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 20+ check definitions via REST — all 5 D-05 categories, production-grade questions and reference answers</name>

  <read_first>
    - .planning/phases/06-audit-real-data-validation-and-stabilization/06-RESEARCH.md (§Check Definitions — sample questions, reference answer guidelines, category matrix)
    - .planning/phases/06-audit-real-data-validation-and-stabilization/06-02-SUMMARY.md (what was ingested — reference answers must align with actual ingested content)
    - src/main/java/com/vn/traffic/chatbot/checks/api/dto/CheckDefRequest.java (DTO fields — confirm category field type)
  </read_first>

  <action>
Author check definitions one by one via POST /api/v1/admin/check-defs. Reference answers must be written AFTER reviewing the actual ingested documents (available in kb_vector_store content column after ingestion). Use spot-check queries from Plan 02 to find real article text to anchor reference answers.

CRITICAL: Reference answers must cite specific article numbers and decree names. Generic answers will score unreliably. Bad: "Mức phạt tùy hành vi". Good: "Theo Điều 6, Nghị định 168/2024/NĐ-CP, người điều khiển xe máy vượt đèn đỏ bị phạt từ 4.000.000 đến 6.000.000 đồng."

--- CATEGORY 1: Fine amounts / penalty tiers (minimum 6 definitions) ---

DEF 01:
```json
{
  "question": "Mức phạt đối với người điều khiển xe máy vượt đèn đỏ theo quy định hiện hành là bao nhiêu?",
  "referenceAnswer": "[Look up actual amount from NĐ 168/2024 Điều 6 — insert real article reference and fine range after reviewing ingested chunks]",
  "category": "FINE_AMOUNTS",
  "active": true
}
```

DEF 02:
```json
{
  "question": "Người lái ô tô có nồng độ cồn vượt mức cho phép bị phạt bao nhiêu tiền theo Nghị định 168/2024?",
  "referenceAnswer": "[Look up from NĐ 168/2024 — nồng độ cồn levels and fine ranges for ô tô]",
  "category": "FINE_AMOUNTS",
  "active": true
}
```

DEF 03:
```json
{
  "question": "Mức phạt đối với xe ô tô chạy quá tốc độ 20km/h trong khu dân cư là bao nhiêu?",
  "referenceAnswer": "[Look up from NĐ 168/2024 or NĐ 100/2019 — tốc độ violation for ô tô in khu dân cư]",
  "category": "FINE_AMOUNTS",
  "active": true
}
```

DEF 04:
```json
{
  "question": "Người điều khiển xe máy không đội mũ bảo hiểm bị xử phạt thế nào?",
  "referenceAnswer": "[Look up from NĐ 168/2024 — mũ bảo hiểm fine for xe máy]",
  "category": "FINE_AMOUNTS",
  "active": true
}
```

DEF 05:
```json
{
  "question": "Phạt bao nhiêu nếu xe máy không có gương chiếu hậu khi tham gia giao thông?",
  "referenceAnswer": "[Look up from NĐ 168/2024 — thiếu gương chiếu hậu]",
  "category": "FINE_AMOUNTS",
  "active": true
}
```

DEF 06:
```json
{
  "question": "Xe ô tô không mang theo giấy phép lái xe khi bị CSGT kiểm tra bị phạt bao nhiêu?",
  "referenceAnswer": "[Look up from NĐ 168/2024 — không mang bằng lái xe]",
  "category": "FINE_AMOUNTS",
  "active": true
}
```

--- CATEGORY 2: Documents / procedures (minimum 4 definitions) ---

DEF 07:
```json
{
  "question": "Cần mang theo giấy tờ gì khi bị cảnh sát giao thông dừng xe kiểm tra?",
  "referenceAnswer": "[Look up required documents from Luật GTĐB 2008 or NĐ 168/2024 — đăng ký xe, giấy phép lái xe, bảo hiểm]",
  "category": "DOCUMENTS_PROCEDURES",
  "active": true
}
```

DEF 08:
```json
{
  "question": "Thủ tục nộp phạt vi phạm giao thông như thế nào? Cần làm gì sau khi bị lập biên bản?",
  "referenceAnswer": "[Look up procedure from NĐ 168/2024 or Luật GTĐB 2008 — biên bản, thời hạn nộp phạt]",
  "category": "DOCUMENTS_PROCEDURES",
  "active": true
}
```

DEF 09:
```json
{
  "question": "Làm sao để lấy lại xe bị tạm giữ do vi phạm giao thông?",
  "referenceAnswer": "[Look up procedure for xe bị tạm giữ — giấy tờ cần thiết, nơi nộp]",
  "category": "DOCUMENTS_PROCEDURES",
  "active": true
}
```

DEF 10:
```json
{
  "question": "Giấy phép lái xe hết hạn bị xử phạt thế nào và thủ tục gia hạn ở đâu?",
  "referenceAnswer": "[Look up fine for bằng lái hết hạn from NĐ 168/2024 + gia hạn procedure]",
  "category": "DOCUMENTS_PROCEDURES",
  "active": true
}
```

--- CATEGORY 3: Refusal boundary (minimum 4 definitions) ---

DEF 11:
```json
{
  "question": "Thuế thu nhập cá nhân tính như thế nào?",
  "referenceAnswer": "Câu hỏi này nằm ngoài phạm vi hỗ trợ của hệ thống. Hệ thống chỉ hỗ trợ thông tin về pháp luật giao thông đường bộ Việt Nam. Vui lòng liên hệ cơ quan thuế hoặc chuyên gia pháp lý về thuế.",
  "category": "REFUSAL_BOUNDARY",
  "active": true
}
```

DEF 12:
```json
{
  "question": "Luật hôn nhân gia đình quy định độ tuổi kết hôn là bao nhiêu?",
  "referenceAnswer": "Câu hỏi này nằm ngoài phạm vi hỗ trợ của hệ thống. Hệ thống chỉ hỗ trợ thông tin về pháp luật giao thông đường bộ Việt Nam.",
  "category": "REFUSAL_BOUNDARY",
  "active": true
}
```

DEF 13:
```json
{
  "question": "Dự báo thời tiết Hà Nội ngày mai thế nào?",
  "referenceAnswer": "Câu hỏi này không liên quan đến pháp luật giao thông. Hệ thống không thể cung cấp thông tin thời tiết.",
  "category": "REFUSAL_BOUNDARY",
  "active": true
}
```

DEF 14:
```json
{
  "question": "Mức phạt vi phạm luật hàng hải Việt Nam là gì?",
  "referenceAnswer": "Câu hỏi này nằm ngoài phạm vi hỗ trợ. Hệ thống chỉ hỗ trợ pháp luật giao thông đường bộ, không bao gồm luật hàng hải.",
  "category": "REFUSAL_BOUNDARY",
  "active": true
}
```

--- CATEGORY 4: Multi-turn scenario (minimum 4 definitions) ---

DEF 15:
```json
{
  "question": "Tôi đang đi xe và bị CSGT dừng lại, tôi phải làm gì?",
  "referenceAnswer": "Câu trả lời phải hỏi làm rõ loại phương tiện (vehicleType) hoặc hành vi vi phạm (violationType) trước khi đưa ra câu trả lời cụ thể. Response mode phải là CLARIFICATION_NEEDED.",
  "category": "MULTI_TURN_SCENARIO",
  "active": true
}
```

DEF 16:
```json
{
  "question": "Xe của tôi bị tạm giữ, phạt bao nhiêu?",
  "referenceAnswer": "Câu trả lời phải hỏi rõ loại phương tiện (vehicleType) vì mức phạt khác nhau cho xe máy và ô tô. Response mode CLARIFICATION_NEEDED với câu hỏi về loại xe.",
  "category": "MULTI_TURN_SCENARIO",
  "active": true
}
```

DEF 17:
```json
{
  "question": "Tôi vi phạm giao thông, bị phạt tiền thì cần làm gì?",
  "referenceAnswer": "Câu trả lời phải yêu cầu làm rõ cả loại phương tiện và hành vi vi phạm cụ thể. Response mode CLARIFICATION_NEEDED. Không được đưa ra số tiền phạt chung chung khi thiếu thông tin.",
  "category": "MULTI_TURN_SCENARIO",
  "active": true
}
```

DEF 18:
```json
{
  "question": "Không phải xe máy, tôi đi ô tô vượt đèn đỏ thì bị phạt bao nhiêu?",
  "referenceAnswer": "[Look up ô tô vượt đèn đỏ from NĐ 168/2024 — this tests negation correction AND fine lookup. Answer should apply ô tô rules, cite NĐ 168/2024]",
  "category": "MULTI_TURN_SCENARIO",
  "active": true
}
```

--- CATEGORY 5: LIMITED_GROUNDING edge cases (minimum 4 definitions) ---

DEF 19:
```json
{
  "question": "Nghị định 168/2024 có thay thế hoàn toàn Nghị định 100/2019 không?",
  "referenceAnswer": "Câu trả lời có thể ở mức LIMITED_GROUNDING hoặc GROUNDED tùy vào nguồn đã nạp. Nếu GROUNDED: giải thích quan hệ giữa hai nghị định, NĐ 168 thay thế một số điều của NĐ 100. Nếu LIMITED_GROUNDING: thông báo nguồn hạn chế và đề nghị kiểm tra văn bản chính thức.",
  "category": "LIMITED_GROUNDING",
  "active": true
}
```

DEF 20:
```json
{
  "question": "Mức phạt đối với người điều khiển xe thô sơ không có đèn chiếu sáng vào ban đêm?",
  "referenceAnswer": "Câu trả lời có thể ở mức LIMITED_GROUNDING (xe thô sơ là trường hợp đặc thù ít xuất hiện trong nguồn chính). Nếu có thông tin: trích dẫn điều khoản cụ thể. Nếu không đủ căn cứ: thông báo LIMITED_GROUNDING và đề nghị tham khảo văn bản gốc.",
  "category": "LIMITED_GROUNDING",
  "active": true
}
```

DEF 21:
```json
{
  "question": "Thủ tục xử lý vi phạm giao thông của người nước ngoài tại Việt Nam?",
  "referenceAnswer": "Câu trả lời có thể ở LIMITED_GROUNDING vì quy định riêng cho người nước ngoài ít xuất hiện trong nguồn đã nạp. Không được bịa đặt quy định. Nếu thiếu căn cứ: thông báo LIMITED_GROUNDING.",
  "category": "LIMITED_GROUNDING",
  "active": true
}
```

DEF 22:
```json
{
  "question": "Theo Điều 99b Nghị định 168/2024, mức phạt là bao nhiêu?",
  "referenceAnswer": "Điều 99b không tồn tại trong Nghị định 168/2024. Câu trả lời phải từ chối đưa ra thông tin về điều khoản không tồn tại (REFUSED hoặc LIMITED_GROUNDING). Không được bịa đặt nội dung Điều 99b.",
  "category": "LIMITED_GROUNDING",
  "active": true
}
```

After creating all 22 definitions, verify via:
```
GET /api/v1/admin/check-defs
```
Response must contain >= 22 active check definitions.

SECURITY NOTE: POST /api/v1/admin/check-defs requires admin auth. Reference answers are static admin-authored content. No user input interpolated.
  </action>

  <verify>
    <automated>
      Via mcp__postgres__execute_sql:
      SELECT category, COUNT(*) FROM check_def WHERE active = true GROUP BY category;
      -- must show: FINE_AMOUNTS >= 6, DOCUMENTS_PROCEDURES >= 4, REFUSAL_BOUNDARY >= 4,
      --            MULTI_TURN_SCENARIO >= 4, LIMITED_GROUNDING >= 4
      SELECT COUNT(*) FROM check_def WHERE active = true;
      -- must return >= 22
    </automated>
  </verify>

  <acceptance_criteria>
    - `SELECT COUNT(*) FROM check_def WHERE active = true` returns >= 22
    - `SELECT COUNT(*) FROM check_def WHERE category = 'FINE_AMOUNTS' AND active = true` returns >= 6
    - `SELECT COUNT(*) FROM check_def WHERE category = 'REFUSAL_BOUNDARY' AND active = true` returns >= 4
    - `SELECT COUNT(*) FROM check_def WHERE category = 'MULTI_TURN_SCENARIO' AND active = true` returns >= 4
    - `SELECT COUNT(*) FROM check_def WHERE category = 'LIMITED_GROUNDING' AND active = true` returns >= 4
    - Every definition with category FINE_AMOUNTS has a referenceAnswer containing "Điều" or "Nghị định" (must be article-grounded, not generic)
  </acceptance_criteria>

  <done>22+ active check definitions created across all 5 categories. All FINE_AMOUNTS definitions cite specific article numbers from ingested decrees.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Trigger check run, collect results, investigate all scores below 0.6 floor, produce AUDIT.md</name>

  <read_first>
    - src/main/java/com/vn/traffic/chatbot/checks/service/CheckRunner.java (async execution — check run reads active param set at trigger time)
    - src/main/java/com/vn/traffic/chatbot/checks/evaluator/LlmSemanticEvaluator.java (scoring logic — language cap, exception handling)
    - .planning/phases/06-audit-real-data-validation-and-stabilization/06-RESEARCH.md (root cause ladder: REFUSED → LIMITED_GROUNDING → GROUNDED-but-low)
  </read_first>

  <what-built>
22+ check definitions created. Active AiParameterSet has production system prompt. Ingested sources are approved/trusted/active. Ready to trigger the full check run.
  </what-built>

  <how-to-verify>
STEP 1 — Trigger check run:
```
POST /api/v1/admin/check-runs/trigger
Authorization: Basic YWRtaW46YWRtaW4=
```
Note the returned runId.

STEP 2 — Poll until COMPLETED:
```
GET /api/v1/admin/check-runs/{runId}
```
Wait for status = COMPLETED. The run is async via @Async("ingestionExecutor"). Allow up to 10 minutes for 22 definitions.

STEP 3 — Retrieve all results:
```
GET /api/v1/admin/check-runs/{runId}/results
```
Note every result with score < 0.6. Also note any result that seems semantically wrong regardless of score (D-07 qualitative trigger).

STEP 4 — Check threshold compliance (D-06):
- averageScore >= 0.75?
- No individual score < 0.6?

If FAILED: mandatory investigation proceeds. If PASSED: still investigate any qualitative anomalies.

STEP 5 — For each result below 0.6 or qualitatively suspicious, execute the root cause ladder:

A. Look up the ChatLog for the same question:
```
GET /api/v1/admin/chat-logs?question={url-encoded-question}
```
Examine: groundingStatus, retrievedChunks, pipelineLog, sources

B. Apply root cause ladder (from RESEARCH.md §Architecture Patterns):
   - groundingStatus = REFUSED → zero chunks → check metadata flags on source, or similarity threshold too high
   - groundingStatus = LIMITED_GROUNDING → 1-2 chunks → ingestion gap for this topic OR chunking issue
   - groundingStatus = GROUNDED but score < 0.6 → prompt construction issue, reference answer too strict, or evaluator scoring anomaly
   - score = 0.0 exactly → evaluator exception (check CheckResult.log field for "error:")

C. Fix the root cause:
   - Config issue (retrieval threshold, topK) → PUT /api/v1/admin/parameter-sets/{id} with adjusted values
   - Reference answer too strict → PUT /api/v1/admin/check-defs/{defId} with corrected referenceAnswer
   - Prompt issue → edit ChatPromptFactory.java or default-parameter-set.yml
   - Code bug → fix in Java source, run ./mvnw test -q to confirm no regressions, restart backend

D. After fixes: if averageScore was below 0.75, trigger a second check run to confirm the fix worked.

STEP 6 — Create AUDIT.md at `.planning/phases/06-audit-real-data-validation-and-stabilization/AUDIT.md`:

```markdown
# Phase 06 — Investigation Audit

## Check Run Results Summary

| Run # | Date | Total Defs | Avg Score | Below 0.6 | Pass/Fail |
|-------|------|-----------|-----------|-----------|-----------|
| 1     | ...  | 22        | 0.XX      | N         | PASS/FAIL |
| 2     | ...  | 22        | 0.XX      | N         | PASS/FAIL (after fixes) |

## Investigation Results

| Question | Score | Root Cause Layer | What Was Changed | Status |
|----------|-------|-----------------|------------------|--------|
| [question text] | 0.XX | ingestion / retrieval / prompt / reference_answer / evaluator | [specific change made] | fixed / deferred-[rationale] |

## DB Cleanup Summary

| Table | Rows Before | Rows After |
|-------|-------------|------------|
| kb_source | N | 0 |
| kb_vector_store | N | 0 |
| ... | | |

## Open Items

[Any deferred items with explicit rationale]
```

NOTES on AUDIT.md:
- Do NOT log raw system prompts or internal API keys in AUDIT.md (D-18 sensitivity)
- "Root cause layer" must be one of: ingestion / normalization / indexing / retrieval / prompt / context_assembly / model / evaluator_criteria
- "What was changed" must be specific: e.g. "Updated CheckDef referenceAnswer to include Điều 6 NĐ 168 citation" — not generic
- Status = "fixed" if the fix was applied and re-run confirmed score improved
- Status = "deferred-[rationale]" only if the fix is genuinely out of scope for Phase 06

SECURITY NOTE: Do not log raw PII, actual user questions from production logs, or internal credentials in AUDIT.md. The check definitions are synthetic evaluation questions, not real user data.
  </how-to-verify>

  <resume-signal>Type "approved" when AUDIT.md is complete and either (a) the final check run shows averageScore >= 0.75 with no score below 0.6, or (b) all below-0.6 results are documented with root cause and status in AUDIT.md. Describe any blocking issues found.</resume-signal>

  <acceptance_criteria>
    - `GET /api/v1/admin/check-runs/{latestRunId}` returns `"status": "COMPLETED"`
    - `GET /api/v1/admin/check-runs/{latestRunId}` returns `"averageScore"` >= 0.75 (or if below 0.75, all failing defs are documented in AUDIT.md with root cause and fix/deferral)
    - AUDIT.md exists at `.planning/phases/06-audit-real-data-validation-and-stabilization/AUDIT.md`
    - AUDIT.md contains a table with column header "Root cause layer"
    - AUDIT.md contains a table with column header "What Was Changed"
    - Every check result with score < 0.6 has a row in the AUDIT.md investigation table
    - AUDIT.md does not contain raw credentials, system prompt text, or PII
    - `./mvnw test -q` passes after any code changes applied during remediation
  </acceptance_criteria>

  <done>Check run complete with averageScore >= 0.75 (or all failures documented). AUDIT.md exists with per-result investigation rows. All fixable root causes fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin HTTP → check-runs/trigger | Triggers LLM calls for all 22 defs — admin-only, must be authenticated |
| CheckRunner → ChatService | Internal service call — no external boundary, but reads active param set |
| AUDIT.md → file system | Contains investigation findings — must not include PII or credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Spoofing | POST /api/v1/admin/check-runs/trigger | mitigate | Always include `Authorization: Basic YWRtaW46YWRtaW4=`. Verify 401 returned without auth. |
| T-06-03-02 | Information Disclosure | AUDIT.md investigation file | mitigate | Do not log raw system prompts, API keys, or user PII. Log only: question text, score, root cause category, change description. |
| T-06-03-03 | Tampering | ChatPromptFactory.java code edit | mitigate | After any code change, run `./mvnw test -q` to confirm no regression. Commit change with clear message referencing the AUDIT.md row it addresses. |
| T-06-03-04 | Denial of Service | Triggering check run with 22 defs (22 LLM calls) | accept | Admin-only operation. Each call is bounded by LLM timeout. Risk of elevated LLM cost acknowledged. |
</threat_model>

<verification>
After both tasks complete:
1. `SELECT COUNT(*) FROM check_def WHERE active = true` returns >= 22
2. `SELECT category, COUNT(*) FROM check_def GROUP BY category` shows all 5 categories with >= minimum counts
3. `GET /api/v1/admin/check-runs` shows at least one COMPLETED run
4. AUDIT.md exists with investigation table
5. `./mvnw test -q` passes green
</verification>

<success_criteria>
- 22+ production check definitions covering all 5 D-05 categories
- Completed check run with averageScore >= 0.75 and no score below 0.6 (or all failures documented)
- AUDIT.md with investigation findings for every below-threshold result
- All fixable root causes remediated (D-08 full remediation)
- Spring Boot test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-audit-real-data-validation-and-stabilization/06-03-SUMMARY.md`

Include:
- Check definition counts by category
- Check run results: averageScore, count below 0.6, count below 0.75
- Root causes found per investigation (layer: ingestion/retrieval/prompt/reference_answer/evaluator)
- Fixes applied: what changed, which files
- Deferred items with rationale
- Whether a second check run was needed and its result
</output>
