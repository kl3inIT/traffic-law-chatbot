// Admin layout — nav items live in components/layout/app-sidebar.tsx (adminNavItems array)
// Entries added in Phase 5: Lịch sử hội thoại, Kiểm tra chất lượng (ClipboardCheck), Lịch sử chạy
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
